# system-register.md Archive — Ashen Crown

Older entries moved out of `system-register.md` (by `log_archive.py`) to keep that file lean for routine session-start reading — `dm-start-session` reads it in full every session. The Core play / Known gaps tables in `system-register.md` always reflect the current, full status — archiving only moves the dated build/fix log's detailed narrative text, never a system's actual status.

| Date | Change |
|------|--------|
| 2026-08-29 | **Four map-derived Karsgate sites built** (`TODO-karsgate-map-locations.md`), closing out the deferred task from the campaign map generation earlier this session. Ground-truth check first (Step 0): confirmed the map's "Riverside Docks" label is the same thing as the already-built Riverside district — no new location built, a clarifying note added to `locations/karsgate.md` instead. Built four full site files, all kept to the map's own plain labels per DM's explicit choice (no invented flourish names): **The Mill** (an independent miller under quiet Ironclad Compact/Grain Exchange pressure — ties directly into `power-base.md`'s Marches-grain-shipments lever), **Outer Farm** (a moral-choice site, refugee laborers under exploitative but non-violent terms, no faction ties by design), **The Caravan Halt** (a recurring travel waystation on the Karsgate road, built to `the-driftway-fair.md`'s standard but themed around transit/road-news rather than trade), and **Stonebridge** (the river checkpoint on the Karsgate road — city-watch controlled but quietly pressured by the Concordat to flag "an elf and a dragonborn," directly reactivating the party's own standing identity-exposure risk from `new-capabilities.md`). All four added to `gazetteer.md`'s Karsgate entry. `name_collision_check.py` run and read by hand (not just the auto-flagged 3+ list) — found one real 2-way collision the script's threshold couldn't catch (**"The Mill" vs. the existing "The Drowned Mill,"** different regions/content) and surfaced it to the DM, who confirmed keeping both names as-is. `session_state_gate.py` clean after the batch. |
| 2026-08-29 | **`**Purpose**` line added as a required field on every NPC file, both tiers — and `npc-tier-verifier` agent built.** Two related DM requests in sequence. First: the DM proposed that Claude should ask itself "why am I creating this NPC, what will I task them with" at creation time — correct instinct, but as originally proposed it was a mental question with no visible answer, the same unenforceable-judgment shape already fixed for the Tier call itself. Converted it into a required, hookable field instead: `**Purpose**` (one-two sentences, why this NPC exists and what they'd plausibly be tasked with), added to `npc-tiers.md`'s standard, enforced by `npc_tier_check.py` (now required on both Important and Minor NPCs, not just Important). Retrofitted onto all 32 existing NPC files in the same pass — full sweep re-verified clean afterward. Second, and logically prior: the DM rejected two proposed guarantees for the Tier call's *correctness* (surfacing it in chat for the DM to catch — rejected, the DM wants the system automatic, not dependent on their own review; a keyword-heuristic scan for phrases like "wants to control" — rejected, trivially dodged by writing vaguer prose). Recognized this project already has the right pattern for "don't trust the same pass that wrote something to also grade it" — `encounter-verifier`. Built `npc-tier-verifier` (new agent, independently re-derives Important/Minor from each NPC's own content before looking at its stored Tier line, flags disagreements, never auto-corrects) and wired it into `/dm-end-session` as an unconditional step. **Could not live-test the agent in the same session it was created** — a new `.claude/agents/*.md` file isn't picked up by the Agent tool until the next session starts; disclosed to the DM directly, who confirmed testing it in the next session is fine. Added `--list` to `npc_tier_check.py` to support the verifier's full-sweep step. |
| 2026-08-29 | **NPC-tier standard converted from a written rule to a hard hook — future NPCs are now actually guaranteed to meet it.** The DM asked directly whether newly-created NPCs are guaranteed to follow `npc-tiers.md`. Honest answer at the time: no — the standard existed as prose plus two runnable-but-not-run scripts (`npc_tier_check.py`, `npc_connection_check.py`), and nothing forced either to execute. Two fixes: (1) added `npc-tiers.md` to `dm-start-session/SKILL.md`'s mandatory Step 1 reading list, the same treatment `two-player-scaling.md`/`dungeon-design.md`/`faction-design.md` already got for the identical reason (a judgment-call standard with no hook coverage needs to be guaranteed *in context*, not just guaranteed to exist as a file). (2) Built real enforcement for the diffable part of the standard: `npc_tier_hook.py` (`PostToolUse` on `Edit\|Write`, fires on any `campaigns/*/info/npcs/*.md` touch, runs `npc_tier_check.py` immediately, sets a pending flag if incomplete) plus a new `check_npc_tier()` function in `session_state_gate.py` (re-verifies every flagged file fresh at `Stop` time, blocks the turn if still incomplete, auto-clears if fixed within the same turn) plus `npc_tier_dismiss.py` for a genuine stub file not meant to be finished yet. Live-tested all three paths (block / auto-clear-on-fix / explicit dismiss) with a disposable test NPC file before wiring into `.claude/settings.json` and cleaning up. **What's now actually guaranteed**: no NPC file can leave a turn without a `**Tier**` line, and no Important one without Class & Level + (where applicable) a Move List. **What's still a judgment call, not a hook**: whether Important vs. Minor was the *correct* call, and whether the class/level or Move List content is any good — `npc-tiers.md`'s presence in the mandatory reading list is the only available mitigation for that half, per the same reasoning already established for the other three judgment-call standards. |
| 2026-08-29 | **NPC cross-reference audit — the 25 Important-tier NPCs' relationships to each other verified and synchronized.** Direct DM follow-up: same-faction goals, opposition, and betrayal potential need to actually be mirrored across both NPCs' own files, not just exist in one. Built `npc_connection_check.py` (advisory, `name_collision_check.py`'s pattern) — scans every Important-tier NPC's `npcs/<slug>.md` references and flags any not reciprocated. First run found 10 real one-directional links (Coren Ashvale ↔ Sethra Shestendeliath, Compact leadership ↔ Renata Kroll's opposition, Yeva Ashwright's stale "not yet built" reference to what's now Kessic Ambrey's file, Corla Vint ↔ Sella Dray's implied rivalry, Aldric Venn ↔ his own superior Varic Sennett, Doreth Kell ↔ his own mole Dallin Marsh, Odric Fenn ↔ Yeva Ashwright, Sefwyn Marrow ↔ Orell Tain, Corla Vint ↔ the Broker, and stale links to the brand-new Hollow Prophet file) — all fixed with reciprocal Connections entries. Re-ran clean except 3 legitimate asymmetric cases (a minor figure aware of a faction leader who has no reason to know they exist), left as-is rather than force-fixed. Added the check to `npc-tiers.md` as a standing step alongside `npc_tier_check.py`, run any time an Important NPC's Connections section changes. |
| 2026-08-29 | **Full retroactive NPC-tier sweep — all 31 existing NPC files classified and 25 built to the Important standard in one pass.** Direct DM follow-up to the `npc-tiers.md` build (see below): "scan them, who's here" (i.e., who are the Important ones). Every NPC file was checked against the Important/Minor test and reported to the DM for confirmation rather than decided unilaterally — the DM confirmed 6 as Minor (Hesta Bram, Sister Mave, Cole, Borgha Kresk, Dunnel Ashe, Alis Wend) and explicitly upgraded two borderline calls to Important (Sylvenna, Dallin Marsh) plus required a fully-built file for the Hollow Prophet despite her concealed identity ("the DM needs full knowledge so the story has no gaps," even though the party never sees it). Built `npcs/the-hollow-prophet.md` from scratch — identity (Ilyra Thorne, a shadar-kai descendant of founder Vessalyn Thorne, resolving `factions/cinder-choir.md`'s open Phase 5 bloodline question), motive, a structural mirror to Sarelle Duskbourne (a faction leader who personally lacks her own doctrine's central proof), Class & Level, and a Move List. Added 17 new `consequences.md` threshold blocks (Coren Ashvale, Mother Vey, Fennick Orle, Corran, Orvenna Kest, Corla Vint, Odric Fenn, Aldric Venn, Renata Kroll, Varic Sennett, the Broker, Sethra Shestendeliath, Vharkoss Shestendeliath, Corvin Thale, Sylvenna, Dallin Marsh, the Hollow Prophet) — closing the file's own stale "Not yet built" note that the Compact and Choir had no threshold coverage. Added Tier + Class & Level + Move List to all 24 other Important-tier files, and a `**Tier**: Minor` line with a one-line reason to the 6 confirmed-Minor files. **Found and fixed a real bug in `npc_tier_check.py` during verification**: its Move-List match required the NPC file's H1 title to exactly prefix-match the `consequences.md` heading name, which false-negatived on nearly every file with a title/honorific/nickname/subtitle ("Archivist-Magus Sarelle Duskbourne" vs. "Sarelle Duskbourne," '"Iron" Sella Dray' vs. "Sella Dray") — rewrote the match as case-insensitive substring containment, checked both directions; re-ran the full 25-file check clean afterward. Class/level choices made deliberate, not filler: Sarelle mirrors Ilvaneth's own Necromancy subclass on purpose; Sethra and Vharkoss use the level ranges their own files had already specified; Sylvenna kept the real SRD Dryad block (aged up) instead of an invented character level, per `npc-tiers.md`'s own exception for non-humanoid NPCs; non-combatants per their own established characterization (Fennick Orle, Corran) were left without a forced combat build rather than statted up artificially. |
| 2026-08-29 | **NPC Tiers standard built — Important vs Minor, PC-style class/level, and Move Lists.** DM design request, not a play bug: `npcs/kessra-vane.md` (built earlier this session via World Event roll) had a reactive Decision-logic section but nothing making her act independently — the DM's own words, an NPC with no move list is "no different from background scenery." `.claude/rules/npc-consistency.md`'s 2026-08-29 proactive-agenda addition covered half of this (a `consequences.md` threshold) but not the other two things the DM explicitly wanted: a real PC-style class/level for NPCs whose actions can move the plot (not a generic SRD-block reskin), with level scaled to actual narrative weight, and a concrete `## Move List` tied to the threshold so escalation is something a DM can actually play, not just a number climbing. Built `.claude/rules/npc-tiers.md` (the standard, an Important/Minor test, a level-scaling table by role, the Move List format) and `npc_tier_check.py` (structural completeness checker, mirrors `faction_design_check.py`'s pattern — verifies a Tier line, a real Class & Level line, and a Move List where a matching `consequences.md` threshold exists). Applied in full to `kessra-vane.md` as the first worked example: Rogue 5 (Thief), built from real SRD class-feature lookups (Sneak Attack, Cunning Action, Fast Hands, Second-Story Work, Uncanny Dodge) rather than an off-the-shelf block, plus a new `consequences.md` "Kessra Vane — Closing In" threshold (2/3, backfilled) and a three-tier Move List. Verified via `npc_tier_check.py`. **Retrofit policy**: existing Important-tier NPCs with a `consequences.md` block already (Sefwyn Marrow, Sella Dray, Orell Tain, Marta Vantor, Kessic Ambrey, Sarelle Duskbourne) get this treatment at their next actual appearance or next weight change, not a blanket pass tonight — see `npc-tiers.md`'s Retrofit policy section. |
| 2026-08-29 | DM asked whether future random encounters (`travel-encounters.md`'s regional tables) would also benefit from today's combat-difficulty fixes — honest answer: only if the discipline is actually applied live each roll, since random encounters have no pre-built file the way dungeon fights do; the mandatory `dm-start-session` load fix keeps `two-player-scaling.md` in context, but nothing enforces its use at the moment a table result gets resolved into a real fight. Added a standing reminder directly into `travel-encounters.md`, at the point every roll is actually made, covering the group-specific techniques (spread, alert exception, occasional tougher individual) — explicitly scoped lighter than named-encounter treatment (no Legendary Resistance/deliberate HP-buffering for routine road encounters, that's reserved for bounties and dungeon bosses). Also clarified: regional tables already scale by region/level range: — revisiting an already-outgrown region's table staying easy is expected, not a gap. |
| 2026-08-29 | **Group-encounter reinforcement — the other half of the combat-difficulty fix.** DM asked directly whether routine group encounters got the same treatment as the solo-boss reinforcement pass; they hadn't. Added a distinct third technique set to `two-player-scaling.md` for groups specifically (HP-buffering is the wrong tool for groups — the fix is preventing one AoE from resolving the whole group: sub-group splitting, physical spacing within a sub-group, an alert-sentry exception to full surprise, and an occasional tougher leader/sergeant). Applied all four immediately to `the-gnoll-battlefield.md` and `the-feral-menagerie.md` (both built earlier today) rather than deferring — gnoll pack leader now runs ~2x normal HP, both sites' sentries/pack members now have a keen-sense-based chance to notice the party regardless of Stealth, and members are explicitly spread rather than clustered. |
| 2026-08-29 | **Five faction-free neutral sites built near Karsgate**, per direct DM request, co-designed iteratively (several concepts rejected/replaced before landing on the final five): The Gnoll Battlefield (war-scavenger pack), The Giant's Cairn (solo named ogre, deliberately built per today's own two-player-scaling combat lesson), The Feral Menagerie (a ruined noble's escaped exotics), The Driftway Fair (a recurring, faction-blind traveling market — the campaign's first genuinely neutral *recurring* hub, not a one-time site), and The Unremembered Shrine (a hidden, high-level level 9-10 pre-schism temple to Ossara, 8-room `dungeon-design.md`-standard site with real fights, puzzle-gated wards, and a unique non-Crown reward, "the First Ember" — deliberately not a sixth fragment, to keep the site genuinely faction-independent). All five added to `gazetteer.md`; `name_collision_check.py` re-run, no real new collisions (shrine/court hits are generic-word recurrence, same pattern already accepted for House/Hall/Row). |
| 2026-08-29 | **Full cross-system audit**, per direct DM request (script wiring, philosophy consistency, enforcement working, conflicts, CLAUDE.md staleness). Verified `session_state_gate.py`'s six-check `CHECKS` list and both enforcement functions are correct and non-duplicated; re-ran `name_collision_check.py` (no new collisions). Found and fixed the one real structural gap: `dm-start-session/SKILL.md`'s mandatory session-start reading list never got extended to include `two-player-scaling.md`, `dungeon-design.md`, or `faction-design.md` — all three are judgment-call standards no hook can enforce, so omitting them from guaranteed context risked exactly the mistakes they were built to prevent recurring in a future session. Added all three, plus a note on why judgment-call standards need this treatment differently from hook-checked state facts. Also updated `CLAUDE.md`'s file table and "Note on enforcement" paragraph, both stale relative to everything built today (missing `bug_signal_hook.py`/`standing_rule_hook.py` from the hook list, no mention of any of today's new rule files). |
| 2026-08-29 | **Combat difficulty audit — the "surprise+AoE trap" documented and fixed as a standing building rule.** DM flagged combat as too easy and unsatisfying; a full retroactive Explore-agent audit of every logged fight, session 2-6, confirmed it precisely — encounters rated Deadly on the standard adjusted-XP table (`two-player-scaling.md`), including both major Concordat convoy ambushes at 2,400+ adjusted XP, were resolved with zero party HP lost every time the party had surprise, because the formula never modeled surprise removing a full enemy round combined with this party's AoE/burst compressing multiple enemies' HP pools into that dead round. Added a new section to `two-player-scaling.md` with six concrete techniques (deny surprise more often via patrols/blindsight/scent-tracking, prefer solo strong creatures, spread formations, vary damage resistances, build past Deadly when surprise is likely, vary fight openings) grounded in what the same audit found actually produced tension (Roskel, the Warden, Hollowmoor's reversed surprise, Kargrim's Reach's alert rooms, the Web Gallery ettercap). **Correction, 2026-08-29**: this line originally claimed all five were "already the structurally nova-resistant shape" for being solo — the DM caught that this was incomplete (the Kargrim's Reach basilisk and Widow's Hollow ochre jelly were also solo and still died in one hit with zero damage taken). Solo avoids the group-multiplier problem but doesn't guarantee survival to act on its own; all five `regional-threats.md` entries were retroactively given explicit HP-buffer/Legendary Resistance/ambush-first notes to actually fix this. See `two-player-scaling.md`'s corrected guidance and `regional-threats.md` itself. |
| 2026-08-29 | **Proactive agenda tracking extended to all four flagged NPCs, and made a permanent standard.** Built `consequences.md` threshold blocks for Kessic Ambrey (unauthorized vault search, 1/3), Sella Dray (moving on Coalback, 1/3), Orell Tain (losing his internal argument to Sarelle, 1/3 — with an explicit note that the party's own anti-Concordat campaign may be *accelerating* his downfall, not helping him, a deliberate unresolved tension), and Marta Vantor (bankrupting a rival, 1/3). All backfilled with an initial weight of 1 reflecting each NPC's own established history, per the same discipline as Sarelle's backfill. Added a new standing section to `.claude/rules/npc-consistency.md`: any future NPC whose "What they actually want" describes an ongoing, self-progressing scheme (not a static want) gets the same `consequences.md` treatment — a judgment-call trigger, not a universal requirement, since `consequence_check.py` already enforces the format generically for whichever entities get a block. Dismissed the `standing_rule_hook.py` flag on this basis rather than building a redundant checker. |
| 2026-08-29 | DM asked whether NPCs pursue their own independent agenda over time, not just react to the party, using Sefwyn Marrow as the example. Found the reactive layer (self-interest, conditional loyalty, Decision-logic branches) is already solid project-wide — but the *proactive* layer (a personal scheme that progresses on its own, the way faction clocks do) only existed for one NPC, Kell's escape fund. Sefwyn's own file already described "their private tally continues to grow independent of what the party knows" with nothing tracking it. Extended `consequences.md`'s threshold format (previously faction-only) to an individual NPC for the first time: "Sefwyn Marrow — Private Tally" (1/3, pending) — the party's own choice to show Sefwyn more fragments/knowledge directly controls how fast this advances toward Sefwyn deciding they have enough and disappearing. Flagged (not yet built) other NPCs with the same shape of gap: Kessic Ambrey's unauthorized vault search, Sella Dray's Coalback tension, Orell Tain's losing internal argument, Marta Vantor's rival-bankrupting campaign. |
| 2026-08-29 | DM asked how the timeline actually works and whether today's new content needed anything added to it. Added a "three mechanisms" cross-reference table to the top of `campaign-clock.md` (faction clocks here, World Events every 7 days in `world-events.md`, threshold-based Consequences in `consequences.md`) since the timeline is now genuinely split across three files with no single place explaining how they relate. Found one real gap in the process: Sarelle's Pattern-of-Losses threshold crossing (day 42, applied same session) was described in the "Sarelle collects" section but never added to the Fired log table at the bottom — every other fired event has a row there. Added it. |
| 2026-08-29 | **Karsgate's casino and two dirty-contract networks built**, per direct DM request. Added **The Wagered Crown** (Karsgate's large, open-to-anyone casino, run by **Orvenna Kest** — deliberately neutral ground where every faction's money and agents mix) alongside the existing exclusive Ivory Court. Built **Corla Vint** (Karsgate's dirty-contract broker, working out of the Broken Wheel — theft, kidnapping, assassination, with a real if narrow moral floor) and elevated the Ninefinger crew into her operational arm for jobs needing actual hands. Built **The Broker** for Ironhold (`npcs/the-broker.md`) — Ironhold's equivalent, deliberately fused with the Broker's Chapel religious site already established: DM truth is a specific person (Ostren Vale, a Paymaster's Hall clerk) who inherited a decades-old, clerk-to-clerk tradition, but whether "the Broker" is genuinely something more than a human tradition is left as deliberately open as the Concordat's own unresolved question about Ossara. Ran `name_collision_check.py` after — no new real collisions ("Crown" appearing 3x is an intentional thematic echo, the Wagered Crown's name is a deliberate pun on the Ashen Crown). |
| 2026-08-28 | **Second sweep pass**, DM asked whether factions would actually "live" in the new content, whether NPC coverage was sufficient, and whether World Events could fire in the new locations. Found and fixed three more gaps: (1) `world-events.md`'s entries 7-8 still referenced `gazetteer.md`'s `[sketch]` sites, which no longer exist after today's build-out — updated to reference `regional-threats.md`'s escalating bounties and already-`[full]` sites developing further. (2) Yeva Ashwright's stated plan to visit Fenn "within the next several weeks" was left as prose in her own NPC file — exactly the kind of promise `campaign-clock.md`'s own standing rule (added after the Sefwyn incident) requires be a real dated clock; added `⏳ Yeva Ashwright's circuit visit to Fenn — fires ~day 52`. (3) Two named characters with real narrative substance — Ostwick's Dunnel Ashe and the Weeping Wood's dryad (now named Sylvenna) — existed only as paragraphs inside location files, violating `dm-notes.md`'s "a named NPC with real weight gets a proper file" rule; built both. NPC count: 25 → 27. |
| 2026-08-28 | **Full sweep of today's session content**, per direct DM request. Found and fixed three real gaps: (1) **naming collisions** — Karsgate had four separate things named around "Ledger" (the district, a watch house, a warehouse, a trading post) and a "Cinderpath Almshouse" colliding with Gallowmere's pre-existing "Cinderpath Shrine"; renamed the warehouse pair to "The Foundered Warehouse"/"Cellar", the trading post to "Thale's Landing", the almshouse to "The Widow's Hearth". Built `name_collision_check.py` as the permanent structural fix, per this session's own script-over-prose discipline — a 3-collision pattern in one batch is exactly what `bug-protocol.md` calls a pattern, not bad luck. (2) **Karsgate's 10,000+ tier requires multiple religious sites** — only had one (the Cathedral); added a second, "The Waterward Shrine" in Riverside, deliberately class-contrasted with the Kingsward's formal cathedral. (3) **12 of the 13 newly-built Chapter 1 sites had zero `two-player-scaling.md` reference** — the established discipline every other major dungoen in this campaign follows. Added explicit adjusted-XP scaling notes to all of them; found and fixed two genuinely overtuned encounters in the process (Weeping Wood's 3-ettercap fight at 3,375 adjusted XP, Hallowmere Chapel's 3-specter fight at 1,500 adjusted XP — both reduced to 2). See `bug-log.md` for the full categorized entries. |
| 2026-08-28 | **Five regional threats built** (`regional-threats.md`) — apex predators tied directly to each region's now-established topography, elevated from flavor into named, huntable bounties with stat blocks, adjusted-XP math (`two-player-scaling.md`), a specific bounty-giver, and a real reputation/reward payoff each: The Drowned Reeve (Marches, giant crocodile, Compact toll factor), The Hollow Reaper (Thornlands, wraith, Harrowgate temple), The Moor-Ghost (Cindermoor, ettin, Cair Dunnow itself — a second path into their trust), The White Reaver (Ashvale, chimera, Coren Ashvale directly), The Undercity Cloaker (Karsgate, already-built Room 17, Marta Vantor). All five deliberately Deadly-or-near-it for their region's top level, per `two-player-scaling.md` — designed to be posted early as `LATENT` in `jobs-and-opportunities.md` and sit until the party can see them coming and choose them, never sprung. |
| 2026-08-28 | **Regional Topography & Texture table added to `gazetteer.md`**, per direct DM request — no elevation/physical-geography framework existed anywhere in the campaign; individual site names (a "moor," a "chalk escarpment") implied terrain without tying regions into a coherent map. Established an explicit elevation profile and signature sensory texture for all 8 regions, with a real ascent/descent shape across the Chapter 1 journey (lowland Marches → high Cindermoor plateau → river-valley Karsgate → chalk-highland Ashvale). Audited `travel-encounters.md`'s existing regional tables against the new framework — found they already reflected terrain well (Marches' water-adjacent stirges, Thornlands' barrow-heavy undead, Cindermoor's weather/will-o'-wisps, Ashvale's chalk-country manticore/chimera) — no table edits needed, the gap was the missing explanatory framework, not the encounter content itself. |
| 2026-08-28 | **All remaining Chapter 1 `[sketch]` sites built to `[full]`**, per direct DM request after the Karsgate audit prompted the question of what else was still undesigned. 13 sites across Regions I, II, III, and V: `nettlecombe.md`, `the-gallow-tree.md`, `riverwatch.md`, `the-sunken-barge.md` (Marches); `ostwick.md`, `the-weeping-wood.md`, `the-tithe-barn.md`, `hallowmere-chapel.md` (Thornlands); `cair-dunnow.md`, `the-drowned-cathedral.md`, `kesh-deeps.md` (Cindermoor); `ironhold.md`, `the-chalk-warrens.md` (Ashvale). Depth scaled proportionally — hamlets got `dm-notes.md`'s 3-4 location minimum, genuine dungeons (Riverwatch, Weeping Wood, Drowned Cathedral, Kesh Deeps, Chalk Warrens) got full `dungeon-design.md` 4-phase treatment, Ironhold got the 10-15 location market-town treatment with a religious site (the Broker's Chapel). Notable connective work: The Chalk Warrens is now the confirmed physical answer to Ashvale Necropolis's own "way in that isn't the front gate," seaming directly into the Chapter 1 climax site; the Gallow Tree confirms the Cinder Choir's manufacture problem is a repeat failure, not a first offense; Kesh Deeps plausibly connects underground to the Drowned Cathedral, left as a DM's-choice thread. `gazetteer.md` updated to `[full]` for all thirteen, with file pointers. |
| 2026-08-28 | **Karsgate fully rebuilt** per direct DM request, after the earlier "flagged but deferred" thinness (`bug-log.md`, 2026-08-26) resurfaced during the faction-design work — Region Control and all three faction files now reference Karsgate heavily, making the deferral no longer safe to leave. Expanded from 7 named locations/0 religious sites to **42 surface locations across 4 districts + 17 Undercity locations** (well past the city-tier 15+ minimum), with a real religious site (the Ember Crown Cathedral) added. The Undercity was rebuilt to `dungeon-design.md`'s standard specifically (concept/ecology, multiple entrances, numbered locations with real content) rather than left as a bare monster list. All three factions now have a confirmed, concrete Karsgate presence, not just a chapterhouse mentioned in passing — new NPCs: **High Prior Varic Sennett** (orthodox faith, suspects an unconfirmed Choir cell in the city), **Brother Kessic Ambrey** (the Choir's second, independently-built cell leader — thinner and more anxious than Fenn, secretly searching the vaults for a fragment without authorization), **Marta Vantor** (Vantor Shipping, the city's river-trade lever and a direct tie to the party's own Gallowmere docks plan), and **"Iron" Sella Dray** (Red Tally gang leader, the Warrens' most organized and most negotiable faction). |
| 2026-08-28 | **World reactivity layer built** (`consequences.md`, `world-events.md`, `consequence_check.py`, `world_events_check.py`, Region Control in `gazetteer.md`), design locked first in `world-reactivity-design.md` then built in one pass per today's own standing-rule discipline (rule and script together, not sequentially). Sarelle Duskbourne's two historical convoy losses backfilled and applied immediately rather than starting the ledger from zero — real, in-effect consequences (harder future shipments, a dedicated investigator) written into `npcs/sarelle-duskbourne.md` and `campaign-clock.md` the same turn. Both new check scripts wired into `session_state_gate.py`. Caught and fixed a real regex bug while testing `consequence_check.py`: `re.DOTALL` combined with a greedy `.*` was letting one entity block's Threshold line match through to a *different* block's Status line whenever more than one entity existed in the file — silently merged the wrong name/status/body together. Rewrote the single-line fields as `[^\n]*` and confined multi-line matching to the body capture only; verified against 1-block, 3-block, and the real file before wiring in. See `bug-log.md`. |
| 2026-08-28 | **Standing-rule enforcement built — the escalation ladder itself is no longer just prose.** The DM raised a general design concern (not a play bug): when a systemic improvement is made, Claude's own judgment about whether to write a script/hook vs. a plain rule was itself unenforced — `bug-protocol.md`'s escalation ladder said "prefer a script," but nothing checked whether that preference was actually followed, and `bug-log.md`'s own frequency table shows written-only rules failing repeatedly before a script/hook replaced them (rest-tracking, dice-ownership, both rewritten 2-3 times). Built `standing_rule_hook.py` (`PostToolUse` on `Edit\|Write`, tracks within a turn whether a new `.claude/rules/*.md` rule or a new `dm-notes.md` "Standing rule —" section was written, and whether a script/`settings.json` was also touched) and `standing_rule_dismiss.py` (explicit last-resort override, mirrors `bug_signal_dismiss.py`); extended `session_state_gate.py`'s `Stop` hook to block on an unresolved case. Complements, doesn't replace, `bug_signal_hook.py` (which only escalates once a bug-log category hits 3+ repeats) — this one checks the ladder on the *first* rule written, not just the third repeat. Live-tested: block, script-resolves, dismiss-resolves, script-only-is-moot, and both the match/no-match cases for the dm-notes.md heading detector — all five passed before wiring into `settings.json`. |
| 2026-08-24 | Register created. Full audit of what exists vs. what's been tested. |
| 2026-08-24 | `cinematic-moments.md` and `missing-systems.md` added per table request. |
| 2026-08-24 | Environment audit — three findings, all fixed: (1) **most scripts crashed without `PYTHONUTF8`** on this Turkish-locale machine; every earlier test had silently passed only because the flag was set by hand. Now set permanently in `settings.json`. (2) `py` was not in the permission allowlist despite being the only working launcher here. (3) **CLAUDE.md instructed the opposite of this table's dice convention** — it said reported rolls are final totals, while this table reports raw d20s and Claude adds the modifier. A fresh session would have read every check ~5 points low, silently. Overridden at the top of CLAUDE.md. |
| 2026-08-24 | CLAUDE.md now points at the campaign's operating documents, so standing rules are reachable from the always-loaded file rather than by chance. |
| 2026-08-24 | **Session 2 resumption fixes.** (1) `campaign-clock.md` travel table had no Thornwick↔Sallow Ford entry and the surrounding text implied the party could beat the day-18 stablemaster clock "if they leave soon" — wrong, since Sallow Ford is *behind* Gallowmere on the road (6-7 days total, not 2-3). Added the missing route row and corrected the arithmetic: the party arrives at or after the clock fires, not ahead of it. (2) Added a provisional Corran buy-price rule to `info/locations/thornwick.md` (see gaps table above) ahead of the trading-post scene. |
| 2026-08-25 | **Pre-flight sweep before restarting — three more bugs.** (1) **No XP command existed** in `update_character.py`. XP is the second-most-updated field and there was no way to write it — which is why combat XP kept being awarded verbally and never recorded. Added `xp` / `xp_add`, which also flags when a level threshold is crossed. (2) **Five skill files invoked `python .claude/scripts/…`**, which does not resolve on this machine (Windows Store alias shadows it). All skills and agents now use `py`. (3) **No way to change HP Maximum** — the level-up skill literally instructed the DM to hand-edit the file, and both PCs are one scene from level 2. Added `hp_max`, which carries Current HP up by the same gain. Session-2 combat XP written (275/250) and state synced. |
| 2026-08-25 | **Root cause of rules not taking hold — fixed.** `/dm-start-session` read only `state.md`; the standing rules in `info/dm-notes.md` and `.claude/rules/` were never actively loaded by anything. They reached play only if `CLAUDE.md`'s system-prompt copy happened to carry them — which is why mid-session overrides kept failing. The skill now **reads all seven operating documents in the main context** as step 1, explicitly not via an agent (an agent's context is discarded, so rules read inside one never reach play). Also added: drift check, clock advance, and a confirmation line naming the three biggest overrides. |
| 2026-08-25 | **New-capability intake check built** (`.claude/rules/new-capabilities.md`). Raised in play: what happens when a future spell has a trigger effect? Enumerating cases is impossible, so this is a procedure — six questions run once per new capability (noise/light, witnesses, physical evidence, **is it identifying**, faction triggers, ascension interaction). Surfaced a live consequence: the Concordat has held descriptions of "an elf and a dragonborn" since day ~5, which makes Kriv's breath weapon a signed confession in front of survivors and turns Disguise Self from a utility spell into infrastructure. |
| 2026-08-25 | **Noise/light detection system built** (`.claude/rules/noise-and-detection.md`). Raised in play: does a Thunderwave alert nearby enemies? 5e has no general noise rule — only a few spells state an audible range, everything else is unwritten DM judgement, which means a different answer every session. Now standardised: use the spell's stated range when it has one, otherwise a five-tier distance table with terrain modifiers; being in range is a Perception check rather than automatic; and hearing produces a **timer** (rounds until arrival) rather than instant reinforcements. Light handled separately since it needs line of sight. Marked ⚠️ provisional until used at the table. |
| 2026-08-25 | **Dice ownership violated in both directions.** Claude rolled Thunderwave's 2d8 damage — a player's roll — and stated the deserters' Constitution saves after the `roll.py` call had already failed, meaning they were never rolled. The existing rule was one weak line ("player-side rolls stay with the players") that didn't make spell damage obviously a player roll. Replaced with an explicit ownership table plus a hard rule: **never state a die result that did not come from a player reporting it or from visible `roll.py` output**, and never substitute a plausible number when a script call fails. |
| 2026-08-25 | **First combat failed — four faults, all fixed.** (1) `combat_status.py --init` caught `EOFError` inside a retry loop, so with no stdin it **looped forever** — this is what hung the session, twice. It now accepts `--initiative "Name=18,Name=11"` and fails fast with a clear message if stdin is unavailable. (2) `roll.py --help` errored instead of printing usage. (3) Surprise was played as a free pre-initiative round for the whole party — RAW is initiative first, surprised creatures then lose their turn. (4) Thunderwave (`Self (15-foot cube)`) was narrated as a ranged opener from the treeline; it originates from the caster, so hitting three deserters puts an 8 HP wizard in melee with all three. Both rules errors written into `.claude/rules/combat-rules.md`. |
| 2026-08-25 | **Two more wasteful agents found — five total, matching the table's recollection.** `character-tracker` (spawned by `dm-show-character` purely to run one script, and by `dm-rest` for an interactive process) and `encounter-verifier` (spawned alongside `combat-tracker` by `dm-load-encounter`, two agents to start one fight), plus `conversation-log-appender` for a file edit. Root cause of the earlier partial fix: **skills carry their own mandates independently of CLAUDE.md**, so overriding CLAUDE.md alone left them live. Also, the original upstream instructions were left standing above each override and competed with it — now explicitly struck through as SUPERSEDED. Full 12-agent ruling table added to CLAUDE.md. |
| 2026-08-25 | **NPC dialogue moved inline.** The framework's default kept `conversation-tracker` alive and relayed every line to it — 60-100s per exchange, tested unplayable across two scenes. Overridden in CLAUDE.md: all NPCs voiced in main context, one `/dm-conversation-log` call at the end. The framework's own skill file warns the live agent "cannot access the raw conversation and will hallucinate content," so inline is both faster *and* more accurate. |
| 2026-08-25 | **Skill argument bug fixed in two places.** `dm-conversation-log` and `dm-combat-log` both take `[campaign] [slug]` but built paths from raw `$ARGUMENTS`, producing `campaigns/ashen-crown corran/party/state.md`. Found in play on the first; the second had the identical fault and would have broken the first time a fight was logged. Both now split the arguments explicitly. Rescanned all 25 skills and 12 agents — no other instances. |
| 2026-08-25 | **Rest-tracking and RP-XP-cap turned into script gates, not memory.** The DM rejected the earlier fix (a written standing rule) outright — a prose reminder had already failed three times in the same session, and manual per-session XP counting was flagged as the same failure mode waiting to happen. Built `rest_check.py` (long-rest drift gate) and `award_rp_xp.py` (roleplay XP cap gate), added `hp_full` to `update_character.py` as a clean full-heal patch type, and rewired `dm-notes.md` / `roleplay-and-xp.md` to point at the scripts instead of describing the behavior in prose. Both scripts tested live before being declared done: `rest_check.py` via a real drift simulation (damaged Kriv, advanced the day, confirmed DRIFT, reverted), `award_rp_xp.py` via a real award-then-revert plus a refusal-case check at the existing 3/3 cap. |
| 2026-08-25 | **Two more errors caught by the session-end verification pass.** (1) Kriv's Battle Master maneuver save DC was written as 15 (8 + prof 2 + STR **saving throw** bonus 5) — the correct formula uses the raw STR **modifier** (+3), giving DC 13. (2) Both PCs' level-3 XP denominator was set to 1,800 — that's `roleplay-and-xp.md`'s "XP to next" gap column for level 3, not the cumulative threshold. `progression.md`'s XP table gives the real level-4 threshold as 2,700. Both fixed on the character sheets. Pattern to watch for: any second table with per-level values invites this kind of column mix-up — cross-check against `progression.md`'s cumulative table specifically when setting a denominator. |
| 2026-08-25 | **Party-knowledge gap found and fixed.** Asked whether the party knew about the Sorrel's Hollow main camp; answered from `info/locations/sorrels-hollow.md` (DM prep) instead of the actual conversation log, and gave a wrong "no" — Hesta had already told them in session 2, npc-02 Exchange 5. The wrong answer shaped a real player decision (not hiding the bodies). Root cause: `state.md`'s `Open Threads` freely mixes DM-only setup with player-known facts, and per-conversation "Information revealed" ledgers exist but were never aggregated anywhere I'd check first. Fixed by adding a "Known to the Party" section to `state.md` as the explicit authority on in-fiction party knowledge, backfilled from both session-2 NPC logs, updated immediately going forward. Marked provisional — first real test is whether it stays accurate under live play. |
| 2026-08-26 | **`bug-log.md` itself made a hook target, not a reminder.** The DM pushed back correctly: a rule saying "log every bug in bug-log.md" is exactly the same failure shape already proven unreliable elsewhere in this session. Built `bug_log_reminder.py`, a `PreToolUse` hook on `Edit\|Write` that fires the instant a new dated row is added to this file's own Log table, and reminds — in that same moment — to add a matching categorized row to `bug-log.md` if it's a bug/gap rather than a feature build. Tested (hit, near-miss with no date, unrelated file) before wiring in. |
| 2026-08-26 | **Two more findings, both fixed.** (1) No settlement had a mandatory religious site despite three religious/death-cult factions driving the entire plot — Gallowmere specifically had none. Added "a religious site" as a required category in `dm-notes.md`'s settlement-scaling table (scaled to population, matched to which faction if any has influence there), and gave Gallowmere **The Cinderpath Shrine** — deliberately unclaimed by any of the three factions, a real power-vacuum opportunity rather than just flavor. (2) There was no categorized record of bugs found, just narrative log entries mixed with feature-built entries in this file — no way to see "how often does this category of thing go wrong" at a glance. Built `info/bug-log.md`: a dated, categorized table with a frequency rollup at the top, backfilled from this file's history, added to `dm-start-session`'s load list (recalled at every session start) and referenced from `bug-protocol.md` as a required step alongside the four. |
| 2026-08-26 | **Full sweep of every provisional/⚠️/❌ marker in the register**, per `bug-protocol.md`'s "check for the same problem elsewhere." Results: XP threshold denominator escalated to a structural fix (see above). Party-knowledge tracking escalated to a `PostToolUse` hook (see above). Noise/light detection, the new-capability intake check, and companion task-resolution all upgraded from provisional to tested — each was genuinely exercised live in session 3 and held up. Shop price bands (Thornwick, Harrowgate) reframed as "proven for these two, still needed elsewhere" rather than generically provisional. Downtime activities, holding management, gods/religion, hoard-sense detail, and faction-reputation tracking were all confirmed still-appropriately-deferred — none have been needed in actual play yet, so building them now would be scope creep ahead of the table, not a fix. |
| 2026-08-26 | **Just-in-time cinematic trigger — a `PreToolUse` hook, not just an end-of-turn catch.** The DM clarified the actual ask: not automating the narration's *quality*, but automating the *trigger moment* so it fires when it should rather than being caught after the fact. Built `.claude/scripts/cinematic_trigger_hook.py`, wired as a `PreToolUse` hook on `Edit\|Write` in `.claude/settings.json`: the instant an edit to `state.md`'s Current Location field names a settlement/seat not yet in Visited Majors, it injects a reminder into context *before the edit completes* — ideally before the arrival narration for that turn has been written, not after the whole turn ends like the `Stop`-hook checks. Verified live in-session (not just pipe-tested): a real test edit to Current Location triggered the exact expected `additionalContext` injection, then the test value was reverted. This is more useful than `location_check.py` for this specific purpose but doesn't replace it — the `Stop` hook still catches the case where Current Location was never updated to reflect the arrival at all. |
| 2026-08-26 | **Bug-protocol sweep (step 3) across the settlement-scaling and NPC fixes.** (1) Built two more mechanical checks, matching the `rest_check.py`/`sync_state.py` pattern, and wired both into the same `Stop` hook: `location_check.py` (flags a settlement/seat named in `state.md`'s Current Location that isn't yet in the Visited Majors list — catches the cinematic-trigger bookkeeping half mechanically; the actual narration judgment call still isn't automatable) and `clock_check.py` (flags `campaign-clock.md`'s header date disagreeing with `state.md`'s day count, and any clock still marked pending whose stated `fires ~day N` threshold has already passed). Both tested clean and drift cases before wiring in. (2) Swept existing settlement files against the new population table: Gallowmere (8 locations, town tier needs 6-8) and Thornwick (5 locations, village tier needs 3-4) both already meet it — no fix needed. Karsgate already exists, is district-organized (city tier requirement) with 5 districts, though could use 1-2 more named locations per district before the party actually arrives — flagged for the session-start check (`sandbox-index.md` 0a) to catch then, not fixed now since not urgent. (3) Found and fixed a real inconsistency the sweep was for: `npcs/orell-tain.md` (created earlier this session) left his location vague, but `locations/karsgate.md` had already placed him at the Grey Hall in Karsgate before that file existed. Corrected. |
| 2026-08-26 | **Real enforcement built: a Claude Code hook, not another written reminder.** The DM rejected "rewrite the rule with more emphasis" as a fix — correctly, since it had already failed twice. Built `.claude/scripts/session_state_gate.py` (runs `rest_check.py --check` and `sync_state.py --check`, read-only) and wired it as a `Stop` hook in `.claude/settings.json` — it now runs automatically after every assistant turn and returns `decision:"block"` with the drift report if either check fails, independent of the assistant remembering to run it. Tested both clean and induced-drift cases before wiring in. Then generalized further per DM instruction: wrote `.claude/rules/bug-protocol.md`, a standing four-step procedure (close the gap → fix the root cause permanently, escalating to a hook when a written rule has already failed → sweep other files for the same issue → no fix ships as a temporary patch) for every future bug/gap, in D&D content or Claude Code config alike. Added to `dm-start-session`'s load list and `CLAUDE.md`'s file table so it's actually in context every session. |
| 2026-08-26 | **Follow-up pass on the same feedback — generalized instead of patched.** The DM pushed back that the first pass fixed instances (Harrowgate) rather than the underlying pattern. Added: (1) a population→minimum-location-count table in `dm-notes.md`, checked proactively at session start (`sandbox-index.md` step 0a) for any settlement the party hasn't fully visited — explicitly flagged for Karsgate before the party gets there, not after. (2) Rewrote the XP/rest/sync/cinematic checklist rule from "do it right after the trigger" to "the tool call is a precondition for the narration" — the earlier phrasing had already failed twice more in the same session. (3) Character-file inventory audit found real drift beyond gold: Ilvaneth's sheet was missing the second Crown fragment and the Harpy Crags healing potion, and both PCs' sheets still listed horses/loot that had already been sold — fixed on both sheets, and `state.md`'s horse note trimmed to a pointer instead of a duplicate description, per the existing "character files are the single source of truth" principle. |
| 2026-08-26 | **Table-wide feedback pass — 7 issues raised, addressed.** (1) Cinematic openings weren't self-triggering — added a "Visited majors" list to `state.md` and a hard trigger in `cinematic-moments.md`: check the list before any arrival narration, no name on the list = first arrival = cinematic required. (2) Audited all faction clocks against the actual current day (had drifted to day 19 while play was at day 25) — fired Sefwyn Marrow's and Alis Wend's clocks, both overdue. (3-4) `state.md`/character-file drift found and fixed twice more in this same session despite the same-day standing rule addition — confirms this needs to be checked reflexively, not just written down once. (5) Harrowgate was under-built for a stated population of 4,000 (a mapmaker, a stable, an inn, an alchemist, a smith) — added the Scriptorium, the Ashgate Conservatory, the Sable Casket, Ironmonger's Row, and the Merchants' Assembly Hall. (6) Rest application is still getting missed despite `rest_check.py` existing — no code fix possible (there's no invariant to diff against XP/rest besides remembering to check), so the trigger was rewritten as an immediate, non-deferred action in `dm-notes.md`. (7) NPC physical-description rule extended to explicitly require sex/gender, and a new rule added: any named NPC with real weight gets a file in `npcs/` the same session, not left as a paragraph in another file (caught with Magus Orell Tain). |
| 2026-08-26 | **Combat XP and rest-application turned into a hard checklist, not memory.** Combat XP was forgotten twice in session 3 (a 12-person Concordat convoy, a hell hound) and long-rest application after multi-night travel was missed and only caught by `rest_check.py`'s drift detection — same failure shape the rest/RP-XP script gates already exist to prevent. No script can diff-check "was XP awarded" the way it can check the day counter, so the fix is a written, immediate-trigger rule in `dm-notes.md`: award XP and run `sync_state.py --sync` the instant a fight ends, run `rest_check.py --check` the instant a camp/rest is narrated — never deferred to a natural pause. |
| 2026-08-25 | **Armor Stealth disadvantage written into `combat-rules.md`.** Was RAW (`04 equipment.md`: Padded, Ring Mail, Chain Mail, Splint, Plate all impose disadvantage) but never recorded in project rules — caught when Kriv (Chain Mail) rolled Stealth hiding from a Concordat honor-guard patrol on the Thornlands road. Table now lists every armor's Stealth property so it's checked automatically, not remembered case by case. |
| 2026-08-26 | **`dm-level-up` skill hardened generally, not just for the cantrip miss.** After the cantrip gap (Wizards gain a cantrip at 4th, missed on Ilvaneth), the DM asked for this to be correct for every future level-up, not patched once. Swept the same skill for the identical shape of gap and found a live one: the ASI level list (4/8/12/16/19) doesn't hold for **Fighter** (extra ASI at 6, 14) or **Rogue** (extra at 10) — Kriv is a Fighter, so this would have silently misfired at his very next ASI. Both now class-specific tables in `.claude/skills/dm-level-up/SKILL.md` Step 5, plus a general Step 4 note about class-specific automatic-feature thresholds (Fighter's Extra Attack scaling, Indomitable). See `bug-log.md`. |
| 2026-08-26 | **`update_character.py` gained Inspiration tracking.** No patch type existed for it — an earlier narrative Inspiration grant (Ilvaneth, the Warden conversation) never made it onto the sheet, only caught when the player remembered to spend it. Added `inspiration_grant`/`inspiration_spend`. See `bug-log.md`. |
| 2026-08-26 | **`Toll the Dead` not in the free SRD** — same gap already flagged for non-default subclasses/maneuvers (Xanathar's Guide content, not core SRD). Ilvaneth picked it as her level 4 cantrip; applied directly from standard published rules (1d8/1d12 necrotic, WIS save, scales at 5th/11th/17th) since it's well-established, unambiguous content. Logged here rather than re-flagged as a new gap — same category as the existing `system-register.md` note on non-default subclasses. |
| 2026-08-26 | **Standing rule added: level-up applies at the next long rest, not instantly on crossing the XP threshold.** Both PCs hit level 4 mid-dungeon (killing the Hollowmoor Warden); the DM asked that the actual level-up conversation (HP roll, ASI/feat, spells) be deferred to the next long rest rather than run immediately. Documented in `progression.md`. `xp_add`'s `** LEVEL UP **` flag still fires immediately as a notification — only the sheet changes are deferred. |
| 2026-08-26 | **Ready Action doesn't skip initiative — rule written after a real miss.** The Weeping Tower's lower-level fight had Kriv's readied "attack whatever comes through the door" resolved (attack, damage, enemy save) before initiative existed at all — only set up afterward as a formality. Caught by the DM asking why this wasn't logged separately from the flat-initiative-totals fix in the same fight. Added an explicit rule to `combat-rules.md`: roll initiative for everyone the instant a Ready Action's trigger goes live, then resolve the readied action as a reaction within that order — never before it exists. See `bug-log.md`. |
| 2026-08-26 | **All pre-rule NPCs assigned a race in a batch pass.** Checked each of Mother Vey, Fennick Orle, Doreth Kell, Sarelle Duskbourne, Coren Ashvale, Orell Tain, Hesta Bram, Corran, and Alis Wend for any race already implied in play — none had one, so the pass was safe (not a retcon). Assigned with deliberate variety (Halfling, Half-Orc, Human, Elf, Dwarf, Tiefling, Dragonborn, Gnome, Half-Elf). Sefwyn Marrow's already-live-established race (Human, from the session 3 conversation log) was formalized into `sefwyn-marrow.md`. Sethra/Vharkoss Shestendeliath already had dragonborn established via family bloodline — no change needed. Closes the deferred half of the NPC-race content-gap. See `bug-log.md`. |
| 2026-08-26 | **Bug-signal dismissal now anchored to one explicit test, not a taxonomy.** The DM asked whether predefining "what counts as a bug" would help. Decided against a fixed list (same list-enumeration trap `new-capabilities.md` already rejects) — instead wrote `missing-systems.md`'s existing test ("would this need to behave the same way next time?") into `bug-protocol.md` as the explicit standard for the log-vs-dismiss judgment call, and referenced it in `bug_signal_dismiss.py`'s docstring so it's visible at the point of decision. |
| 2026-08-26 | **Bug-signal detection upgraded from reminder to hard enforcement.** The DM asked directly why a reminder Claude can still ignore is meaningfully different from the prose rules that already failed twice. `bug_signal_hook.py` now sets a pending flag on a correction signal; `session_state_gate.py` (the existing `Stop` hook) blocks the turn from ending unless `bug-log.md` was actually edited since, or the signal was explicitly dismissed via `bug_signal_dismiss.py "<reason>"`. Same hard-block pattern already proven for `sync_state.py`/`rest_check.py` drift. Live-tested: block → dismiss → clean, and block → real edit → auto-clear. |
| 2026-08-26 | **`bug_signal_hook.py` built — closes the bug-log blind spot.** `bug_log_reminder.py` can only fire when Claude is already editing `system-register.md`; it can't catch a bug that never gets written to either log file at all, which happened twice this session. New `UserPromptSubmit` hook fires on the DM's own correction language (a different, earlier signal) instead of on Claude's file-edit behavior. Wired into `.claude/settings.json`, tested against real correction/non-correction messages. See `bug-log.md`. |
| 2026-08-26 | **`dm-start-session` now explicitly asks whether the sitting is a new session or a continuation.** Root cause of the session-3-folder bug: nothing ever asked this question, a `session-manager` agent silently guessed wrong once, and nothing downstream could catch it. New Step 1.5 asks the DM directly every time and creates the new session folder immediately on "new," instead of leaving folder creation to whichever skill happens to write the first log entry. See `bug-log.md`. |
| 2026-08-26 | **Session 3's misfiled content moved to its own folder.** No `party/session-3/` folder had ever been created — everything that actually happened in session 3 lived inside `party/session-2/`, mislabeled. Found the true split point (line 493 of `session-2-conversation.md`, matching `state.md`'s own Session 2/Session 3 summaries) and split the file there; moved and renumbered the Sefwyn NPC log to `session-3-npc-01-sefwyn-marrow.md`; updated every cross-reference (`campaign-clock.md` ×2, `session-log.md`). See `bug-log.md`. |
| 2026-08-26 | **`combat_status.py --initiative` now requires RAW+MOD, not a bare total.** Third dice-ownership violation this session: PC initiative was typed in as flat totals ("Ilvaneth=15,Kriv=15") without asking either player for their roll. The `roll.py --owner` fix from earlier didn't cover this — different script, different flag. `--initiative` now rejects anything but `RAW+MOD` (e.g. `15+4`), making the player's actual reported roll visible in the command. See `bug-log.md`. |
| 2026-08-26 | **"Never batch resolved beats" generalized beyond combat.** A 3-night journey was compressed into one summary line and narrated straight to arrival — the same shape as the earlier batched-combat-turn bug, just outside combat, because that fix was scoped only to `combat-rules.md`. Generalized into `narration.md` to cover any multi-beat sequence. Also logged: this bug was initially missed in both `system-register.md` and `bug-log.md`, and the `bug_log_reminder.py` hook couldn't catch it because it only fires on edits to this file's own Log table — an honest, acknowledged enforcement gap, not something fixable with another written reminder. See `bug-log.md`. |
| 2026-08-26 | **Weeping Tower guardian encounter file built** (`info/encounters/weeping-tower-guardian.md`) ahead of the party's arrival — reflavors an SRD Ghost (CR 4) as Ovelt Harn's bound spirit-guardian, social-first per the location file's own design. Deadly-and-then-past for this level-4 party (1,650 adjusted vs. an 1,000 threshold) — flagged explicitly not to run as a forced fight. |
| 2026-08-26 | **Hollowmoor deep-barrow boss profile built** (`info/encounters/hollowmoor-deep-barrow.md`) before the party opened the door — per `cinematic-moments.md`'s requirement to prepare a boss's character layer (entrance, voice, want, bloodied/kill/death lines) ahead of the fight rather than improvising it mid-combat. The Warden (a Wight, reflavored as one of the last ash-king's own guard who put him down) is written with a genuine social path first, matching this table's Neutral Evil, talk-first tendencies, and the Deadly-vs-level-3 math is called out explicitly so the fight isn't run as a forced Deadly encounter. |
| 2026-08-26 | **`update_character.py`: bare `gold`/`xp` removed.** Claude used `gold 10` meaning "add 10" while splitting loot; it actually silently SET gold to 10, wiping Ilvaneth's 170 gp and Kriv's 111 gp. Same ambiguous-verb trap existed for `xp`. Both bare forms now exit with an error instead of running — `gold_set`/`xp_set` for an explicit absolute set, `gold_add`/`xp_add` for the common case. `README.md` updated to match. See `bug-log.md`. |
| 2026-08-26 | **`roll.py` now requires `--owner`.** Claude rolled Ilvaneth's Burning Hands damage itself in session 4 — a repeat of session 2's Thunderwave incident, meaning the prose dice-ownership table had already failed once and failed again. Per `bug-protocol.md`'s escalation ladder, rewriting the rule a third time wasn't acceptable — `roll.py` now hard-refuses to run any plain dice roll without an explicit `--owner enemy\|dm-table\|npc` flag, and the refusal message itself restates that PC rolls never belong here. See `bug-log.md`. |
| 2026-08-26 | **Hollowmoor Barrows encounter file built** (`info/encounters/hollowmoor-outer-mounds.md`) ahead of the party's session-4 arrival — no encounter file existed despite it being the obvious next destination. Outer mounds scaled to a fair Medium fight (2 Skeletons, 200 adjusted XP); the deep barrow's Wight is flagged explicitly as Deadly-on-its-own for this level-3 two-player party (1,050 adjusted XP vs. an 800 threshold) and must be telegraphed and chosen, not sprung, per `two-player-scaling.md`. |
| 2026-08-27 | **`.claude/rules/dungeon-design.md` written — a full four-phase dungeon design standard — and the first dungeon built to it, `locations/kargrims-reach.md`.** The DM judged the mid-session Widow's Hollow density patch insufficient and wrote out a complete framework directly (concept/ecology logic, non-linear "Jaquaysing" map with loops/verticality/multiple entrances, four balanced content types per room including faction conflict, DM-side pacing/atmosphere), asking for it as a permanent standard. Wrote the rule file, referenced it in `sandbox-index.md`. Built Kargrim's Reach (20 rooms, 3 vertical levels, two warring factions — a kobold clan and a bugbear warband — plus a basilisk both avoid in the flooded depths) fully to the new standard, with three companion encounter files (`kargrims-reach-groth.md`, `-skree.md`, `-basilisk.md`). Found via a map inside a fireproof box recovered from the Widow's Hollow's burning Web Gallery. See `bug-log.md`. |
| 2026-08-27 | **The Widow's Hollow densified after direct table feedback mid-delve.** DM called the freshly-built dungeon out as thin — 3 of 16 rooms had real content, one secret room had no discoverable clue, felt like moving room to room rather than being inside a place. Same shape as the earlier Harrowgate settlement-scaling gap. Rewrote the location file in place (ambient detail in every room, a findable clue for room 6's secret alongside the fallback roll, an explicit note preserving the content the party's own underwater shortcut caused them to skip) and added a new standing rule to `dm-notes.md` generalizing this into a density checklist for every future dungeon. See `bug-log.md` (`content-gap`, 10th instance). |
| 2026-08-27 | **First faction-free, arc-free dungeon built: The Widow's Hollow.** DM raised, as an OOC design discussion, that every site in the sandbox — even the nominally-pure-treasure Drowned Mill — turns out to be wired into a faction, NPC, or personal arc, leaving no genuine "just go explore" option. Confirmed true by checking every prepared location file. Built `locations/the-widows-hollow.md` (16-room cave system, 3 secret rooms, a green hag) and `encounters/the-widows-hollow-hag.md` (social-first boss, per DM request that the witch not be a straight fight) collaboratively with the DM's stated preferences (cave system, 15+ rooms, level 4 scaling as the party levels up mid-delve, non-combat witch). New standing rule in `dm-notes.md` and a session-start check in `sandbox-index.md` (step 4b) make this an ongoing category — the next standalone site gets built once this one resolves, not left as a one-off. See `bug-log.md` (`content-gap`). |
| 2026-08-27 | **All five hooks anchored to `$CLAUDE_PROJECT_DIR` instead of a cwd-relative path.** Session 5 start: creating `party/session-5/` needed a `cd` into `campaigns/ashen-crown/party`, which persisted across tool calls and broke every hook in `.claude/settings.json` at once — each command referenced its script as `.claude/scripts/X.py`, relative to the ambient cwd rather than the project root. Fixed structurally: every hook command now resolves via `py "$CLAUDE_PROJECT_DIR/.claude/scripts/X.py"`. Verified live with cwd deliberately left drifted. See `bug-log.md` (`hook-fragility`, new category). |
| 2026-08-24 | **Encoding fixed at the root.** Setting `PYTHONUTF8` in `settings.json` only covered Claude's own calls — the DM running `py …` in their own PowerShell would still have crashed. All 11 scripts now force UTF-8 on stdout/stderr internally and pass `encoding="utf-8"` on every file open, so they work in any shell with no environment setup. Verified in real PowerShell with `py`, and with every env var unset. |
| 2026-08-27 | **Sefwyn Marrow's "will re-initiate contact" promise fired after a 12-day silent gap** (day 25→37) because it was deliberately written as "an open thread, not a pending clock" in `campaign-clock.md`, exempting it from `clock_check.py`'s drift detection entirely. Fired it in play (Sefwyn intercepts the party at The Hanged Man, Harrowgate) and added a standing rule: any future "NPC will find the party" promise must be written as a real pending clock, never bare prose. See `bug-log.md`. |
| 2026-08-27 | **NPC consistency system built: `.claude/rules/npc-consistency.md` + `npc_consistency_reminder.py`.** Orell Tain was played offering free help with no basis in his character file — pure altruism serving the scene's convenience, not his own stated self-interest. The DM identified the actual root cause directly: NPC files are static "what they want" bios with no mechanism for tracking how an NPC's knowledge/stance evolves as they learn things in play, and nothing forces a check of a new dialogue offer against that NPC's own motive before it's narrated. Built the standing rule (every NPC file gets two new living sections: "Current knowledge & stance" and "Decision logic — how new information could redirect them") and a `PostToolUse` hook on `Write` that fires when an NPC conversation log is saved, reminding to update the corresponding `npcs/*.md` file. Retrofitted five active NPCs immediately at the DM's direction (Sarelle Duskbourne, Sefwyn Marrow, Coren Ashvale, Corvin Thale, Odric Fenn) plus Orell Tain (the triggering case); all other NPC files get the same treatment the next time they actually appear in a scene, per the same precedent already set for physical descriptions and race variety. Live-tested the hook (fires on a real NPC-log path, correctly ignores the NPC's own bio file and unrelated files) before wiring in. See `bug-log.md` (`npc-inconsistency`, 5th instance). |
| 2026-08-27 | **`clock_check.py` extended to detect scheduling clusters, not just overdue clocks.** A new NPC-meeting date (~day 42) was set in play without checking it against two other already-pending dated commitments (Corran's game ~day 40, Roskel's summons ~day 41-43), piling three events into the same week by accident — caught only because the DM asked directly. A written "cross-check before setting a date" rule was judged insufficient alone (prose reminders have failed repeatedly in this project), so the script itself now flags any two pending clocks within 3 days of each other, with a `(cluster ok)` heading annotation to permanently exempt a deliberately-accepted overlap. Live-tested against a synthetic campaign folder (cluster caught, exemption respected, distant clock ignored) before wiring in. Also fixed the same root problem that let the new date go unchecked: Corran's game was itself prose-only in `state.md`, never a real pending clock — converted it to one alongside the new Tain-meeting clock. See `bug-log.md`. |
