# Bug Log Archive — Ashen Crown

Older entries moved out of `bug-log.md` (by `bug_log_archive.py`) to keep that file lean for routine session-start reading — `dm-start-session` reads it in full every session. The Category frequency table in `bug-log.md` always reflects the FULL history, including everything archived here — archiving never changes a count, only where the detailed entry text lives.

| Date | Category | What happened | Resolution |
|------|----------|----------------|------------|
| 2026-08-30 | `combat-geometry` | The wraith boss fight (95 HP, checked against `solo_burst_check.py`'s floor beforehand, designed to survive an Action Surge round) still ended in round 1 for 114 total damage with zero HP lost by either PC — Kriv's real round (4 attacks via Action Surge, 3 Battle Master maneuvers spent, vs-undead bonus on every hit) totaled 83, and Ilvaneth emptying a Wand of Magic Missiles added 31. The floor check never modeled maneuver dice, Action Surge's attack-count doubling, or a caster's burst spell/item — all three were live in this exact fight. DM's own hand math derived the real ceiling (~86 for Kriv alone, ~114+ combined) and proposed the actual fix: stop scaling HP upward, reserve Legendary Resistance/Actions for genuine solo *named* bosses, and add more enemies (splitting party burst across targets) for every other "serious fight" | Extended `solo_burst_check.py` with a `--full-nova` flag: parses Action Surge (doubles attack count) and Battle Master Superiority Dice (via the sheet's own "Superiority Dice (N/M uses) ... dXs" phrasing) directly off the character files, prints each martial PC's real coordinated-nova ceiling, and explicitly reminds the DM to add a caster's actual burst spell/item by hand (spell-slot choice isn't reliably guessable). Live-tested against the real character files: Kriv's ceiling vs undead computes to 86.0, matching the DM's own hand-derived number almost exactly; combined with a rough Fireball estimate (28) lands at ~114, matching the fight that just happened. Added the design principle to `two-player-scaling.md`: Legendary Resistance/Actions only for genuine solo named individuals; multiple enemies (not higher HP) for every other serious encounter |
| 2026-08-30 | `format-bug` | `combat_status.py --init` on the Shestendeliath wraith encounter produced HP 9567/9567 instead of 95 — the encounter file's HP Each cell read "95 (SRD base 67, raised — see DM Notes)" (a deliberate note explaining the solo-boss HP correction), and the parser's `re.sub(r"[^\d]", "", cell)` stripped every non-digit character and concatenated the two numbers ("95" + "67") into one string before parsing it as an int. Caught immediately from the display before any turn resolved | Fixed the encounter file to state the HP plainly ("95") with the explanation left only in prose below, not embedded in the data cell. Hardened the parser itself: both the HP Each and AC cell parsers now use `re.search(r"\d+", cell)` to extract the first standalone number, instead of stripping-and-concatenating all digits in the cell — robust to any future cell that legitimately contains more than one number. Live wraith corrected via `combat_status.py --update` (95/95) before play continued |
| 2026-08-30 | `combat-geometry` | Design validation, not a bug: after the 2-wraith (80 HP each) fix was already retconned into canon, the DM asked to test a second variant — 3 wraiths at completely unmodified base SRD stats (67 HP, no raise at all) — in a second disposable test campaign, live-played the same way. Result: a longer, more satisfying fight (4 full rounds vs. 2) with genuine round-to-round danger (the party got lucky — several near-misses and a fully-depleted Mirror Image — rather than being safely padded), while requiring zero HP-inflation math | Confirmed as the standing default going forward, ahead of the lightly-raised-HP approach: `two-player-scaling.md` updated — when using "more enemies, not more HP" for a serious (non-solo-named-boss) fight, default to fully unmodified SRD stats and tune difficulty via enemy *count*, not HP inflation. HP inflation stays reserved for the genuine solo-named-boss case (paired with Legendary Resistance/Actions) or a specific narrative reason for one tougher individual within a group (a sergeant/leader, per the pre-existing group technique). Not retconned into the already-resolved Shestendeliath Hold encounter a second time — this governs fights from here forward |
| 2026-08-30 | `lore-consistency` | Session 9, Shestendeliath Hold: earlier the same scene, established (and wrote into `shestendeliath-hold.md` itself, as a deliberate consistency fix) that the gate was sealed from inside by the bound garrison and nobody has looted the site in 15 years because the retainers attack intruders. Then, two beats later, improvised — without checking that fact — that someone had physically entered the muniment room after the fall to doctor the gate ledger's watch-night page and formally close the family records. Those two claims directly contradict each other: if the garrison stops all intruders, no one could have gotten in to tamper with anything. Caught by the DM, who stopped play rather than let it compound further ("don't run the scene forward, you're making a mistake in the continuity") | Reconciled rather than retconned away: the records visit happened once, in the immediate aftermath of the house's fall, by an Ember Crown Temple representative with legitimate standing as part of the attainder process (already established via Tain: the attainder was "irregularly fast-tracked") — not a random intruder defeating the garrison. The garrison's oath-bound recognition of legitimate temple authority (distinct from "attacks all intruders," which the folk rumor and 15 years of failed looters still support) explains the access without contradicting either established fact. Updated `shestendeliath-hold.md`'s "What happened here" section to state this explicitly so it can't self-contradict again for this site. Root cause: introduced a new access-related detail without re-reading the site's own already-written access/security facts first, in the same scene I'd written them. No mechanical check is practical here (semantic contradiction detection between prose passages isn't a diffable fact a script can reliably catch, the same class of judgment call already accepted for NPC voice/scene-pacing quality) — the concrete fix is procedural: added an explicit note to `dungeon-design.md` requiring a re-read of a site's own "what happened here"/access section before improvising any new entry/intrusion detail for that site, once one exists |
| 2026-08-30 | `combat-geometry` | Same wayside Specter fight, immediately after the damage-ask fix below: the DM pointed out `solo_burst_check.py` itself (built earlier this same turn) had the identical blind spot — its damage parser only read the base `1d12+6` off Kriv's Attacks cell and never looked at the `(+1d8 radiant vs undead)` clause, so the 48 HP fix it recommended was computed against a floor (25/round) that undercounted his real output against the fight's actual target, an undead. Recomputed with the bonus included, Kriv's real floor vs undead is 34/round, requiring 51+ HP — 48 was still marginally short | Added a `--vs <creature-type>` flag to `solo_burst_check.py`, reusing `attack_damage_check.py`'s same bonus-clause regex, so the floor includes a matching conditional bonus when the caller states the actual target type. Live-tested: `--vs undead` correctly raises Kriv's floor from 25.0 to 34.0/round and correctly flags 48 HP as still under the new 51.0 minimum. Live specter corrected again, mid-fight: `hp_max` 48→56, `hp_current` 24→32 (preserves the 24 damage already dealt, adds the same +8 delta to both so the creature ends up with exactly the extra toughness the corrected floor calls for, rather than silently erasing Ilvaneth's already-resolved Scorching Ray hit) |
| 2026-08-30 | `workflow-skip` | Session 9, the wayside Specter fight: asked Kriv's player for damage as plain "1d12+6 radiant" on both hits, missing that his own Attacks table already records the Barrow-King's Blade as `1d12+6 radiant (+1d8 radiant vs undead)` — and the target was a Specter, an undead. Same root shape as the 2026-08-29 Mithral Chain Mail/Cloak of Elvenkind Stealth-ruling miss (`character_modifier.py`'s own origin incident): a generic assumption was applied instead of the specific bonus already written on the sheet. Caught by the DM before the damage was resolved | Built `attack_damage_check.py` — given a character, a weapon-name substring, and an optional `--vs <creature type>`, it parses the Attacks table's Damage cell directly (base dice/type, plus any "(+NdM type vs X)" bonus clause) and prints the exact formula to ask for, plus a Great Weapon Fighting reroll reminder if that fighting style is on the sheet. Live-tested against Kriv's real file: correctly adds the bonus vs "undead", correctly omits it vs "human" or with no `--vs` given, and correctly reports "no bonus clause" for his mundane Greataxe |
| 2026-08-30 | `combat-geometry` | Session 9: a Thornlands travel roll (result 9, Undead) was built live as a single base-SRD Specter (22 HP, CR 1) for a level 5 two-PC party, reasoning "daytime + solo = automatically light" — never checked against what this party can actually do in one round. Kriv's Barrow-King's Blade alone averages 25 dmg/round (Extra Attack), meaning he could end the fight in round 1 before the specter ever acted — a zero-tension non-event. Caught immediately by the DM before any damage was dealt ("ÇOK KOLAY Fight HATA"). Root cause: `two-player-scaling.md`'s own "solo is necessary but NOT sufficient" HP-sizing technique has only ever been applied by hand to *prepared* bosses — the equivalent live-improv extension that already exists for GROUPS (2026-08-29 fix) was never written for SOLO results, so nothing prompted a check here | Retconned live before any turn resolved: raised the specter to 48 HP via a new `combat_status.py` patch key (`hp_max` — didn't exist before this fix either; auto-fills `hp_current` to match if the combatant hadn't taken damage yet). Built `solo_burst_check.py` — reads both PCs' actual Attacks tables, computes each one's real single-round damage floor (highest-avg attack row × its stated attacks-per-round multiplier), and flags REVIEW if a proposed solo enemy's HP doesn't clear 1.5x the strongest PC's floor alone. Live-tested against the real character files: correctly flags 22 HP (floor 25 from Kriv, needs 37.5+) and correctly clears 50 HP. Added the matching concrete trigger to `two-player-scaling.md` (mirroring the group-fix's own 2026-08-29 addendum) so this applies to every future live-improvised solo result, not just this one. Updated `encounters/thornlands-wayside-specter.md` and re-patched the live `combat_state.json` to reflect the corrected HP before play continued |
| 2026-08-30 | `hook-fragility` | `npc_description_check.py` flagged "**Initiative:**" (my own bold header introducing the combat tracker's initiative-order code-fence display) as a brand-new NPC speaking. 12th `hook-fragility` instance — same root shape as the already-fixed "**Üzerinde:**"/"**Loot:**" bullet-list false positives, just a different structural block (a code fence) following the bold label instead of a bullet list; the existing `after.startswith(("-", "*"))` skip didn't cover it | Extended the skip to also cover a code fence (` ``` `) and a markdown table row (`\|`) immediately following the label — swept proactively to the table case too since this project's combat/status displays render as both fences and pipe tables interchangeably, not just the one shape that actually fired. Live-tested 4 cases (the exact Initiative false positive, the prior Loot bullet case, a synthetic table-row header, and real NPC dialogue) — all correct, no loss of real detection |
| 2026-08-30 | `hook-fragility` | `npc_jobs_reminder.py`'s name-matching used raw substring containment (`label in speaker or speaker in label`) to link a speaking NPC to a `jobs-and-opportunities.md` entry — "Yveth Corrane" (a brand-new NPC, properly named/filed this session) false-matched "Corran" (a different, established Thornwick NPC with open jobs) because "corran" is a literal character substring of "corrane." Same root shape as this session's other name-collision findings, different script | Switched to whole-word-token overlap (`re.findall(r"\w+", ...)` sets, intersected) instead of raw substring containment — "corrane" and "corran" no longer share a token, so no false match, while "Corvin" still correctly matches "Corvin Thale" via the shared "corvin" token. Live-tested both cases: the Corrane false positive is now clean, a real Corran-speaking turn still correctly blocks |
| 2026-08-30 | `xp-loot-missed` | Narrated finding 45 gp on Marsh's body (Ostwick ambush loot) but never actually called `update_character.py gold_add` for it — caught later while writing it into Ilvaneth's Notable Items list, which would have falsely stated "already added to totals" | Applied it (23/22 split) before finalizing the item note; corrected the note's wording to match |
| 2026-08-30 | `hook-fragility` | `npc_description_check.py` flagged "On them"/"In the leather bag" (my own bold loot-list headers, e.g. "**On them:**\n- 45 gp") as brand-new NPCs speaking — same root shape as the earlier "**Ne yaptım:**" OOC incident, but this time inside real in-fiction narration, not meta-discussion, so the existing `OOC_BLOCK_RE` skip didn't cover it. A bold label followed by a bullet list is structurally never dialogue | Added a check: if the text immediately after a matched `**Label:**` starts with a markdown bullet (`-`/`*`), skip it — a real NPC line is followed by prose or a quote, never a list. Live-tested: the exact false-positive case (loot headers) now produces clean output; a real undescribed new NPC's dialogue tag still correctly triggers the block, confirming no loss of detection. Cleaned a test-pollution entry ("gorven blackwood," a synthetic test name) out of the real campaign's `npc_description_state.json` afterward |
| 2026-08-30 | `format-bug` | Building a new improvised encounter file (`ostwick-alley-ambush.md`) for a party-initiated ambush, its `## Enemies` table used non-standard headers ("HP"/"Initiative Mod" instead of the standard "HP Each"/"Initiative" every other encounter file in the campaign uses) — `combat_status.py --init` silently fell through to hardcoded defaults (8 HP, +0 initiative mod) with no error, initializing "Marsh" at 8/8 HP instead of the real 27. The exact same silent-default failure shape this file's own PC-parsing code already had to fix once (the AC-defaulting-to-10 bug, noted in its own comments) had never been applied to the enemy-side parser. Caught immediately by eyeballing the combat display after `--init`, fixed live by directly patching `combat_state.json` before continuing the fight (the `--update` patch command doesn't expose `hp_max`/`initiative` as settable keys — only `hp_current`, `conditions`, `death_saves`, `advance_turn`, `round` — so a JSON patch attempt silently no-op'd too, a second, smaller instance of the same "unsupported key, no error" shape) | Two fixes: (1) corrected the encounter file's own headers to match the established convention. (2) Hardened `parse_encounter()` to fail loudly — checks for `hp each`/`initiative` keys explicitly before parsing rows, exits with a clear error naming the actual headers found if either is missing, rather than silently defaulting. Live-tested against a disposable test campaign: a correctly-headered encounter inits normally, an incorrectly-headered one now fails with an actionable error instead of producing wrong combat state |
| 2026-08-30 | `hook-fragility` | While fixing the row above, found a second, more structurally significant issue in the same function: enemy initiative in `combat_status.py --init` was generated with plain `random.randint()` — real randomness, but internal to the script and never printed, unlike every other DM-side roll in this campaign, which goes through `roll.py`'s `SystemRandom` + visible-output convention specifically so no DM roll is ever unauditable. This has been true for every encounter this campaign has ever run via `--init`, not just this one — never caught because the numbers were always genuinely random, just invisible | Switched to `random.SystemRandom()` (matching `roll.py`'s own source exactly) and added a printed line per enemy (`"{label} initiative: {raw}+{mod} = {roll}"`) so the draw is visible in the same way a PC's initiative confirmation already was. Live-tested: the printed line appears correctly, matches the computed initiative shown in the combat display |
| 2026-08-30 | `state-drift` | Ilvaneth's own character file already recorded (session 7, day 47) that 150 gp of her total was Corvin Thale's earmarked advance for the exact Verrick collection job the party just completed — but when the 90 gp purchase from Halvard Verrick was resolved, it was split fresh 45/45 between Ilvaneth and Kriv instead of drawn from that already-tracked advance. A fact that existed in the file wasn't checked before the transaction, the same failure shape as the Ash-Kilns/debt-book fabrications earlier this session, just smaller stakes and money moving the wrong direction instead of a number being invented outright. Caught by the DM directly | Reversed Kriv's 45 gp (refunded), had Ilvaneth cover the remaining 45 gp herself so the full 90 gp came out of her own total (which already includes the advance) rather than the party's pooled funds. Updated her Notes to show 60 gp of the 150 gp advance still remaining, to be accounted for when she reports back to Corvin. No new script built — this is the first time an earmarked-advance scenario has come up (not yet a 2+ pattern per this file's own frequency discipline); the fix is a standing personal reminder to check a paying character's Notes section for advance/earmark flags before resolving any job-tied purchase, escalate to a real check if this recurs |
| 2026-08-30 | `missing-system` | DM-proposed root fix, after `travel_roll_check.py`'s "Night 1" collision recurred 4 times in one session despite two patches (OOC-block skip, then a broadened search): the relative per-journey night-numbering convention itself ("Gece 1" meaning "this leg's first night") was the actual design flaw, not just the regex matching it — it's inherently ambiguous (which journey's Night 1?) and structurally guaranteed to collide with any later recap of that same night. Patching the detector twice had addressed symptoms, not the cause | Switched the narration convention to absolute in-fiction day numbers ("Gece 49," the real `state.md` day, never a relative "Gece 1") and rewrote `travel_roll_check.py` to match: it now extracts every "Gece N"/"Night N" mentioned and only flags if N equals the party's actual CURRENT day with no `--travel` roll in the same turn. This is strictly more precise than the old heuristic (ties to a real fact instead of assuming "1 means fresh") and structurally immune to the recap collision — a past day can never equal today's day, no OOC-block skip even required for this specific case (kept anyway as a second independent guard). Documented the new convention in `travel-encounters.md`. Live-tested 4 cases: today's day with no roll → blocks; today's day with a roll → clean; a past day recapped → clean; old-style bare "Night 1" text with current day 49 → clean (confirms the original collision class is now structurally impossible, not just currently unlucky) |
| 2026-08-30 | `hook-fragility` | The `OOC_BLOCK_RE` fix above was applied and re-fired anyway, 4th time running, even though the triggering "Night 1" text couldn't be found in the turn's own final response text on manual re-reading. Most likely cause (not fully confirmed — no direct transcript access): the "current turn" window boundary (`last_user_idx`, found by scanning backward for a real `type: user` message) may be extending further back than intended when a Stop-hook block's relayed feedback isn't recognized as a fresh boundary, pulling an EARLIER turn's OOC block (which did say "Night 1" twice) back into scope. The original fix's `^\s*\*\*OOC` anchor only excluded a block if the marker was the very first thing in it — fragile against exactly this kind of boundary uncertainty | Broadened all 7 scripts sharing this pattern (`batching_check.py`, `npc_description_check.py`, `npc_detection_check.py`, `npc_jobs_reminder.py`, `npc_pending_reminder.py`, `npc_verification_check.py`, `travel_roll_check.py`) from an anchored `.match()` (marker must be at position 0) to an unanchored `.search()` (marker anywhere in the block skips the whole block) — robust to block-splitting and window-boundary uncertainty alike, since real in-fiction narration should never contain the literal string "**OOC" regardless of where the boundary actually falls. Live-tested against a simulated multi-turn transcript reproducing the suspected shape (an OOC block containing "Night 1" pulled into a later turn's scan window) — confirmed clean. All 7 scripts compile clean |
| 2026-08-30 | `hook-fragility` | `travel_roll_check.py` false-fired 3 turns in a row — every time an OOC reply referenced "Night 1" while explaining that Night 1 had already happened and didn't need a fresh roll, the reply's own recap text re-triggered the exact check it was explaining away. This is the identical `DIALOGUE_TAG_RE`-family bug already fixed in 6 other scripts this session (the "**What did I do:**" incident) but never applied here, since `travel_roll_check.py` uses its own separate `NIGHT_ONE_RE` scan with no OOC-block skip. 7th `hook-fragility` instance, and the clearest case yet that the fix needed to be swept to every script sharing this text-scanning shape, not just the ones that happened to fire first | Added the same `OOC_BLOCK_RE` skip (a block opening with `**OOC` is excluded from the scan) used everywhere else this session. Live-tested: an OOC block mentioning "Night 1" now correctly produces no block; a real narrative block mentioning "Night 1" with no `--travel` call in the same turn still correctly blocks — no loss of the real detection |
| 2026-08-30 | `hook-fragility` | Writing the corrected `Current Location` ("On the road, Harrowgate → Ostwick — Night 1 resolved...") after fixing the day/location drift, `cinematic_trigger_hook.py`/`location_check.py` false-fired a first-arrival cinematic check for Ostwick even though the party hadn't arrived — still mid-journey, tracks found on the path. Root cause: both scripts' `APPROACH_WORDS` pre-window check has no entry for an arrow between two location names ("X → Y"), a natural way to write "traveling from X toward Y" that neither script's existing word list anticipated. 6th `hook-fragility` instance | Added `"→"`/`"->"` to both scripts' `APPROACH_WORDS` list (kept in sync, matching this pair's existing convention). Live-tested: the exact triggering text now correctly allows clean, and a real arrival with no arrow/approach word still correctly fires the cinematic check — no loss of the real detection. Also reworded `state.md`'s `Current Location` itself to be less ambiguous regardless ("a day out from Harrowgate," no arrow) as a belt-and-suspenders fix, not a substitute for the script fix |
| 2026-08-30 | `state-drift` | Session 8, returning to play: Night 1 of the Harrowgate→Ostwick leg (merchant convoy, rumor #12 given) was actually played out at the tail end of session 7 — `travel-encounters.md`'s roll log and `rumors.md`'s delivery log both recorded it (day 48-49) — but `state.md`'s Current Location and In-fiction day were never advanced to match, still reading "Harrowgate, day 48" at Session 8's start. Neither `sync_state.py` nor `session_state_gate.py`'s other checks catch this class of drift — they verify the party stat block and day-based clock markers against each other, not `state.md`'s location/day against `travel-encounters.md`'s own roll log. Caught because rolling what looked like "Night 1" fresh (before checking the existing log) surfaced the pre-existing entry; confirmed by the DM directly ("I remember it") rather than assumed | Corrected `state.md`: day 48→49, Current Location to "on the road, Night 2 pending," Last Long Rest retroactively applied to day 49 (`rest_check.py --apply`, both PCs already full so a no-op numerically, but the marker now matches). This also surfaced a real World Events roll owed (last rolled day 42, now day 49 — exactly the 7-day cadence) that would otherwise have gone unrolled if the day had stayed stuck at 48 — rolled it (result 5, Rumor #9 about Mother Vey's daughter, queued for the next social scene) rather than skip it. Updated `travel-encounters.md`'s roll log row to show Night 1 resolved / Night 2 pending instead of both "being resolved in play." No structural fix built for the underlying detection gap (state.md vs. travel-encounters.md consistency) this time — flagged here for visibility per the frequency table below, escalate to a real script check if this recurs |
| 2026-08-30 | `missing-system` | Same session as the Ash-Kilns fabrication (row below): the DM read `holdings.md` further and caught a second, structural instance of the same underlying mistake — Fennick's debt book was modeled with `Income`/`Upkeep` (a perpetual weekly cash stream) even though a debt ledger is fundamentally a **finite pool** (roughly forty households' existing obligations, per `npcs/fennick-orle.md`), not an operating business. Unlike the Ash-Kilns case this wasn't a fabricated number so much as the wrong *shape* of model entirely — no amount of "the real number should be X gp/week" would have been correct, because a fixed debt pool can't regenerate weekly the way a trade route can | Added a second holding `Type` to the system: `Business` (Income/Upkeep/Last collected, unchanged) vs `Pool` (`Total pool`/`Remaining pool`, depletes as collected, never regenerates). `holding_status.py` extended with `holding_type()`, `pool_status()`, a shared `split_and_pay()` helper (de-duplicated from the original Business-only `--collect`), and Pool-specific `cmd_collect_pool` supporting a new `--amount` flag for partial collection (the debt-book's actual fiction: cashing in debts burns the leverage they represent, so a partial collect that preserves the rest as leverage is a real, meaningful choice, not just a UX nicety). Live-tested end-to-end on a disposable test campaign: full status display, partial collect (50 of 150gp, correct split + remaining-pool update), full collect of the remainder (correct DEPLETED state), idempotent no-op on an already-depleted pool, and a request for more than the remaining balance correctly capping at the actual remaining amount rather than overpaying — all verified before updating `holdings.md`'s own documentation (a new "two holding types" section explaining when each applies) and re-confirming zero drift in the real `ashen-crown` character files afterward |
| 2026-08-30 | `npc-inconsistency` | Building `holdings.md` as a live worked example for the new holding-management system, the Ash-Kilns entry was given **40 gp/week income and 12 gp/week upkeep with no basis in any session** — no negotiation with Odric Fenn for a cash cut was ever played out, and his own file's "What he can offer" section (legitimacy, the cell's cooperation, screening records, ash-supply access, Choir intel) never mentions money. This is the exact class of error `npc-consistency.md`'s core rule exists to prevent — an NPC commitment asserted with no trace back to what was actually established — just produced by a systemic ledger file instead of live dialogue. `power-base.md`'s own existing tracker line was explicit and was overridden anyway: *"No income/upkeep mechanics exist yet for this holding — build them the moment the party actually tries to draw a number from it, not before."* Caught by the DM directly, reading `holdings.md` cold | Corrected the entry: Income/Upkeep replaced with an honest "not established" note citing the actual `npcs/odric-fenn.md` offer and naming this as a corrected fabrication, not silently swapped. Added a standing note to `holdings.md`'s own header (the rule that got violated, read before adding numbers to any `Held` entry) distinguishing `Held` numbers (must trace to something that actually happened) from `Not yet taken` *projected* numbers (explicitly speculative, fine to estimate ahead). Found and fixed a second bug while correcting the first: `holding_status.py`'s `status.lower() == "held"` exact-match checks (3 call sites) broke the instant the Status field grew a descriptive suffix ("Held (access/leverage only...)") — silently stopped showing the risk tier and the "can't compute accrual" fallback with no error, the same brittle-exact-match shape already caught once for the Risk tier field itself. Replaced all three with a shared `is_held()` using `.startswith("held")`. Re-verified live: `--status` now correctly shows the fallback message + risk tier for the corrected entry, and `--collect "ash-kilns"` correctly refuses with an explicit error instead of paying out a number that was never real |
| 2026-08-30 | `npc-inconsistency` | Building `holdings.md` as a live worked example for the new holding-management system, the Ash-Kilns entry was given **40 gp/week income and 12 gp/week upkeep with no basis in any session** — no negotiation with Odric Fenn for a cash cut was ever played out, and his own file's "What he can offer" section (legitimacy, the cell's cooperation, screening records, ash-supply access, Choir intel) never mentions money. This is the exact class of error `npc-consistency.md`'s core rule exists to prevent — an NPC commitment asserted with no trace back to what was actually established — just produced by a systemic ledger file instead of live dialogue. `power-base.md`'s own existing tracker line was explicit and was overridden anyway: *"No income/upkeep mechanics exist yet for this holding — build them the moment the party actually tries to draw a number from it, not before."* Caught by the DM directly, reading `holdings.md` cold | Corrected the entry: Income/Upkeep replaced with an honest "not established" note citing the actual `npcs/odric-fenn.md` offer and naming this as a corrected fabrication, not silently swapped. Added a standing note to `holdings.md`'s own header (the rule that got violated, read before adding numbers to any `Held` entry) distinguishing `Held` numbers (must trace to something that actually happened) from `Not yet taken` *projected* numbers (explicitly speculative, fine to estimate ahead). Found and fixed a second bug while correcting the first: `holding_status.py`'s `status.lower() == "held"` exact-match checks (3 call sites) broke the instant the Status field grew a descriptive suffix ("Held (access/leverage only...)") — silently stopped showing the risk tier and the "can't compute accrual" fallback with no error, the same brittle-exact-match shape already caught once for the Risk tier field itself. Replaced all three with a shared `is_held()` using `.startswith("held")`. Re-verified live: `--status` now correctly shows the fallback message + risk tier for the corrected entry, and `--collect "ash-kilns"` correctly refuses with an explicit error instead of paying out a number that was never real |
| 2026-08-30 | `missing-system` | DM direct request: close both `holding-management` and `downtime-activities` `❌ Missing` gaps in `system-register.md` — these had sat unbuilt since `power-base.md` and `dm-notes.md` were first written, and were about to become live (Renata Kroll's Harrowgate offer, the Ash-Kilns held since day 33 with zero income ever collected) | Built `info/holdings.md` + `holding_status.py` (structured ledger, `--status`/`--collect`/`--risk-check`, income/upkeep accrue per in-fiction week and never decay while uncollected) and `info/downtime-activities.md` + `downtime_check.py` (`--research`/`--carouse`/`--craft`/`--resolve`, shells out to `character_modifier.py` for real modifiers, never a hand-picked number). Both explicitly documented as homebrew house rules, not SRD content — this project's free SRD has no downtime chapter, so the numbers are stated as original and labeled as such rather than misattributed. Two real bugs caught live-testing `holding_status.py` before wiring in: a `base()` path helper was one `dirname()` call too deep (landed at `Desktop` instead of the project root, silently reporting "no holdings.md found"), and the Risk tier field's own trailing explanation text broke tier lookup until split on its em-dash. Every mutating path (`--collect`, `--carouse`'s `gold_spend`) was tested against disposable throwaway campaign directories (`_test_holdings`, `_test_downtime`, deleted after) rather than the real character files — confirmed zero drift in real `ashen-crown` gold/state afterward. The Ash-Kilns' real first `--collect` (60 gp accrued since day 33) is deliberately left uncollected for the DM to trigger in actual play, not applied during system-building |
| 2026-08-30 | `hook-fragility` | `npc_description_check.py`'s Stop-hook fired for real, flagging "What did I do" (a bold section header, "**What did I do:**", in my own OOC dev-discussion reply about the just-built pending-NPC system) as a brand-new NPC speaking with no description. Root cause: `DIALOGUE_TAG_RE` matches any `**Text:**` pattern, and the shared `EXCLUDE_RE` name-exclusion list (identical across 5 scripts) had no entry for this Turkish meta-header, or for OOC dev-discussion generally — the same class of gap the list was already built to cover (PC names, "Sonuç:", "Not:", etc.), just missing this specific term. 5th instance of `hook-fragility` — a pattern, per this file's own frequency table | Swept all 6 scripts sharing `DIALOGUE_TAG_RE` (`npc_description_check.py`, `npc_pending_reminder.py`, `npc_jobs_reminder.py`, `npc_verification_check.py`, `batching_check.py`, `npc_detection_check.py`) rather than patching only the one that fired. Structural fix, not just a word added to the list: any text block opening with `**OOC` is now skipped entirely from dialogue-tag scanning in all 6 (this project's own established convention for marking dev-discussion, so a whole class of future meta-headers can't false-positive the same way, not just this one term) — `EXCLUDE_RE` also extended with "ne yaptım"/"ooc" as a belt-and-suspenders catch for a mixed block. Live-tested: a synthetic OOC block with 3 bold headers produces clean output on all of `npc_description_check.py`/`npc_verification_check.py`/`batching_check.py`/`npc_pending_reminder.py` (correctly still blocks for the *real* reason — Renata Kroll's pending business genuinely unaddressed — not the false one), and real NPC dialogue (Renata Kroll speaking) still correctly increments `npc_verification_check.py`'s counter, confirming no regression |
| 2026-08-30 | `missing-system` | DM design question at Session 8's start: an NPC with an open, undecided offer (Renata Kroll's standing-force offer — Active Quests, Status "Pending", no deadline given, sitting in Harrowgate) shouldn't require the party to specifically go looking for them; the moment the party is IN that NPC's home settlement, the NPC could plausibly appear, send word, or simply be run into. Nothing surfaced this before except memory — a real gap, live right now, since the party is in Harrowgate as of this session with Renata's offer unaddressed since session 4 | Built `npc_pending_check.py` (standalone lookup, reuses two data sources that already exist — Active Quests' Giver+Pending status and each NPC file's own `**Location**` field, no new bookkeeping format), `npc_pending_trigger_hook.py` (PreToolUse, just-in-time reminder the instant Current Location is edited — mirrors `cinematic_trigger_hook.py`'s pattern), and `npc_pending_reminder.py` (Stop hook, retroactive safety net with persistent per-location idempotency — mirrors `location_check.py`/`npc_description_check.py`'s patterns). Live-tested: positive match (Renata/Harrowgate) and negative matches (Gallowmere, Karsgate, Millward Crossing) on the standalone script; PreToolUse allow-with-context and allow-clean on both matching and non-matching location edits; Stop-hook block (nothing checked/no dialogue), clear-via-check-call, clear-via-NPC-dialogue, idempotent re-call, and clear-then-re-fire-on-location-change all verified against synthetic transcripts before wiring into `.claude/settings.json`. One incident during testing: a synthetic test accidentally wrote a fake location directly into the real `party/state.md` (a POSIX `/tmp` path wasn't visible to Windows `py`, so a test transcript silently failed to load and a *later* test step's direct regex-edit of `state.md` landed for real) — caught immediately via the file-change system reminder, restored the correct Current Location before continuing. Root cause noted for future test-writing in this repo: always stage test fixtures under the Windows-native scratchpad path, never a bash-only `/tmp` path, when the script under test will be invoked via `py` (not bash) — Git Bash's `/tmp` and Windows Python's filesystem view don't share a path space |
| 2026-08-30 | `workflow-skip` | Category 4's NPC-description gap was logged 2026-08-29 as "no available hard enforcement" — a real, honest conclusion at the time, but reached before this session's later multi-signal text heuristics (`npc_detection_check.py`, `batching_check.py`'s NPC-exchange signal, `npc_jobs_reminder.py`) proved the same class of problem could actually be caught well enough to be useful. The conclusion was never revisited even after those techniques existed | Built `npc_description_check.py` — flags a brand-new NPC's first line of dialogue if no race word appears anywhere in that turn's narration, evaluated once per NPC and never re-flagged. Live-tested: 3 synthetic cases + a repeat-appearance check + real transcript, all correct |
| 2026-08-30 | `state-drift` | `CLAUDE.md`'s own AIGM coverage table said Category 1 had "no staleness check for Open Threads; no payoff tracking for foreshadowing" — both were already built and wired into `sandbox-index.md` earlier the same session (the 7-item AIGM gap-closing round), but the table cell was never revisited afterward. Same root cause as every other state-drift entry in this log — a fact recorded once, never re-checked against later work — just at the level of the project's own status-summary documentation instead of campaign content | Verified both scripts still work correctly against real data before touching anything. Corrected the table cell. Built `aigm_table_check.py` (`PostToolUse` hook on CLAUDE.md edits) to catch this exact class going forward — flags any AIGM gap-description mention of a `.py` script that already exists in `.claude/scripts/`. Live-tested: clean on the fix, correctly fires on an artificially reintroduced stale mention (scoped precisely to the gap column, confirmed it does *not* false-fire when the same text is in the coverage-percentage column instead) |
| 2026-08-30 | `workflow-skip` | Buildable-list item #6: a pacing report flagging an imbalance (spotlight, content mix, downtime) had no memory of its own past flags — if the same imbalance was still present next window, it looked identical to a brand-new finding, with nothing distinguishing "just noticed" from "noticed and ignored twice" | Added `last_flags` persistence to `session_pacing_check.py` — each report's flags reduce to stable keys, compared against the prior window's, and a repeat gets an explicit "STILL UNRESOLVED" annotation. Live-tested across 3 cycles: fresh flag → no annotation, exact repeat → annotated, partial overlap → only the genuinely-repeated keys annotated. Closes the full 4-item Buildable list |
| 2026-08-30 | `workflow-skip` | Buildable-list item #1: `batching_check.py` only ever covered combat, despite `narration.md`'s own rule being written generally ("a turn, a night, a roll, an exchange") after being caught in BOTH a combat round AND a multi-night journey the same session — the travel half of the incident that produced the rule was never actually hooked, only the combat half was | Extended `batching_check.py` with a travel signal (same structural `--travel` roll-count-vs-text-blocks comparison as combat) and a weaker NPC-exchange text heuristic (sequencing markers in the DM's message + compressed dialogue tags). Live-tested: 5 synthetic cases all correct, real transcript clean |
| 2026-08-30 | `content-gap` | Buildable-list item #4: `content_variety_check.py` (now `session_pacing_check.py`) only ever showed a single window's content mix — nothing tracked whether Downtime/Personal specifically kept coming up empty window after window, which is the actual DM-flagged symptom ("it feels very empty to whoever's actually playing the game") rather than any single window's snapshot | Added a persistent `downtime_streak` counter to `session_pacing_check.py`'s state, escalating to an `ALERT` line at 3 consecutive zero-downtime windows (~60 turns). Live-tested: streak climbs correctly across synthetic zero-downtime windows and resets the moment downtime content appears |
| 2026-08-30 | `workflow-skip` | Buildable-list item #3: `npc_jobs_check.py` (the organic-job-surfacing lookup, built earlier the same session) had no enforcement of its own — it existed, was correct, and still depended entirely on Claude remembering to call it before a scene with a job-carrying NPC. Exactly the failure shape that produced the script in the first place (Cole, Corvin Thale both surfaced their jobs only because the party asked directly) | Built `npc_jobs_reminder.py`, a `Stop` hook scanning each turn's dialogue tags against every NPC with an `OPEN`/`LATENT` `jobs-and-opportunities.md` entry, blocking if the matching NPC spoke without a same-turn `npc_jobs_check.py` call. Live-tested (3 synthetic cases + real transcript, all correct) before wiring into `.claude/settings.json` |
| 2026-08-30 | `workflow-skip` | The DM revealed directly that `/dm-end-session` is a command they almost never run. This meant an entire session's worth of automation built earlier the same day (`npc-tier-verifier`, `npc-fact-verifier`, four advisory sweep scripts, `session_spotlight_check.py`, `content_variety_check.py`, player-feedback capture) had never actually fired in real play — all of it was wired into that one command without ever checking whether the command itself gets used. A deeper layer made it worse: several of those checks read `session-N-conversation.md`, which itself only gets written by `/dm-checkpoint-log` or `/dm-end-session` — confirmed live, session-7's own conversation log was still its original 6-line stub after the entire session's play | Root-caused to copying the existing two agents' wiring pattern without questioning it. Fixed the two verifier agents' trigger: `npc_verification_check.py`, a transcript-reading `Stop` hook that accumulates NPC dialogue volume across turns and blocks to run both agents once a DM-calibrated threshold (20 tags) is crossed — no command, no flushed file required. `session_spotlight_check.py`/`content_variety_check.py` still have the same flushed-file dependency and have not yet been converted — flagged to the DM as explicitly still open, not silently left broken |
| 2026-08-30 | `workflow-skip` | Closes the remaining half of the row above, DM-approved same session ("Çevirelim"): `session_spotlight_check.py` and `content_variety_check.py` still read `session-N-conversation.md`, gated behind the same rarely-run `/dm-end-session` command | Deleted both, replaced with `session_pacing_check.py` — a single `Stop` hook reading the live transcript directly (same pattern as `npc_verification_check.py`), self-triggering every 20 assistant turns with a combined spotlight-balance + content-mix report, no command or flushed file required. `dm-end-session/SKILL.md` Step 5 updated to drop both from the manual list. Live-tested against the real session transcript (turn counter increments correctly, block fires at threshold with a real computed report, `--reset`/`--status` both verified) |
| 2026-08-30 | `workflow-skip` | (continued) (real `Stop`-hook block for combat-turn batching) against the actual live session transcript triggered a false positive — not from gameplay, but because the test-fixture-generating Python code written moments earlier in the same turn contained the literal strings `combat_status.py` and `advance_turn` as part of building synthetic test cases, and the naive substring match on Bash command text counted those too | Confirmed via isolated synthetic-file tests (no meta-code in the transcript) that the underlying detection logic is correct — the false positive is specific to writing test-generation code inside the same live session as the tool being tested, which never happens during normal play. Documented as a known, accepted edge case in the script's own docstring rather than building a stricter command parser to rule out an occurrence that can't happen in real gameplay |
| 2026-08-30 | `format-bug` | While building `npc_jobs_check.py` (AIGM gap-audit item #3), the first regex only recognized `## From X` headings for both finding a section AND bounding where it ends. Coren Ashvale's section has no other "From" heading after it before end-of-file, so it silently swallowed three unrelated sections (Kriv's thread, regional threats, self-directed threads) whose headings don't start with "From" — reporting 11 "job entries" for an NPC who actually has exactly 1. **Third occurrence of this same mistake this session** (Karsgate's gazetteer parsing, and the earlier `#### Bag of Holding` subheading truncating `character_modifier.py`'s Notable Items scan) — a real pattern: match ANY heading of the relevant level for boundary purposes, never assume the next occurrence of the specific pattern being searched for is also the correct end-of-section marker | Rewrote to match any `##` heading for section boundaries, filtering to "From X" only for the actual name search. Re-tested against Coren Ashvale (now correctly 1 entry), Corvin Thale, and Hesta Bram — all correct |
| 2026-08-30 | `state-drift` | While retrofitting `**[Day N]**` tags onto all 20 `state.md` Open Threads entries (AIGM gap-audit item #6), found two already-resolved entries still marked open: "Sefwyn will arrange contact with Tain" (the meeting happened this same session, day 42) and "Ilvaneth on Corran's game list" (the game was played this same session, day 47-48) — both left unmarked simply because nothing ever re-checked an old entry against what had already happened | Marked both resolved with a cross-reference to where the actual resolution is recorded (Active Quests table, `npcs/vesa-ilkraven.md`). Built `open_threads_check.py` specifically so a thread sitting stale doesn't require remembering to check it — 20+ day-old open entries now get surfaced automatically at session start |
| 2026-08-30 | `npc-inconsistency` | While wiring the four advisory sweep scripts into `/dm-end-session` (fifth item off the gap-audit list) and running them live to verify, `npc_connection_check.py` surfaced 3 real one-directional NPC references not caught by the 2026-08-29 sweep: `aldric-venn.md`→`sarelle-duskbourne.md`, `dallin-marsh.md`→`fennick-orle.md`, `vharkoss-shestendeliath.md`→`sarelle-duskbourne.md`. These had been sitting unreciprocated since whenever they were written, invisible because the script that catches them was never actually run on a schedule | Reported to the DM, who confirmed fixing them immediately. Added reciprocal Connections entries to `sarelle-duskbourne.md` (×2, for Aldric Venn and Vharkoss Shestendeliath) and `fennick-orle.md` (Dallin Marsh) — each written from what that NPC would actually know, not just a mirrored restatement. Re-ran `npc_connection_check.py`: clean, all Important-tier cross-references now reciprocated. The structural fix still matters most: the script now runs automatically every session-end instead of only when remembered, so this class of drift can't silently accumulate for a full year again |
| 2026-08-30 | `format-bug` | While building `settlement_density_check.py` (fourth item off the gap-audit list), the first regex draft only matched settlements written as a same-line gazetteer bullet — Karsgate's entry is a `### IV. Karsgate` section heading with the population and `locations/karsgate.md` reference several lines further down, so Karsgate (the campaign's largest, most-flagged-for-thinness settlement) was invisible to the check entirely. Separately, Thornwick's population is stated as "~35 households," not people — comparing that raw number against the people-based tier table directly would have silently misclassified its tier | Added a section-heading fallback pass (matches by `### N. Name`, searches the whole section for population + file reference) and household detection with a documented 4.5-people/household conversion. Live-tested: all 8 real settlements now correctly parsed and checked (was 7, missing Karsgate); a synthetic "Nettlecombe at 9,000" case correctly flagged as under-built |
| 2026-08-30 | `state-drift` | While building `ascension_check.py` (second item off the gap-audit list, DM-requested), found `ascension-tracks.md`'s own "Tracker" table's last row was still "Session 3, 2 fragments" despite the party holding 3 fragments since session 6 — the row for the third fragment was never added. Currently inert (both PCs are still level 5, below the level-7 floor for stage 1, so the recorded stage 0 happens to still be correct) but a real staleness in the file's own history log | Added the missing "Session 6, 3 fragments" row. Built `ascension_check.py` to catch this class of drift going forward — computes the correct stage from fragment count + level against `ascension-tracks.md`'s own gate table, flags a mismatch against state.md's recorded stage. Wired into `session_state_gate.py` |
| 2026-08-30 | `xp-loot-missed` | Not a new incident — a proactive fix to an already-4x-repeated category (2026-08-25, 2026-08-26 ×2). Per the DM's own audit request ("how do we scan the system without getting stuck on bug-log alone"), `dm-notes.md`'s "combat XP... are checklist items, not memory" rule was flagged as the strongest still-open candidate: it explicitly says "there is no backstop here besides catching it live" for the XP half, despite `combat_state.json` already tracking enough structured data to fix it | Added `xp_awarded` tracking to `combat_state.json` and `combat_xp_check.py` (Stop-hook gate) — see `system-register.md`'s matching entry for the build/test detail. Frequency count not incremented (no new occurrence today), row kept for traceability to the pattern it closes |
| 2026-08-30 | `hook-fragility` | While expanding the cinematic-opening trigger from "settlements only" to "every named location" (direct DM instruction), found that `cinematic_trigger_hook.py` and `location_check.py` each carried their own hardcoded `KNOWN_MAJORS` copy — the exact duplication already caught diverging once before (2026-08-27, one script had a fix the other didn't). Expanding the trigger to ~37 location names would have meant maintaining two long hand-written lists in parallel, the same fragility at a much larger scale | Replaced both hardcoded lists with a shared dynamic-load function reading every `locations/*.md` file's H1 title — self-maintaining, a new site is covered the moment its file exists, no list to keep in sync at all. Live-tested both scripts against a new/unvisited location (correctly flagged) and an already-visited one (correctly silent) |
| 2026-08-30 | `missing-system` | After a session with repeated same-category failures (NPC descriptions ×4, an equipment ruling from memory, a cross-NPC knowledge fabrication), Claude's own analysis concluded three of six categories had "no available hard enforcement" and should stay written-rule-only. The DM pushed back directly and explicitly: full automation is the standing priority for this commercial-grade project ("whether the work is long or hard, we will do it"), and "a hook can't evaluate this" should never be the first conclusion reached. On reflection, this was correct — the earlier analysis stopped at the first-obvious mechanism (regex over narration text) without trying harder options: explicit state-tracking, a script that computes the answer instead of asking Claude to remember, a reminder tied to the actual triggering tool call, or a second independent verification pass | Added a "Producer's Vision" section to `CLAUDE.md` making this a standing directive, and sharpened `bug-protocol.md`'s escalation ladder to require exhausting harder mechanisms before concluding a gap is unfixable. Then built all three: `character_modifier.py` (equipment rulings become a script query, not a memory check), `concealment_status.py` + `npc_detection_check.py` (a real `Stop`-hook block, narrowed to a two-signal keyword match after live-testing caught real false positives on unrelated "anlaştılar"/"tanıştı" dialogue), and the `npc-fact-verifier` agent (a session-end independent pass, mirroring `npc-tier-verifier`, for the one category — cross-NPC knowledge fabrication — where even a live hook wouldn't have caught the actual incident since the source file had likely already been read). See `system-register.md`'s matching entry for full build detail |
| 2026-08-29 | `workflow-skip` | Session 7 continuation, Harrowgate → Ostwick road, night 1: a second travel-encounter merchant was voiced with full dialogue and no physical description, the fourth instance of this exact gap in one session (Tain's aide, a first merchant, a five-NPC location batch, now this). The batch-writing checkpoint added earlier this session (list races before writing descriptions) didn't cover this case — it's a single NPC, not a batch, so the fix's own scope was too narrow. Caught by the DM again, not self-caught | Described on the spot (half-orc, scarred brow, weathered driving coat). **Honest assessment recorded rather than another rule rewrite**: this category has no available file-based hook — `npc_tier_hook.py` and its kin only see edits to tracked `.md` files, and live NPC dialogue in travel/social scenes is chat text, not a file write, so there's no mechanical surface to hook into. Per `bug-protocol.md`'s own allowance to admit a judgment-call gap has no hard enforcement available yet rather than oversell a fourth prose patch, this is logged as a known residual risk — the DM catching it live remains the actual backstop, the same accepted pattern already stated in `dm-notes.md` for short-rest resource application |
| 2026-08-29 | `workflow-skip` | Session 7 continuation, the four new Karsgate-hinterland location files: all five named NPCs (Osgar Fenwick, Halda Merrow, Corwin Aldis, Tam Ferro, Rennit Dhale) were written as human, despite `narration.md`'s existing "don't default to human" rule — the third time this exact rule was violated in the same session (Tain's aide, a travel merchant, now a whole batch). Caught by the DM directly, not self-caught. Root cause: the rule reads fine for a single NPC written in isolation, but breaks down under batch-writing pressure — race selection becomes an afterthought when several NPCs are being drafted in the same response | Reassigned all five (dwarf, half-elf, gnome, halfling, half-orc) — no two the same. Strengthened `narration.md`'s existing rule with a concrete batch-writing checkpoint: list every new NPC's chosen race in one line before writing full descriptions, whenever 2+ are being created in the same response. Built `race_variety_check.py` (advisory, mirrors `name_collision_check.py`) to catch aggregate human-default drift campaign-wide going forward — run and verified clean (human at 26% of 42 detected files, not disproportionate) after this fix |
| 2026-08-29 | `combat-geometry` | Session 7 continuation, night 4: a travel-table result ("Concordat party, undead honor guard escort, Dangerous") was improvised live as 6 skeletons + 2 priests (3,600 adjusted XP, ~1.6× Deadly at level 5) and narrated as a standard clustered marching column, with none of `two-player-scaling.md`'s own group-hardening techniques (sub-group spacing, an alert exception) applied. With surprise in hand, a Fireball + Kriv's full attack routine would have cleanly resolved most of the group at near-zero risk — precisely the surprise+AoE trap the file exists to prevent. The DM caught it before committing to the attack, asking directly whether this was actually a fair scale. Root cause: the three group techniques (added 2026-08-29, earlier this session's own history) had only ever been applied to *prepared* encounter files (Gnoll Battlefield, Feral Menagerie) — nothing carried them into live, on-the-spot improvisation of a travel-table result | Retconned the formation before any attack was declared: the "patrol" is actually a spread-out search party (thematically apt — searching for tracks, not marching), 15-20+ ft apart, no single Fireball radius can catch the whole group. Added a concrete trigger to `two-player-scaling.md`: the instant a travel/random table result names 3+ enemies with the party holding surprise, apply spacing/alert-exception *before* describing positions to the table, not after positions are already known and a plan is being built around them. **First pass incomplete, DM caught it immediately**: only spacing (technique #2) was applied — sub-group splitting by area (#1), an alert exception (#3), and a tougher leader (#4) were all still missing, and standard SRD HP (13/27) is fragile enough against this party's burst that spacing alone doesn't remove the trap, exactly as the file's own "solo is necessary but NOT sufficient" section already warns for solo bosses and, it turns out, applies to groups too. Completed the fix: split into two sub-groups physically out of blast range of each other, and upgraded one priest to a named investigator (~double HP, sharper passive Perception) as a real leader-tier presence. **Second incompleteness, same DM push-back**: even after sub-groups/leader, the rank-and-file were still plain CR 1/4 Skeletons — identical stats whether the party is level 1 or level 5, when `bestiary.md`'s own signature-creature table explicitly says "escalate them rather than replacing them" and lists Wights/Specters as the Tier-2 (level 5+) step up from Skeletons/Zombies. Reused Sarelle's own operational-hardening narrative (already established this session via Tain) to justify upgrading part of the honor guard to Wights (CR 3, 45 HP, necrotic/nonmagical-physical resistance) rather than leaving everything at Tier-1 stats regardless of party level. Swept the session for the same live-improv gap elsewhere: none found — this was the only combat-eligible live encounter so far |
| 2026-08-29 | `workflow-skip` | Session 7 continuation, night 4 stealth approach: Kriv was ruled to roll Stealth with disadvantage from `combat-rules.md`'s armor table alone ("Chain Mail → disadvantage"), without checking his own character file — which already records **Mithral Chain Mail** ("no Stealth disadvantage — replaces his old mundane Chain Mail") and an attuned **Cloak of Elvenkind** (advantage on Stealth, hood up). The correct ruling was advantage, the opposite of what was applied. Caught by the DM before the roll's result was used | No re-roll needed — Kriv had already rolled two d20s (11, 15) under the incorrect disadvantage instruction; reinterpreted as advantage (take the higher: 15) rather than wasting the already-random rolls. Added a clarifying note to `combat-rules.md`'s Armor and Stealth section: the table is a default, not a final answer — always check the PC's actual Notable Items/equipment entry for material (mithral removes disadvantage) or magic-item overrides before applying it. Swept the rest of the session for the same "ruled from generic assumption instead of the actual sheet" pattern: none found |
| 2026-08-29 | `workflow-skip` | Session 7 continuation, travel to Harrowgate: a merchant convoy got full dialogue and a rumor exchange with no physical description at all, and — in the same session's earlier Tain scene — his aide was left as "a young aide" with no race chosen. Both violate already-written rules (`dm-notes.md`'s "every NPC gets a visible physical description" and "don't default to human"), skipped because both NPCs read as low-stakes/background, and the physical-description rule's text didn't explicitly say it applies to unnamed/transactional NPCs too (only the separate file-creation requirement has an explicit "not a one-line shopkeeper" carve-out) | Retroactively described both on the spot (no NPC file needed for either, per the existing file-requirement carve-out). Added a concrete clarifying line to `dm-notes.md`'s existing standing rule: the physical-description requirement applies to unnamed/transactional NPCs exactly as much as significant ones — a file and a description are two separate requirements with two different bars, not one bundled threshold |
| 2026-08-29 | `npc-inconsistency` | Session 7 continuation, Millward Crossing: closing the Tain scene, narrated "Sefwyn de uzaktan gözlemliyordu" (Sefwyn was watching from a distance) as an accomplished, perceived fact in the read-aloud text. This came straight from the quest's own DM-prep note ("Sefwyn will watch from a distance, uninvolved if anything goes wrong") — a staging detail, not something the party had rolled or acted to notice. Worse than a bracket leak (the existing hidden-info rule's usual failure mode): this wasn't even bracketed, it was presented as directly witnessed. Caught by the DM immediately | Retracted the line — Sefwyn's presence stays true in the fiction (per the original staging note) but unconfirmed to the party, since nothing was rolled or done to surface it. Extended the existing `dm-notes.md` "hidden info" standing rule (rather than writing a new one) with a named example and a concrete check: before narrating any hidden observer/off-screen actor/DM-only staging detail as a fact, verify the party actually did something (a check, a search, the NPC's own choice to reveal) that would have surfaced it. Swept the rest of the scene for the same leak: none found — this was the only DM-prep detail narrated as directly perceived without a check behind it |
| 2026-08-29 | `npc-inconsistency` | Session 7 continuation, Millward Crossing: when Kriv named House Shestendeliath to Orell Tain, Tain's recognition was narrated as coming partly from "the fighting style Roskel mentioned" — but Roskel and Tain have never met and have no established channel between them at all. Roskel's recognition of Kriv's fighting style was a private session-2 scene between Kriv and Roskel alone; nothing in either NPC's file links them. Caught by the DM directly ("there is NO WAY he could know about Roskel"). Distinct from the self-interest violation this file's main rule already covers — this was inventing a fact of *shared knowledge between two NPCs* that never existed, not an NPC acting against its own motive | Retconned the line: Tain's recognition now rests only on the spoken name itself plus his own archive knowledge (already demonstrated earlier in the same conversation) — no external corroboration needed or claimed. Added a new "cross-NPC knowledge rule" to `.claude/rules/npc-consistency.md`: before one NPC references what another NPC (or a PC's private scene) said/learned off-screen, verify a real channel exists in a file (Connections section, shared faction, or something told to them directly in the current scene) — never let one NPC's knowledge silently draw on a different NPC's private conversation. Dismissed `standing_rule_hook.py` (judgment call, not a diffable fact — mirrors the existing self-interest rule's own reasoning). Swept the rest of the scene's Tain dialogue for the same pattern: none found — the investigator info, the delivery-order reading, and the Ash-Warden archive knowledge were all legitimately grounded in his own established role |
| 2026-08-29 | `workflow-skip` | Session 7 continuation, Millward Crossing: narrated Orell Tain recognizing that Ilvaneth's face was magically disguised ("a changed face reveals itself eventually") as flavor text, with no roll behind it — caught only because the player asked OOC how he knew. Root cause: the dice-ownership table in `CLAUDE.md` lists "Enemy attacks, saves, damage" as Claude's rolls but never named NPC skill/ability checks used to detect a PC's hidden action (Investigation vs. Disguise Self, Perception vs. Stealth, Insight vs. Deception) — an unlisted sub-case of the same existing rule, easy to skip without the table naming it explicitly. The retroactive roll (Investigation 1d20+8 vs. DC 15, Tain's own file numbers) came back 9 — a **fail** — meaning the original narration wasn't just unrolled, it was wrong | Added an explicit row to `CLAUDE.md`'s ownership table for NPC detection checks, plus a clarifying line on the hard rule: a sentence narrating that an NPC "notices"/"sees through"/"recognizes" a hidden PC action states a die result exactly as much as a number does, and needs the roll first, not retroactively. In-fiction correction: Tain's line reframed as a general/philosophical remark, not a specific detection — he does not know Ilvaneth is disguised. Swept for the same live issue elsewhere in the current scene: none found (first live instance of this specific sub-case) |
| 2026-08-29 | `format-bug` | First-ever manual live test of `npc-tier-verifier` (spawned directly, ahead of its real `/dm-end-session` trigger, so the DM could see actual output before trusting the wiring): the agent's core work was correct — checked all 32 NPC files, 0 disagreements, 0 structural anomalies — but its own printed summary said "28 Important-tier NPCs" and "4 Minor-tier NPCs," then listed 6 names under Minor. 28+6=34, not 32; the real split is 26 Important + 6 Minor. Caught by the DM reading the output, not by the agent itself — exactly the "don't trust the same pass to grade itself" failure this whole agent exists to prevent, now recurring one layer up, in the verifier's own report | Rewrote `npc-tier-verifier.md`'s Step 3: counts must now be derived from `len()` of the actual list printed, never stated as a separate typed number; a required sanity check (Important-count + Minor-count = total checked) must pass before the report is sent, with an explicit instruction to recount from the real lists rather than adjust the printed number to make the arithmetic look right. The specific failure (28/4 vs 32 files) is written into the agent's own instructions as a named past incident, matching this project's standing convention of documenting failure modes directly in the file that must not repeat them |
| 2026-08-29 | `missing-system` | DM pushed back directly on two proposed fixes for guaranteeing correct NPC tier classification: (1) surfacing the Tier decision visibly in chat so the DM catches errors by reading — rejected, since the DM explicitly wants the system to run automatically, not depend on their own review ("if it depends on my approval, I'd just handle everything myself"); (2) a keyword-heuristic script scanning NPC text for phrases like "wants to control/own/lead" — the DM correctly identified this is trivially avoidable (Claude can write vaguer prose, intentionally or not, and nothing would catch it). Both proposals tried to patch the *check*, not the actual root cause: the same Claude that writes an NPC's content also grades its own Tier call, under the same time-pressure incentive to cut corners either way | Recognized the project already has the correct pattern for exactly this shape of problem: `encounter-verifier` never trusts the pass that wrote an encounter to also check it against the SRD. Built `npc-tier-verifier` (new agent, `.claude/agents/npc-tier-verifier.md`) — independently re-derives Important/Minor from each NPC file's own substantive content (not keywords) before looking at the file's stored Tier line, flags disagreements, never auto-corrects. Wired into `/dm-end-session` as an unconditional step, mirroring `session-manager`/`story-teller`. Added `--list` to `npc_tier_check.py` to support it. **Could not live-test in this session** — a newly-created `.claude/agents/*.md` file isn't picked up by the Agent tool until the next session starts (agent definitions are snapshotted at session start); this was disclosed to the DM directly rather than claimed as verified. Real first run will be the next actual `/dm-end-session` |
| 2026-08-29 | `hook-fragility` | `standing_rule_hook.py`'s flag correctly recorded `npc_tier_hook.py`/`npc_tier_dismiss.py`/`session_state_gate.py`'s `check_npc_tier()`/`settings.json` all being touched as real script/hook additions — but `check_standing_rule()`'s own early-clear branch deletes the pending flag the moment it sees `script_touched` with no `rule_added` yet ("nothing to enforce"), reasoning that a script-only touch is moot. When the matching `.claude/rules/npc-tiers.md` documentation edit landed in a *later* Stop-hook cycle, a fresh flag was created with only `rule_added` — the earlier script-touch memory was already gone, producing a false BLOCK for a rule that in fact already had real, tested enforcement | Dismissed with an accurate, verified reason (re-confirmed all four pieces exist and are wired before dismissing, not a blind bypass). **Deliberately not loosening `check_standing_rule()`'s early-clear behavior** — the alternative (keep a stale `script_touched` around indefinitely so it can pair with an unrelated future rule) trades an occasional false block for a possible false pass, and a false pass is the worse failure direction for this specific gate. Documented so a future occurrence of this exact shape is recognized immediately rather than re-diagnosed from scratch |
| 2026-08-29 | `workflow-skip` | DM asked directly whether NPCs created from now on are actually guaranteed to meet `npc-tiers.md`'s standard. They weren't — the standard was prose, `npc_tier_check.py` and `npc_connection_check.py` existed but nothing forced either to run, and `npc-tiers.md` itself wasn't even in `dm-start-session`'s mandatory reading list yet, the same gap already caught and fixed for `two-player-scaling.md`/`dungeon-design.md`/`faction-design.md` two entries above this one in this same log | Added `npc-tiers.md` to the mandatory reading list. Built real hook enforcement for the diffable half of the standard: `npc_tier_hook.py` (`PostToolUse`, flags an incomplete NPC file the instant it's touched) + a new `check_npc_tier()` in `session_state_gate.py` (blocks at `Stop` if still incomplete, auto-clears if fixed same-turn) + `npc_tier_dismiss.py` for genuine stubs. Live-tested block/auto-clear/dismiss with a disposable test file before wiring in |
| 2026-08-29 | `npc-inconsistency` | DM asked directly whether the 25 Important-tier NPCs' cross-relationships (same-faction goals, opposition, betrayal potential) were actually synchronized. A manual audit plus a new script found 10 one-directional links: Coren Ashvale's file never mentioned his own captain Sethra Shestendeliath; Renata Kroll's opposition to Compact control wasn't reflected on Kell's or Coren's side; Yeva Ashwright's file still said the Karsgate cell was "not yet built" after Kessic Ambrey's file (referencing her by name) had already been built; Corla Vint and Sella Dray's implied rivalry existed only in a newly-written `consequences.md` threshold, not either NPC's own Connections; Aldric Venn's file never named his own superior, Varic Sennett; Doreth Kell's file didn't acknowledge his own subordinate (Dallin Marsh) was a mole; Odric Fenn's file didn't mention Yeva Ashwright despite her being due to visit him; Sefwyn Marrow's file didn't credit itself as the actual bridge to Orell Tain; Corla Vint and the Broker (Ironhold's equivalent) weren't aware of each other; the Hollow Prophet's file (brand new this session) wasn't yet linked back from Yeva's or Fenn's older files | Fixed all 10 with reciprocal Connections entries. Built `npc_connection_check.py` (advisory, mirrors `name_collision_check.py`'s pattern) to catch this class of gap going forward — scans every Important-tier NPC's `npcs/<slug>.md` references for reciprocity. Re-ran after fixes: 3 remaining one-directional links are legitimate asymmetry (a minor figure aware of a faction leader who has no reason to know they exist) and were left as-is, not fixed blindly |
| 2026-08-29 | `format-bug` | While verifying the full 25-file NPC-tier sweep, `npc_tier_check.py` reported "no consequences.md threshold to match yet" for entities that definitely had one (Sarelle Duskbourne, Sefwyn Marrow, Coren Ashvale, and 9 others) — the script's own name-matching required the NPC file's H1 title to exactly prefix-match the `consequences.md` heading, so any file with an honorific, nickname, or subtitle in its title ("Archivist-Magus Sarelle Duskbourne," '"Iron" Sella Dray,' "Corvin Thale — The Sable Casket") silently failed to match its own threshold | Rewrote the match as case-insensitive substring containment checked in both directions — catches a decorated title wrapping the plain consequences.md name, or the reverse. Re-ran all 25 files clean afterward |
| 2026-08-29 | `content-gap` | DM asked for a full retroactive scan of which existing NPCs qualify as Important under the new `npc-tiers.md` standard — checked all 31 NPC files against the test and found most had never been evaluated for independent agency at all, since the standard itself was built the same session | Classified all 31; DM confirmed 6 as Minor and corrected 2 borderline calls (Sylvenna, Dallin Marsh) up to Important. Built all 25 Important files to the full standard (Tier, Class & Level, Move List) plus a new `npcs/the-hollow-prophet.md` from scratch, and added 17 new `consequences.md` threshold blocks. See `system-register.md`'s matching entry for full detail |
| 2026-08-29 | `missing-system` | DM raised a direct design question (not a play incident): `npcs/kessra-vane.md`, built earlier this session via World Event roll, had a reactive Decision-logic section (how she responds if the party finds her) but nothing making her act independently — no proactive movement, no real combat stats beyond a generic SRD-block pointer. The DM's own words: an NPC with no move list is "no different from background scenery." `npc-consistency.md`'s existing proactive-agenda rule (2026-08-29, same file) only covered a `consequences.md` threshold number — it never specified that Important NPCs also need a real class/level or a concrete list of playable actions tied to that threshold | Built `.claude/rules/npc-tiers.md` (Important vs Minor test, PC-style class/level scaled to narrative weight, a required `## Move List` format) and `npc_tier_check.py` (structural checker, mirrors `faction_design_check.py`). Applied in full to Kessra Vane as the worked example — real Rogue 5 (Thief) stat block sourced from SRD class-feature lookups, a new `consequences.md` "Kessra Vane — Closing In" threshold (2/3, backfilled), and a three-tier Move List. Retrofit for other existing Important NPCs (Sefwyn Marrow, Sella Dray, Orell Tain, Marta Vantor, Kessic Ambrey, Sarelle Duskbourne) deferred to their next actual appearance, per the same retrofit discipline `npc-consistency.md` already established |
| 2026-08-29 | `workflow-skip` | DM asked directly whether the solo-boss reinforcement pass had also been applied to routine group encounters — it hadn't. Worse, HP-buffering (the solo fix) is the wrong tool for groups entirely; the original audit's most common trivialized fights (goblin band, 4 stirges, kobold Warren Entrance) were all groups killed by a single AoE before acting, and that half of the original problem had gone completely unaddressed while the fix chased the less common solo case | Added a distinct group-specific technique set to `two-player-scaling.md`: sub-group splitting (already partly in use), physical spacing within a sub-group so one AoE can't catch it all, an alert-sentry exception so a group isn't always entirely surprised, and an occasional tougher leader/sergeant (Roskel is the campaign's own proven example). Applied immediately, not deferred, to `the-gnoll-battlefield.md` and `the-feral-menagerie.md` |
| 2026-08-29 | `workflow-skip` | DM pushed back directly on the same-session "prefer solo strong creatures" fix: turning every real fight into one big monster would just trade one monotony for another, and — more importantly — the campaign's own audit already disproved the claim that solo alone fixes the surprise+AoE trap. The Kargrim's Reach basilisk and the Widow's Hollow ochre jelly were both solo and both still went down in one hit with zero party HP lost ("the basilisk never got a turn," a direct quote from the audit that then went on to recommend solo creatures as the fix). The fix I'd just shipped contradicted evidence already sitting in the same document | Corrected `two-player-scaling.md`: solo is necessary but not sufficient — the real fix is HP sized against this party's actual round-1 burst (not the CR table), damage resistance/immunity for anything meant to survive, and Legendary Actions/Resistance (SRD-precedented via dragons) for save-reliant bosses. Retroactively applied to everything built under the old, incomplete guidance: all five `regional-threats.md` bounties got explicit HP-buffer notes (several bumped 20-40 HP above SRD base) and the cloaker got reframed to ambush the party itself rather than the reverse; Grask (Giant's Cairn) and the High Ward (Unremembered Shrine, now also with Legendary Resistance) both bumped. Also corrected the stale "confirmed encouraging" claim in `system-register.md` that had asserted the opposite |
| 2026-08-29 | `missing-system` | DM asked for a full cross-system audit: does everything built today follow the project's own logic, does the "permanent system, script-enforced, nothing forgotten" discipline actually hold, are there any conflicts, does `CLAUDE.md` need updating. Checked `.claude/skills/dm-start-session/SKILL.md`'s Step 1 mandatory-read list (the one guaranteed-loaded context every session) against everything built today and found `two-player-scaling.md`, `dungeon-design.md`, and `faction-design.md` were all missing from it — these are judgment-call standards no hook can enforce (unlike drift/rest/thresholds, which `session_state_gate.py` already checks every turn regardless of session start), so if they aren't explicitly loaded into context each session, a future session has no mechanical guarantee of remembering them and could repeat exactly the mistakes they were built to prevent (the surprise+AoE trap, a thin dungeon, a thin faction). `CLAUDE.md`'s own file table and "Note on enforcement" were also stale — missing `bug_signal_hook.py` and `standing_rule_hook.py` from the hook list, and not mentioning any of today's rule files at all | Added all three files to `dm-start-session/SKILL.md`'s Step 1 list (with a note explaining why judgment-call standards need this treatment when state-facts don't), and a note to Step 3 that the timeline is now three mechanisms, not one. Updated `CLAUDE.md`'s file table (added rows for `two-player-scaling.md`, `dungeon-design.md`, `faction-design.md`, `campaign-clock.md`'s expanded scope, and the standing-rule-ladder enforcement) and rewrote the "Note on enforcement" paragraph to list the complete, current hook set. Verified: `session_state_gate.py`'s `CHECKS` list and both new hook functions (`check_bug_signal`, `check_standing_rule`) read correctly with no duplication; `name_collision_check.py` re-run, no new collisions since the last sweep |
| 2026-08-29 | `format-bug` | DM asked to review `bug-log.md` for still-open items. Found the combat-difficulty entry (row above) and the Sefwyn-agenda entry immediately after it had been silently merged into a single 3,250-character table row — the Edit that added the combat entry apparently didn't insert an actual newline before the next row's `\| date \| category \|` prefix, so the Sefwyn row's own date/category columns were swallowed into the previous row's Resolution cell, breaking the table structure for both entries at once. Ironic given this is the bug-log's own table | Split back into two properly delimited rows (verified via line length: 3,250 chars → 1,990 + 1,338). No content was lost, only the row boundary. Standing discipline this reinforces: after any Edit that inserts a new dated row into this file's Log table, sanity-check the line count/length of the affected lines before moving on — a table row should never be one multi-thousand-character line |
| 2026-08-29 | `missing-system` | DM flagged directly that combat has felt too easy and unsatisfying — "yenilmez gibiyiz" (we feel unkillable), fights ending before enemies can act when the party opens with surprise. A full retroactive audit (Explore agent, every logged fight session 2-6) confirmed this precisely: the goblin band, 4 stirges, the grick, the basilisk, the kobold Warren Entrance, Skree's Warren, and — most significantly — **both twelve-plus-body Concordat convoy ambushes (session 3 and session 6, both rated 2,400+ adjusted XP, "Deadly" on the standard table)** were all resolved with **zero party HP lost**, because surprise removes a full round of enemy actions and this party's AoE/burst (Thunderwave, Fireball, Scorching Ray crits) compresses multiple enemies' HP pools into that same dead round. `two-player-scaling.md`'s adjusted-XP formula computes correctly every time but never modeled this specific interaction — a real, structural gap in the difficulty system itself, not an execution mistake. Genuinely tense fights in the same audit (Roskel, the Warden, Hollowmoor's reversed-surprise skeletons, Kargrim's Reach's "alert, no surprise" rooms, the Web Gallery ettercap) all shared one trait: surprise was denied, or the alpha strike didn't cleanly finish the target | Added a new section to `two-player-scaling.md` ("The surprise+AoE trap") documenting the audit's findings and six concrete building techniques going forward: deny surprise more often (patrols, blindsight/tremorsense/scent-tracking, already proven with the hell hound), prefer solo strong creatures over groups for anything meant to feel dangerous (confirms `regional-threats.md`'s five bounties were already built this way), spread enemy formations deliberately, vary resistance/immunity to this party's signature damage types, build past Deadly on the standard table when surprise is likely, and vary how fights start instead of always opening with an unseen scout-and-choose setup |
| 2026-08-29 | `missing-system` | DM asked whether NPCs pursue their own agenda over time rather than just reacting to the party — using Sefwyn Marrow as the example ("does Sefwyn have a plan from within, or is every NPC just a loyal servant"). Checked `npcs/sefwyn-marrow.md`: the reactive layer (self-interest, conditional Decision-logic branches) was already solid, but the file explicitly states "their private tally continues to grow independent of what the party knows about it" with nothing anywhere tracking that growth. Swept for the same shape elsewhere: only Kell's escape fund (`campaign-clock.md`) had ever gotten this treatment — a proactive personal-agenda tracker, as opposed to a purely reactive Decision-logic branch | Extended `consequences.md`'s threshold format (previously faction-only) to individual NPCs for the first time: "Sefwyn Marrow — Private Tally" (1/3, pending, threshold = deciding they have enough Crown knowledge to sell it and disappear). The party's own choice to show Sefwyn more directly controls the clock's speed — a deliberate lever, not just flavor. **All four built in the same session, corrected here 2026-08-29** (this line was stale) — Kessic Ambrey, Sella Dray, Orell Tain, and Marta Vantor all now have `consequences.md` threshold blocks (each 1/3, backfilled), and the trigger itself was made a permanent standard in `.claude/rules/npc-consistency.md`. See `system-register.md`, 2026-08-29 |
| 2026-08-29 | `state-drift` | DM confirmed the Alis Wend thread had been fully closed (session 5, day 33, via the second lie) while reviewing the faction-clock list just shown. Checked `campaign-clock.md`'s own "Alis Wend runs out" section — it still read "FIRED, day 25... not resolved, not forgotten... surfaces the next time they're near Gallowmere," even though `state.md` had recorded the actual resolution back on day 33. Two files disagreeing about the same fact for two sessions | Rewrote the clock section to match `state.md`'s existing resolution and the DM's explicit closure ruling (no lingering exposure risk); corrected the Fired log row to show fired day 25 → resolved day 33 instead of "unresolved"; softened `state.md`'s own trailing caveat ("could still put the pieces together later") to match the DM's ruling that this is now permanently closed |
| 2026-08-29 | `state-drift` | DM asked how the timeline mechanism works and whether anything from today's new content needed adding to it. Checked `campaign-clock.md`'s own Fired log table (the record of every clock that's actually fired) against the narrative "Sarelle collects" section — Sarelle's Pattern-of-Losses threshold crossing (day 42, applied the same session it was built) was described in prose there but never given a row in the Fired log, unlike every other fired event in the file | Added the missing Fired log row. Also added a "three mechanisms" cross-reference table to the top of `campaign-clock.md` — the timeline is now genuinely split across three files (day-based faction clocks here, 7-day World Events, threshold-based Consequences) with nothing previously explaining how they relate in one place |
| 2026-08-28 | `content-gap` | DM asked whether World Events could actually fire in today's new content. Checked `world-events.md` directly: entries 7 and 8 (the "monster/hoard activity" categories) both explicitly instructed picking from `gazetteer.md`'s `[sketch]` sites — but today's earlier build-out converted every Chapter 1 `[sketch]` site to `[full]`, so those two table entries had no valid target left. A stale reference to content that no longer exists in the campaign's own current state | Rewrote both entries: #7 now escalates one of `regional-threats.md`'s five apex bounties, #8 now develops an already-`[full]` site past its written content (naming Kesh Deeps, Sella Dray, and Kessic Ambrey as concrete examples) |
| 2026-08-28 | `workflow-skip` | Same sweep: `npcs/yeva-ashwright.md` (built earlier this session) states she's due to visit Fenn "within the next several weeks" — a real future NPC promise left as prose, the identical shape already caught and fixed once this project (Sefwyn Marrow's re-contact promise sitting forgotten 12 days because it wasn't a real clock, `bug-log.md` 2026-08-27). The standing rule added after that incident — any "NPC will re-initiate contact" promise must be written as a dated `campaign-clock.md` entry — existed the whole time this session but wasn't applied to a new NPC created in the same session | Added `⏳ Yeva Ashwright's circuit visit to Fenn — fires ~day 52` to `campaign-clock.md`, checked against `clock_check.py` for clustering (none found) |
| 2026-08-28 | `content-gap` | Same sweep: two named characters with real narrative substance — Ostwick's Dunnel Ashe (a tragic wereboar figure) and the Weeping Wood's dryad — existed only as paragraphs inside their location files, violating the standing rule that "a named NPC with real weight gets a proper file the same session they're introduced," previously caught with Magus Orell Tain (2026-08-26) | Built `npcs/dunnel-ashe.md` and `npcs/sylvenna.md` (named the previously-unnamed dryad), both with full Current-knowledge/Decision-logic sections from the start. Location files updated to point to them instead of describing them inline |
| 2026-08-28 | `content-gap` | DM asked for a full sweep of the session's content. Manual + scripted check found Karsgate had **four** separate things named around "Ledger" (the district itself, "The Ledger Watch House," "The Sunken Ledger," "The Salt Ledger") — the last two aren't even in the Ledger district, making the shared name actively misleading. Also found "The Cinderpath Almshouse" (Karsgate) colliding with Gallowmere's pre-existing "The Cinderpath Shrine." Root cause: generating many names quickly in one batch, with no cross-file naming awareness | Renamed: "The Sunken Ledger"/"Cellar" → "The Foundered Warehouse"/"Cellar," "The Salt Ledger" → "Thale's Landing," "The Cinderpath Almshouse" → "The Widow's Hearth." Built `name_collision_check.py` (advisory scan across all location/NPC names, flags words in 3+ distinct names) as the permanent structural fix — 3 collisions in one batch is a pattern per `bug-protocol.md`, not one-off. Live-tested against the real campaign files: correctly flagged nothing beyond expected intentional repeats (Ashvale, Vantor, Shestendeliath family/business names; House/Hall/Row as generic English toponyms) |
| 2026-08-28 | `workflow-skip` | Same sweep: checked whether the 13 newly-built Chapter 1 sites followed the standing "read `two-player-scaling.md` before building any encounter" discipline that every other major dungeon in this campaign (Kargrim's Reach, Hollowmoor, the Weeping Tower guardian) already carries. **12 of 13 had zero reference to it at all.** Computing the real adjusted XP retroactively found two genuinely overtuned encounters that had shipped unflagged: the Weeping Wood's 3-ettercap fight (1,350 raw → 3,375 adjusted, Deadly at level 9) and Hallowmere Chapel's 3-specter fight (600 raw → 1,500 adjusted, Deadly beyond level 3) — both well past what either site's stated level range could survive | Added explicit `two-player-scaling.md` checks with real adjusted-XP math to all 12 missing files. Reduced the Weeping Wood's Ettercap Nest and Hallowmere Chapel's specter count from 3 to 2 each, with an explicit "never fight all 3 at once" note preserved for future reference. Also flagged (without needing a reduction) that Riverwatch's kobold band and the Sunken Barge's lizardfolk camp are only safe because the room/site design already prevents a full-group fight — made that safety margin explicit rather than implicit |
| 2026-08-28 | `content-gap` | DM asked directly whether the campaign's locations sat on a coherent theme/texture and whether elevation varied between them (sea level to mountaintop), pointing out that terrain should be what determines which creatures roam a region, not an arbitrary per-region monster list. Checked `gazetteer.md`: no elevation or physical-geography framework existed anywhere — individual site names implied terrain (Cindermoor a "moor," the Chalk Warrens an "escarpment," Kesh Deeps a mine implying hills) without ever tying regions into one coherent map | Added a "Regional Topography & Texture" table to `gazetteer.md` — explicit elevation profile and signature sensory texture for all 8 regions, shaped as a real ascent/descent across the Chapter 1 journey. Audited `travel-encounters.md`'s existing regional tables against it first, per the standing "don't invent speculatively" discipline — found they already reflected terrain well (Marches' water-adjacent stirges, Thornlands' barrow-heavy undead, Cindermoor's weather/will-o'-wisps, Ashvale's chalk-country manticore/chimera), so no table content needed to change, only the missing explanatory framework around it |
| 2026-08-28 | `content-gap` | **Closes the 2026-08-26 Karsgate deferral.** That entry found Karsgate marked `[full]` while actually holding 7 named locations and zero religious sites, and explicitly deferred the fix as "not urgent." It stopped being safe to defer the moment today's faction-design work made Karsgate load-bearing — both new faction files (`ashlord-concordat.md`, `ironclad-compact.md`) point at Karsgate chapterhouses, and `gazetteer.md`'s new Region Control table marks it contested. The DM asked directly whether Chapter 1 still had undesigned locations, prompting the check that confirmed the gap was still open | Rebuilt `locations/karsgate.md` in full: 42 surface locations across 4 districts (from 7), 17 Undercity locations built to `dungeon-design.md`'s standard (from an unstructured monster list), and a real religious site (the Ember Crown Cathedral) added. All three factions now have concrete, connected Karsgate presence rather than a chapterhouse mentioned once. Four new NPC files: Varic Sennett, Kessic Ambrey, Marta Vantor, Sella Dray |
| 2026-08-28 | `content-gap` | DM pushed back on the "campaign completeness vs. execution discipline" framing directly — asked whether the three factions actually have their full depth documented (alignment, resources, power, connections, intelligence networks, key-NPC web, betrayal, a prophet, where gods fit), not just new NPCs/locations. Checked before answering rather than assuming: `dm-notes.md` gives each faction 4-5 lines (Goal/Methods/Leader/Disposition); `faction-subplots.md` gives each exactly **one** internal fracture. No dedicated per-faction file exists. This is the 15th `content-gap` instance and matches the exact shape already fixed twice before at smaller scope (settlements checked by count but not category; a dungeon checked by room count but not content density) — a paragraph existing was mistaken for depth existing, at faction scale this time | **Fixed same session, corrected here 2026-08-29** (this line was stale) — built `.claude/rules/faction-design.md` (a five-phase standard co-designed with the DM) and applied it to all three factions in full: `factions/ashlord-concordat.md`, `factions/ironclad-compact.md`, `factions/cinder-choir.md`, each verified via `faction_design_check.py`. See `system-register.md`, 2026-08-28 entries |
| 2026-08-28 | `format-bug` | Caught while testing the new `consequence_check.py` (world-reactivity build) before wiring it in — not a play incident. `BLOCK_RE` used `re.DOTALL` so `.` matched newlines everywhere in the pattern, including the greedy `\*\*Threshold\*\*:\s*(?P<threshold>\d+)\s*→.*\n` line. With more than one `### Entity` block in the file, that greedy `.*` consumed forward across block boundaries and backtracked to the *last* `**Status**:` line in the whole file, silently pairing the first block's name/threshold with a different block's status and body. Single-block test files passed; the real file (which always has 2+ entities) would have silently mis-evaluated every entity but the last one | Rewrote the single-line fields (`Threshold`, `Status`) as `[^\n]*`/`[^\n]+` instead of relying on `.` + DOTALL, and confined multi-line matching to the body capture (`[\s\S]*?`) with the global `DOTALL` flag removed entirely. Verified against a 1-block file (passed even before the fix, which is why it wasn't caught immediately), a 3-block mixed file (drift/applied/below-threshold — failed before the fix, passed after), and the real `consequences.md`. Standing discipline this reinforces: a regex meant to isolate independent repeating blocks needs its per-line fields kept line-scoped even under `DOTALL` — test with 2+ blocks specifically, not just one, before trusting a "it matched" result |
| 2026-08-28 | `missing-system` | Companion gap to the entry directly below: the DM asked that the "prefer script/hook over prose" standard spread project-wide, not just live inside `bug-protocol.md`. Checked `missing-systems.md` (the other protocol that produces new standing-rule content, for building systems rather than fixing bugs) — it had zero mention of the escalation ladder, script preference, or `bug-protocol.md` at all. Anyone following that protocol on its own would write a brand-new system as pure prose with no prompt to consider scripting it first | Added an explicit pointer in `missing-systems.md`'s Step 3 ("Build it") to run the escalation ladder before writing prose. No new script needed — `standing_rule_hook.py` (built earlier this session) already enforces this mechanically for any `.claude/rules/*.md` content regardless of which protocol produced it, so this was a documentation-linkage fix, not a missing mechanism; dismissed via `standing_rule_dismiss.py` rather than adding a redundant second hook |
| 2026-08-28 | `missing-system` | Raised by the DM as a design discussion, not a play incident: `bug-protocol.md`'s own escalation ladder ("prefer a script/hook over a written rule") had no enforcement — it was only prose, the same failure shape this file already shows repeating for rest-tracking and dice-ownership before those got scripted. Nothing checked whether a *new* standing rule actually reached for a script, only whether an already-3+-repeated bug category eventually did (`bug_signal_hook.py`'s scope) | Built `standing_rule_hook.py` (`PostToolUse` on `Edit\|Write`, flags any new `.claude/rules/*.md` content or a new `dm-notes.md` "Standing rule —" section) and `standing_rule_dismiss.py` (explicit override, mirrors `bug_signal_dismiss.py`); extended `session_state_gate.py` to block the turn if a rule was written with no script/`settings.json` touched in the same turn. Live-tested (block / script-resolves / dismiss-resolves / script-only-is-moot / heading match+no-match) before wiring in. See `system-register.md`, 2026-08-28 |
| 2026-08-24 | `format-bug` | Scripts crashed without `PYTHONUTF8` on this Turkish-locale machine | Set permanently in `settings.json` |
| 2026-08-24 | `workflow-skip` | `py` wasn't in the permission allowlist despite being the only working launcher | Added to allowlist |
| 2026-08-24 | `workflow-skip` | `CLAUDE.md` instructed the opposite dice convention (final totals vs. this table's raw d20s) | Overridden at the top of `CLAUDE.md` |
| 2026-08-24 | `content-gap` | `campaign-clock.md` travel table missing a route, arithmetic implied beating a clock that was actually behind it | Route added, arithmetic corrected |
| 2026-08-25 | `format-bug` | No XP command existed in `update_character.py` at all | `xp`/`xp_add` added |
| 2026-08-25 | `workflow-skip` | Five skill files invoked `python` instead of `py`, silently failing on this machine | All switched to `py` |
| 2026-08-25 | `missing-system` | No way to change HP Maximum for level-up | `hp_max` patch type added |
| 2026-08-25 | `missing-system` | New-capability intake check didn't exist | Built `.claude/rules/new-capabilities.md` |
| 2026-08-25 | `missing-system` | Noise/light detection had no standard | Built `.claude/rules/noise-and-detection.md` |
| 2026-08-25 | `workflow-skip` | Claude rolled Thunderwave's player damage, then invented Con saves after `roll.py` failed instead of retrying | Dice-ownership table rewritten in `dm-notes.md`, hard rule added |
| 2026-08-25 | `format-bug` | `combat_status.py --init` looped forever on `EOFError`; `roll.py --help` errored | Both fixed |
| 2026-08-25 | `combat-geometry` | Surprise played as a free pre-initiative round; Thunderwave narrated as ranged when it's `Self` | Both written into `combat-rules.md`'s warning box |
| 2026-08-25 | `workflow-skip` | Two more wasteful agent spawns found (`character-tracker`, `encounter-verifier`) beyond the three already overridden | Full 12-agent ruling table added to `CLAUDE.md` |
| 2026-08-25 | `workflow-skip` | Live `conversation-tracker` agent cost 60-100s per NPC exchange | Moved to inline voicing, logged once at scene end |
| 2026-08-25 | `format-bug` | `dm-conversation-log`/`dm-combat-log` built paths from raw `$ARGUMENTS`, producing broken paths | Both skills split arguments explicitly |
| 2026-08-25 | `state-drift` | Rest tracking (HP/slots/hit dice) forgotten three times across one multi-night travel sequence | `rest_check.py` built and tested live |
| 2026-08-25 | `xp-loot-missed` | Battle Master maneuver DC miscalculated (STR save bonus used instead of STR modifier); level-3 XP denominator column mixed up | Both fixed on character sheets |
| 2026-08-25 | `npc-inconsistency` | A party-knowledge question answered from DM-prep instead of the actual conversation log, giving a wrong answer that shaped a player decision | "Known to the Party" section built in `state.md` |
| 2026-08-26 | `content-gap` | Harrowgate (population 4,000) had 5 locations — read like a village | Expanded to 10; population→location-count table built afterward |
| 2026-08-26 | `xp-loot-missed` | Combat XP forgotten twice in one session (the Concordat convoy, the hell hound) | Awarded retroactively; checklist rewritten as a precondition, not a follow-up |
| 2026-08-26 | `state-drift` | `sync_state.py` found drift **three separate times** in the same session after being told to fix it once | Wired into the `Stop` hook so the harness checks every turn, not the assistant's memory |
| 2026-08-26 | `state-drift` | Character inventories stale beyond gold — a second Crown fragment and a looted potion missing; sold horses/loot still listed as held | Both sheets corrected; `state.md`'s duplicate description trimmed to a pointer |
| 2026-08-26 | `combat-geometry` | An escort formation modeled as split across hundreds of feet instead of clustered around its cargo | Third bullet added to `combat-rules.md`'s warning box |
| 2026-08-26 | `npc-inconsistency` | `npcs/orell-tain.md` left his location vague; `locations/karsgate.md` had already placed him at the Grey Hall | Corrected to match the existing placement |
| 2026-08-26 | `workflow-skip` | Cinematic wide→close opening skipped on Harrowgate's first arrival | Retroactively delivered; `Visited Majors` list + `PreToolUse` hook built |
| 2026-08-26 | `format-bug` | Armor Stealth disadvantage is RAW but was never written into any project rule file | Table added to `combat-rules.md` |
| 2026-08-26 | `missing-system` | XP threshold denominator required a manual fix every level-up, already missed once | Fixed structurally in `update_character.py` — auto-advances now |
| 2026-08-26 | `content-gap` | No religious sites in almost any settlement, despite three religious/death-cult factions driving the whole plot | Religious site made a required category in the settlement-scaling table; added to Gallowmere |
| 2026-08-26 | `npc-inconsistency` | "Known to the Party" went stale a second time | Escalated to a `PostToolUse` hook (`knowledge_sync_reminder.py`) |
| 2026-08-26 | `workflow-skip` | This log's own "log every bug here" instruction was just a written rule — the exact failure shape already proven unreliable elsewhere this session | Escalated to a `PreToolUse` hook (`bug_log_reminder.py`) on `system-register.md`'s own Log table |
| 2026-08-26 | `workflow-skip` | Session 4: Claude rolled Ilvaneth's Burning Hands damage (3d6) via `roll.py` itself instead of asking the player — a repeat of the session-2 Thunderwave incident, the exact same failure shape the dice-ownership table was already written to prevent. The written rule (a table naming which rolls are whose) had already failed once and failed again under normal play, confirming a prose rule alone isn't enough here | Escalated past a second rewrite, per `bug-protocol.md`: `roll.py` now requires an explicit `--owner enemy\|dm-table\|npc` flag on every plain dice roll and refuses to run without one, with the refusal message itself restating that PC rolls never go through this script. This doesn't make misuse impossible (Claude could still mislabel a PC roll as "enemy"), but it converts a silent default into an active, harder-to-miss declaration on every call — the same escalation pattern as `session_state_gate.py`. The invalid roll's effects (skeleton HP) were reverted and the damage re-rolled by the actual player |
| 2026-08-26 | `workflow-skip` | Session 4, first combat: round 1 (surprised PCs + 2 enemy attacks, all real `roll.py` rolls) was resolved correctly but presented as one compressed jump straight to a "Round 2" status block — technically accurate, but the table never watched it happen turn by turn. DM: correct DM-side, but doesn't feel like good play to the player | Rule added to `combat-rules.md`: every combatant's turn, PC or enemy, gets its own narrated beat before the next; never batch multiple turns into one message. No script gate possible (pacing/feel, not a diffable fact) — the checkable trigger is counting turns-since-last-message before any status block |
| 2026-08-26 | `xp-loot-missed` | Splitting the outer-mound loot, Claude called `update_character.py ... gold 10` intending to *add* 10 gp — but bare `gold <N>` silently SETS the total, wiping Ilvaneth's 170 gp and Kriv's 111 gp down to 10 each. Caught immediately by re-reading the command's own output ("Gold set to 10 gp") rather than by the DM. Same ambiguous-verb trap existed for `xp` too (bare `xp <N>` also silently sets instead of adds), just not yet triggered in play | Gold restored (180/121 — previous total + the 10 gp share). Structural fix: bare `gold` and `xp` patch types removed entirely from `update_character.py` (they now exit with an error explaining the split) — `gold_set`/`xp_set` required for an explicit absolute set, `gold_add`/`xp_add` for the common case. `README.md` updated to match. No script gate could catch this after the fact since it's not a drift the sync scripts check for; the fix is structural (the ambiguous command no longer exists) rather than a written reminder |
| 2026-08-26 | `workflow-skip` | Ilvaneth's level 4 level-up prompt only asked for "2 new spells" — never mentioned she also gains a new cantrip known at level 4 (Wizard table). Caught only because the player asked "shouldn't I also pick a cantrip?" — the `dm-level-up` skill's own Step 5 has an explicit checkable level-list for ASI (4/8/12/16/19) but no equivalent for cantrip-known increases, so it was silently folded into (and dropped from) the generic "ask about spells" line. **The DM explicitly flagged this as a pattern, not a one-off**: every future level-up needs to be correct per 5e standard, not just level 4 | Added an explicit cantrip-known-increase table to `.claude/skills/dm-level-up/SKILL.md` Step 5 (Wizard/Sorcerer/Druid/Cleric/Bard/Warlock). Sweeping the same file for the identical shape of gap found a second, live one: the ASI level list (4/8/12/16/19) is **wrong for Fighter** (also gets ASI at 6 and 14) **and Rogue** (also at 10) — exactly the trap Kriv (a Fighter) would have hit at his very next ASI level. Fixed with a class-specific table, same format. Also added a general note to Step 4 about class-specific automatic-feature thresholds (Fighter's Extra Attack scaling at 11/20, Indomitable at 9/13/17) so a single early lookup isn't assumed to cover later scaling. Swept: no earlier level-up (2→3 for either PC) crossed a cantrip or off-pattern ASI threshold, so no other instance exists yet to fix retroactively — but level 6 (Kriv's next bonus ASI) and level 5 (Extra Attack) are now the next checkpoints to verify this fix actually holds |
| 2026-08-26 | `workflow-skip` | Same session, same category, second instance: a 3-night journey (2 travel-encounter rolls) was compressed into a single summary line and the party jumped straight to arrival, without ever seeing either night — the identical failure shape as the earlier batched-combat-turn bug, just outside combat. The earlier fix (in `combat-rules.md`) was scoped only to combat turns, so it didn't cover this case | Generalized the "never batch resolved beats" rule into `narration.md` as a rule that applies to any multi-beat sequence, not just combat rounds — the journey was retroactively re-narrated night by night for the table |
| 2026-08-26 | `workflow-skip` | The above bug was never written to `bug-log.md` in the same turn it happened, and the `bug_log_reminder.py` `PreToolUse` hook — built specifically to catch this — did not fire, because the hook only triggers on edits to `system-register.md`'s own Log table, and no such edit was made for this bug (only for the unrelated Weeping Tower encounter build). The hook cannot catch a miss where neither file is touched at all | **Fixed later the same session** (see the `bug_signal_hook.py` entry below) — built a `UserPromptSubmit` hook that fires on the DM's own correction language instead of on Claude's file edits, closing exactly this blind spot |
| 2026-08-26 | `workflow-skip` | Same root gap, addressed directly: `bug_log_reminder.py` can only catch a miss if Claude is already editing one of the two log files — it can't catch the case where neither file is ever touched, which is exactly what happened twice this session. A third rewritten prose reminder was explicitly rejected as a fix (already failed twice) | Built `bug_signal_hook.py`, a `UserPromptSubmit` hook — fires on every DM message, before Claude processes it, scanning for correction-language markers ("hata," "yanlış," "düzelt," "unuttun," "bug," etc., kept narrow to avoid firing on ordinary questions). Injects a reminder to run the full bug-protocol process, sourced from the DM's own words rather than from Claude's downstream editing behavior — upstream of the exact point where the previous hook's blind spot lived. Wired into `.claude/settings.json`, tested against a real correction message (fired), an ordinary question (didn't fire), and a normal gameplay action (didn't fire) |
| 2026-08-26 | `workflow-skip` | The DM asked directly why this isn't just enforced rather than a (third) reminder — a fair question, since a reminder Claude can still ignore isn't actually different in kind from the prose rules that already failed twice | Upgraded from reminder to **enforcement**: `bug_signal_hook.py` now drops a pending-signal flag (`bug_signal_pending.json`) recording `bug-log.md`'s mtime at the moment of the signal. `session_state_gate.py` (the `Stop` hook already running every turn) checks this flag and **blocks the turn from ending** if `bug-log.md` hasn't changed since — the same hard-block pattern already used for `sync_state.py`/`rest_check.py` drift. Auto-clears if `bug-log.md` genuinely was edited after the signal; otherwise requires either a real log entry or an explicit dismissal (`bug_signal_dismiss.py "<reason>"`) for a confirmed false positive. Tested live: signal → block → dismiss → clean, and signal → real edit → auto-clear, both verified working |
| 2026-08-26 | `workflow-skip` | Third dice-ownership violation this session: `combat_status.py --init` was called with `--initiative "Ilvaneth=15,Kriv=15"` — both flat totals typed in without ever asking either player for their actual d20 roll. `roll.py`'s `--owner` fix (from the earlier Burning Hands incident) didn't cover this because it's a different script; `combat_status.py --initiative` accepts pre-computed totals by design, which is exactly what made it easy to fabricate one | Real rolls obtained retroactively (Ilvaneth 15+4=19, Kriv 12+1=13) and the combat state corrected. Structural fix: `--initiative` now requires RAW+MOD format (e.g. `15+4`) instead of a bare total, and rejects anything else — this doesn't make fabrication impossible but makes the player's actual raw roll a visible, checkable part of the command instead of an opaque number, matching `roll.py`'s existing transparency convention |
| 2026-08-26 | `workflow-skip` | Root cause of the session-3-folder bug, found while fixing it: `/dm-start-session` never asks or checks whether a new sitting is a new session or a continuation — a `session-manager` agent silently decided "this is a resumption of Session 2 (not a new Session 3)" for what later turned out, per the DM's own later record, to actually be session 3. `/dm-checkpoint-log` just trusts whatever `state.md`'s Session # already says, so nothing downstream could catch the original bad call either | Added an explicit Step 1.5 to `dm-start-session/SKILL.md`: ask the DM directly, every time, "is this Session N continuing or a new Session N+1" — never infer it. On "new," increment `state.md`'s Session # and create the `party/session-[N+1]/` folder immediately, before any content is written into it |
| 2026-08-26 | `content-gap` | The DM asked that shop price bands actually match 5e — checked `04 equipment.md`'s real "Selling Treasure" rule (flat 50% of value, not a range) against the two settlement price files built earlier: Harrowgate had invented "50-70%," Thornwick/Corran had invented "40-60%" — both quietly drifted from RAW instead of anchoring to it, an assumption nobody had checked against the actual rule text | Added a "Selling — the RAW baseline" section to `items-and-loot.md` citing the real rule (50% anchor, haggling moves the actual result around it, not a pre-shifted band) so future settlements inherit the correct default instead of each improvising its own. Fixed both existing files to match. Upgraded from ⚠️ provisional to ✅ in `system-register.md`'s known-gaps table |
| 2026-08-26 | `content-gap` | Roskel's session-3 Harrowgate task (find a corruptible Compact contact, find a suspicious corporal instead) implied a garrison/military structure in Harrowgate, but `locations/harrowgate.md` never actually placed one — asked directly by the player where it was and had no answer on file | Added "The Garrison House" to `harrowgate.md`, following the same Compact-retainer pattern already established for Gallowmere's Captain Kell, with the suspicious corporal now placed there |
| 2026-08-26 | `content-gap` | Every NPC introduced in session 4 (Yveth Corrane, the Ironmonger smith, Corvin Thale, Aldric Venn, Renata Kroll) was written as human by default — in a setting where the party itself is an elf and a dragonborn and every non-human race is available. The DM had flagged this as a bug before ("I thought I had fixed it") and it recurred, meaning an earlier fix (if any) didn't actually stick or was never written down as a standing rule | Added an explicit rule to `narration.md`: actively choose a race per new NPC rather than defaulting to human; two same-session NPCs sharing a race should be deliberate, not accidental. Fixed live: Renata Kroll → Dwarf, Corvin Thale → Gnome, Aldric Venn → Half-elf (both existing files corrected, not just the in-session narration). Older pre-rule NPCs (Mother Vey, Fennick, Kell, Sarelle, etc.) flagged for a check-not-blind-retcon pass next time each is on-screen. **Completed same session** (DM-directed batch pass, post-session cleanup): checked each for any race already implied in play (none were — safe to assign, not a retcon) and assigned Mother Vey → Halfling, Fennick Orle → Half-Orc, Doreth Kell → Human, Sarelle Duskbourne → Elf, Coren Ashvale → Dwarf, Orell Tain → Tiefling, Hesta Bram → Dragonborn, Corran → Gnome, Alis Wend → Half-Elf. Sefwyn Marrow's race (Human) was already established live in the session 3 conversation log but never written into `sefwyn-marrow.md` — formalized, not changed. Sethra and Vharkoss Shestendeliath already have dragonborn established via bloodline (red-scaled/bronze-scaled), no fix needed |
| 2026-08-26 | `format-bug` | Writing session 4's checkpoint log, found that **no `party/session-3/` folder was ever created** — everything that actually happened in session 3 (Harrowgate arrival, the convoy ambush, Harpy Crags, Sefwyn's first meeting) lived in `party/session-2/session-2-conversation.md` and `session-2-npc-05-sefwyn-marrow.md`, mislabeled. This means any future `/dm-combat-log` or `/dm-conversation-log` call for session 3 content, or anyone reading session-2's folder expecting only session 2, would have hit stale/wrong labeling | **Fixed** (2026-08-26, DM-directed cleanup pass). Found the exact split point by outlining every scene heading in `session-2-conversation.md` and matching against `state.md`'s own "Session 2 Summary"/"Session 3 Summary" text: line 493 (`### New sitting`) is where session 3 actually begins. Split the file there — `session-2-conversation.md` now ends cleanly at the level-3 level-up, and everything from the convoy ambush onward moved to the new `party/session-3/session-3-conversation.md` with a boundary note explaining the move. Moved and renumbered `session-2-npc-05-sefwyn-marrow.md` → `party/session-3/session-3-npc-01-sefwyn-marrow.md` (Session field corrected 2→3). Updated every reference: `campaign-clock.md` (2 places), `session-log.md`'s Session 3 entry. Old file deleted, not just copied |
| 2026-08-26 | `xp-loot-missed` | The deep barrow's sarcophagus search only awarded the Barrow-King's Blade — `hollowmoor-deep-barrow.md`'s own Rewards section also promised "old ceremonial grave-wealth," which was never narrated or given. Caught only because the player asked to double-check for missed coin/jewelry from the trip | Awarded retroactively: 140 gp coin (70 each) plus a set of ash-king ceremonial pieces, held unsold (worth more to Corvin Thale specifically than a generic buyer, per his established collector interest) |
| 2026-08-26 | `format-bug` | Granted Ilvaneth Inspiration narratively (Warden conversation) but `update_character.py` had no patch type for it — the sheet's `Inspiration` field stayed at 0, never written. Only surfaced because the player remembered and asked to spend it on an Insight check | Added `inspiration_grant`/`inspiration_spend` patch types (named-verb style, matching the gold/xp fix — no ambiguous bare `inspiration` command). Retroactively granted then spent for this use. Every future Inspiration award must go through the script now, the same as HP/gold/XP |
| 2026-08-26 | `missing-system` | `srd_lookup.py spell "Toll the Dead"` returned not-found — Xanathar's Guide content, same class of gap already logged twice for non-default subclasses/maneuvers (Necromancy, Battle Master). 3rd instance of this specific sub-pattern | Applied directly from standard published rules (well-established, unambiguous content), same handling as the prior two instances. No new fix needed — this sub-pattern's mitigation (apply + flag in `system-register.md`) is already the established response, not a fresh gap |
| 2026-08-26 | `content-gap` | No encounter file existed for Hollowmoor Barrows despite it being the party's explicit, obvious next destination going into session 4 — same shape as Harrowgate's under-build, this time caught before the session instead of mid-play. Also surfaced a real danger: the site's stated difficulty (level 6+/8+ effective for this two-player party) is far above the party's actual level 3, with the deep barrow's Wight alone exceeding the level-3 Deadly threshold | Built `info/encounters/hollowmoor-outer-mounds.md` before play: a fair Medium outer-mound fight, plus an explicit note that the deep barrow must be telegraphed and chosen, never sprung, given the math |
| 2026-08-26 | `combat-geometry` | Weeping Tower lower level: Kriv's Ready Action ("attack whatever comes through the door") was resolved the instant the door opened — attack roll, damage, the zombie's saving throw — **before initiative had been rolled for anyone**. Initiative was only set up afterward as a formality for the rest of the fight. Caught by the DM asking directly why this wasn't logged when the flat-initiative-totals bug (row 68, same fight) was fixed — that fix addressed *how* the numbers were obtained, not *when* they were established relative to the readied action | Wrote an explicit rule into `combat-rules.md`: Ready Action doesn't exempt a combatant from initiative, even when it's the trigger that opens a fight — roll initiative for everyone the instant the trigger becomes live, *then* resolve the readied action as that combatant's reaction at the correct point in the established order. Not retroactively re-run (the fight's real dice already stood once the separate flat-totals bug was fixed, and the sequencing anomaly didn't change any outcome) — fixed going forward only. Swept: the Hollowmoor and Warden fights both rolled initiative properly before any action; this was a single instance, not a repeat |
| 2026-08-26 | `npc-inconsistency` | Session 4 recap stated Sefwyn Marrow was shown "their fragments" (plural) in exchange for the Tain lead — actually only the first (ossuary) fragment was shown; Sefwyn has no knowledge of the second (convoy) fragment, per the source conversation log itself. Caused by `state.md`'s "Known to the Party" entry using an ambiguous singular "the fragment" with the party now holding two — the exact ambiguity the section exists to prevent slipped through anyway. 4th instance of this category — a pattern, not one-off | `state.md`, `campaign-clock.md`'s fired log, and `npcs/sefwyn-marrow.md` (also found stale — still read "not yet met in-fiction") all corrected to name the specific fragment. Added an explicit rule to `state.md`'s "Known to the Party" header: never write an ambiguous singular reference to an item when the party holds multiple of the same type — always name which specific instance |
| 2026-08-27 | `content-gap` | DM asked directly whether Gallowmere's locations were scaled by size the way Harrowgate's were. Checking against `dm-notes.md`'s settlement-scaling table found Gallowmere (population ~1,200, Town tier) had 9 distinct locations — well past the 6-8 minimum — but had never had an inn or a smith, 2 of the 6 named categories the Town-tier row requires (inn, market/trading post, religious site, smith, authority figure's seat, specialty shop). The 2026-08-26 sweep that checked Gallowmere against this same table had only compared total counts and marked it fine without checking the category list item by item — meaning the check itself was incomplete, not just the content. Ironic since Gallowmere is the party's own home base and most-visited settlement, and Thornwick (a smaller village with no category requirement at all) already had both an inn and a smith | Added **The Salt Lantern** (inn, Genna Ostler) and **Coalsmoke Forge** (smith, Torv Adder) to `locations/gallowmere.md`. Rewrote the settlement-scaling rule in `dm-notes.md` to require checking each named category by name, not just the total count. Swept the rest: Sallow Ford (hamlet/village tier, no category list) already has an inn and meets its own simpler count; Thornwick and Harrowgate both already have all 6 Town-tier categories present — no other settlement needed fixing this pass |
| 2026-08-27 | `hook-fragility` | Session 5 start: creating `party/session-5/` required `cd`-ing into `campaigns/ashen-crown/party` from the Bash tool. That cwd change persisted across tool calls (not just Bash) and broke every `PreToolUse`/`Stop`/`PostToolUse`/`UserPromptSubmit` hook at once — all five are wired in `.claude/settings.json` with commands like `py .claude/scripts/cinematic_trigger_hook.py`, a path relative to whatever the ambient cwd happens to be, not anchored to the project root. The very next `Write`/`Edit` call (creating the session-5 log file, bumping Session # in `state.md`) failed outright with a Python "file not found" error instead of running silently wrong — loud, not silent, but a single stray `cd` anywhere in the session (mine or a future one) would otherwise disable the rest/sync/bug-signal/cinematic/knowledge-sync enforcement with no guarantee of a loud failure every time (a hook that doesn't require sub-path resolution, or one whose failure mode is swallowed, could go silent) | Retried the blocked writes from the project root (worked). Root cause fixed structurally, not by remembering to `cd` back: all five hook commands in `.claude/settings.json` now resolve the script path via `$CLAUDE_PROJECT_DIR` (the absolute path Claude Code sets for exactly this purpose) instead of a bare relative path — e.g. `py "$CLAUDE_PROJECT_DIR/.claude/scripts/cinematic_trigger_hook.py"`. Verified live: deliberately left cwd drifted into `party/` and made another Edit call — hook fired and resolved correctly. Swept: this is a project-wide settings.json concern, not per-hook, and all five hook entries (`bug_signal_hook.py`, `session_state_gate.py`, `cinematic_trigger_hook.py`, `bug_log_reminder.py`, `knowledge_sync_reminder.py`) were the complete set — all fixed in the same pass, none missed. Residual risk noted, not hook-coverable: the many `py .claude/scripts/...` calls Claude makes directly during play (`roll.py`, `update_character.py`, `sync_state.py`, etc.) are still typed as relative paths and would fail the same way if cwd drifts again — going forward, avoid leaving the shell cwd anywhere other than the project root after a `cd`, since that's a written-discipline gap this fix doesn't cover |
| 2026-08-27 | `content-gap` | DM raised directly, as an OOC design discussion: the party is constantly running between main/side quests, and there is no option that's independent of everything else — no "just go explore a dungeon" choice. Checking confirmed this was real, not a feeling: every prepared site, including `the-drowned-mill.md` (the closest thing to a pure treasure site — its own file says "no neutral way to loot this building," Vey vs. Kell), turned out to be wired into a faction, NPC relationship, or personal arc. This is the direct, foreseeable cost of `dm-notes.md`'s own "nothing is purely side content" rule, which had no counterbalancing exception on record | Built `locations/the-widows-hollow.md` (a green hag cave system, 16 rooms including 3 secret, found via an old misfiled map at Harrowgate's Scriptorium) and `encounters/the-widows-hollow-hag.md` (a social-first boss encounter, combat available but not the point) — designed collaboratively with the DM (cave system, 15+ rooms with secrets, level 4 scaling, a non-combat-focused witch) as the first explicitly faction-free, NPC-free, arc-free site in the campaign. Added a new standing rule to `dm-notes.md` ("at least one site should owe nothing to the plot") and a session-start check (`sandbox-index.md` step 4b) so this becomes a maintained category, not a one-time fix — when this site is resolved, the next one gets built the same way |
| 2026-08-27 | `workflow-skip` | Mid-delve into The Widow's Hollow, room description was running as one broad atmospheric paragraph covering the whole site rather than room-by-room, tactically-grounded detail. DM asked directly for room-by-room description reflecting what the party's passive Perception would surface (layout, exits, visible contents/hazards) so a player could actually picture where they stood and plan a next move — and asked explicitly that the fix be permanent ("sürekli hale gelsin"), not a one-time correction | Added a new section to `.claude/skills/dm-narration-format/SKILL.md` ("Room-by-room exploration — passive Perception sets the baseline"): every new room's read-aloud must state what's visible at passive Perception automatically (shape, exits, plain-sight contents/hazards), atmosphere layered on top rather than substituted for it, hidden things still gated behind an active check as normal. Framed as the same "one beat per message" discipline already required for combat turns and travel nights in `narration.md`, now extended to dungeon rooms |
| 2026-08-27 | `combat-geometry` | The Widow's Hollow, room 12: an Ochre Jelly was described in the room (visible, hostile, blocking the only dry path), the player said "Atak," and Claude resolved a full Scorching Ray attack — 3 attack rolls, crit damage, 36 total fire damage — before ever calling `combat_status.py --init` or asking for initiative. Caught only by accident: the follow-up HP patch failed with "combatant 'Ochre Jelly' not found," not because the sequencing was noticed on its own. 5th instance of this category — the existing Ready-Action-specific rule in `combat-rules.md` (added session 4) didn't cover this because the trigger here wasn't a Ready Action at all, just an ordinary declared attack | Generalized the rule in `combat-rules.md`: initiative must exist before any attack roll against a hostile creature is resolved, not just for Ready Actions — the checkable trigger is verifying `combat_status.py --init` has run for the encounter before running or narrating an attack roll. Retroactively fixed in the same turn: asked for initiative, re-applied the already-rolled Scorching Ray as Ilvaneth's action at her actual position in the established order |
| 2026-08-27 | `format-bug` | Same fight, second issue found immediately after the first fix: `encounters/widows-hollow-ochre-pool.md` used a column header of `HP` instead of the `HP Each` the parser expects (confirmed against the two other new encounter files built the same session, both correctly using `HP Each`) — `combat_status.py --init` silently misread the table and initialized the Ochre Jelly at 8/8 HP instead of the real 45, with no error | Fixed the header to `HP Each`, re-ran `--init`. No script-level fix needed — this was a one-off authoring slip in a freshly written file, not a parser bug (the same parser reads `HP Each` correctly in every other encounter file in the campaign, old and new) |
| 2026-08-27 | `content-gap` | Mid-delve, the DM called out `locations/the-widows-hollow.md` (built earlier the same session, in direct response to different table feedback) as feeling thin — too few monsters (only 3 of 16 numbered rooms had any real content), a secret room (6) with no in-fiction way to ever find it (a bare DC15 roll nobody had a reason to make), and an overall "moving room to room" feeling rather than being inside a real place. Exactly the same failure shape already caught and fixed for settlements (Harrowgate, and later Gallowmere's category-not-count sweep) — a room/location *count* that looks sufficient on paper without checking that the individual entries actually carry content. 10th instance of `content-gap`, a well-established pattern by now | Rewrote `locations/the-widows-hollow.md` in place: every room now carries at least ambient/environmental detail (signs of what actually lives there) even where there's no fight; room 6's secret got a discoverable in-fiction clue (torn web silk leading to the hidden panel) with the blind Perception DC dropped from 15 to 13 as a fallback, not the primary route; added an explicit "what was left unexplored" note for the rooms the party's own underwater shortcut caused them to skip, so that content stays written and available rather than silently vanishing. Added a new standing rule to `dm-notes.md` ("dungeons need density, the same way settlements do") generalizing this into a checklist for every future dungeon: ambient content in every room, no secret behind a blind roll alone, real content in at least half of numbered rooms, skipped content preserved not deleted |
| 2026-08-27 | `content-gap` | Same session, immediate follow-up: the DM judged the `dm-notes.md` density patch above still wasn't enough — wanted a genuinely complete, professional-grade dungeon design standard, and wrote out a full four-phase framework directly (concept/ecology logic, non-linear "Jaquaysing" map design with loops/verticality/multiple entrances, four balanced room-content types including faction conflicts and environmental storytelling, and DM-side pacing/atmosphere management), explicitly asking for it saved as a permanent rule, in English | Wrote `.claude/rules/dungeon-design.md`, a complete restatement of the DM's four-phase framework as the project's dungeon-design standard, superseding the narrower `dm-notes.md` patch above. Referenced it in `sandbox-index.md`'s "read before running any fight" table so it's loaded the same way `two-player-scaling.md` already is. Declared explicitly that every dungeon from here forward is built to this standard from Phase 1, not retrofitted after the fact |
| 2026-08-27 | `workflow-skip` | Short rest inside Kargrim's Reach: Ilvaneth's player asked to use Arcane Recovery, and Claude refused, stating it was already used (0/1) that day. This was wrong — a long rest had genuinely occurred since the last use (day 35, en route from The Widow's Hollow, before entering this dungeon), and `rest_check.py --apply`'s own output at the time explicitly said "all features restored to max uses." Claude answered from memory of the pre-rest state instead of re-reading the character file, which actually showed `Arcane Recovery(1/1 uses)` — available the whole time. DM caught it and asked for a bug-log entry directly | Corrected immediately: confirmed the file's actual state (1/1) and let the use proceed. No script fix needed — the tooling (`rest_check.py`) behaved correctly; the miss was Claude stating a resource's availability from recollection right after a rest event, instead of re-checking the file the rest had just updated. Root cause matches the project's broader "trust the file, not memory" principle already applied elsewhere (`dm-notes.md`'s "character files are the single source of truth") — worth remembering as a standing discipline for any feature-use question asked shortly after a rest, not just spell slots/HP which are already sync-checked by script |
| 2026-08-27 | `workflow-skip` | Groth's Hall fight, Kargrim's Reach: Ilvaneth's player reported three Scorching Ray attack rolls (18, 15, 20) and, separately, 8 damage dice (matching 2d6+2d6+4d6 — i.e. two normal hits plus one crit). Claude used only the first 6 of the 8 numbers as three 2d6 rolls, missing that the third attack roll was a **natural 20** (an automatic critical hit requiring doubled damage dice per `combat-rules.md`'s own Attack Resolution table) — undercounting total damage by 10 (25 instead of the correct 35) and leaving Groth alive at 2 HP instead of correctly dead. Player caught it directly ("Yirmi var zarlarda") | Recomputed correctly: Ray1 (18) 2d6=10, Ray2 (15) 2d6=9, Ray3 (20, crit) 4d6=16, total 35 — Groth's combat state corrected from 2 HP to dead in the same turn. No script exists to catch this (attack-roll interpretation happens in narration, not a tool call) — the standing discipline is to check every reported attack roll for a natural 20 *before* asking for or totaling damage dice, every time multiple attacks are resolved together, not just single-attack turns |
| 2026-08-27 | `workflow-skip` | Same fight, immediately after: narrated a short rest at Room 2 (Kargrim's Reach) as restoring Kriv's Action Surge and Superiority Dice, but never actually called `update_character.py Kriv features_restore_all` — the character file kept showing `Action Surge(0/1)` and `Superiority Dice(1/4)` from before the rest. Surfaced when Kriv's player tried to use Action Surge in the Groth fight and the file's true state didn't match what had been narrated. This is the same root pattern as the Arcane Recovery miss two entries above — mechanical state described in prose without the tool call that actually changes the file | Corrected in place: ran `features_restore_all` (representing the rest that should have applied), then re-applied the one Superiority Die actually spent earlier in the Groth fight, landing on the correct current state (3/4 Superiority Dice, 1/1 Action Surge) before resolving the turn in progress. Reinforces the same standing discipline already named above: any rest — short or long — narrated as restoring a resource needs the matching script call in the same turn, not just prose, and this applies to Kriv's Battle Master/racial features exactly as much as it does to Ilvaneth's spell slots |
| 2026-08-27 | `workflow-skip` | Same fight, immediately after (third in a row): Kriv's player reported an attack roll and damage for a hit on Bugbear bodyguard A (11+6=17, lethal), then in the same message an Action Surge follow-up attack on Bugbear B. Claude resolved the Action Surge attack but never applied the first attack's damage to Bugbear A — its HP stayed at 8/27 (alive) in the tracker while narration moved on, only caught on the very next status check when Bugbear A was still shown alive after supposedly dying. Happened directly while Claude was mid-fix on the two bugs immediately above — a concrete instance of the general risk that handling an interruption (a bug fix) drops a step from the turn that triggered it | Applied the missed 17 damage to Bugbear A, correctly marking it dead, in the same turn once caught. No structural fix beyond the general discipline already being reinforced this session: when a bug-fix detour happens mid-turn, explicitly re-check what part of the *original* turn (not just the bug) still needs resolving before moving on — three misses in three consecutive exchanges here is itself the signal that this needs active attention, not just noting it once |
| 2026-08-27 | `format-bug` | Session 6 start: DM asked whether a list of Bag of Holding contents was kept anywhere. It wasn't a single list — the three items looted into it during Kargrim's Reach (Groth's morningstar, corroded dwarven weapons, bugbear gear) were recorded as inline notes on **Kriv's** character sheet (whoever physically looted them), while the bag itself was listed as **Ilvaneth's** item (she bought it) on a separate sheet — the same duplication-across-files shape already caught for HP/gold/XP, just for shared-container inventory instead. Not yet a drift (no disagreement found), but the exact setup that produces one the next time either sheet is edited without the other. This will recur every future dungeon haul, per Kriv's own stated reasoning (session 5) that the bag removes the "not worth carrying" calculus — a system needed going forward, not a one-off | Added a single canonical "Bag of Holding — Contents" table, consolidating all three items with provenance. Initially placed on `ilvaneth-duskmere.md` (the bag's purchaser); DM immediately clarified **Kriv is the one physically carrying it**, so moved the table to `kriv-shestendeliath.md` with a pointer from Ilvaneth's sheet instead. Going forward, bag contents are edited only on the carrying PC's sheet — Kriv's, unless the bag itself changes hands |
| 2026-08-27 | `workflow-skip` | Sefwyn Marrow's "will re-initiate contact once Tain is arranged" promise (fired first contact day 25, session 3) was written into `campaign-clock.md` explicitly as "an open thread, not a pending clock" — meaning nothing mechanically forced a check on it. It sat completely unmentioned across sessions 4, 5, and most of 6 (day 25 → day 37, 12 real in-fiction days, two full sessions) until the DM asked directly whether it had been forgotten. It had been — the "not a pending clock" framing meant `clock_check.py`'s automatic drift detection, which already catches every *other* dated clock whose threshold passed, had no way to see this one at all | Fired it immediately, in-fiction: Sefwyn intercepts the party at The Hanged Man, Harrowgate (day 37) with the Tain introduction ready. Structural fix, not a rewritten reminder: added a standing rule to `campaign-clock.md`'s "Running the clock" section — any future "NPC will re-initiate contact" promise must be written as a real pending clock (`⏳ Name — fires ~day N`, an honest estimate) so it's subject to the same automatic `clock_check.py` drift detection as every other clock, never left as bare prose in `state.md`'s Open Threads/Active Quests where nothing can catch it going stale. Swept `state.md` for other "NPC will proactively contact/find the party" promises with no attached date: found none — Corran's game already has an estimated day (~40), Roskel's summons already has a proper dated clock (~day 41-43), and Renata Kroll's offer is waiting on the party's own decision, not an NPC follow-up, so none of those share this failure shape |
| 2026-08-27 | `content-gap` | Session 6, roleplaying Sefwyn Marrow's Tain-meeting offer: set the meeting at "5 days from now" (~day 42, Millward Crossing) without cross-checking it against two other pending dated commitments already on the books — Corran's game (~day 40, Thornwick) and Roskel's summons (~day 41-43, Gallowmere). All three now cluster in the same ~3-day window, in three different, likely-distant locations, with no deliberate design intent behind the pile-up — caught only because the DM asked directly whether it was intentional | Asked the DM rather than deciding unilaterally (a player-agency call). DM chose to keep the Tain/Roskel overlap (different locations, real tension) but move Corran's game clear of both. **Escalated past a written reminder** per `bug-protocol.md`'s ladder — a prose note alone has already failed repeatedly in this project — and extended `clock_check.py` itself: it now also flags any two pending clocks whose `fires ~day N` windows land within 3 days of each other, the same way it already flags an overdue clock. Added a `(cluster ok)` heading annotation so a deliberately-accepted overlap (Tain/Roskel) doesn't re-flag forever once decided. Live-tested with a synthetic campaign folder before wiring in: caught a real cluster, respected the exemption, ignored a distant clock — all three cases correct. Also swept and fixed the trigger for this bug: Corran's game (~day 40) was itself prose-only in `state.md`, never a real pending clock, so it couldn't have been caught by `clock_check.py` even if the tool had existed — converted it to a proper `⏳` entry along with the new Tain meeting, closing the same gap already fixed for Sefwyn's thread this session |
| 2026-08-27 | `workflow-skip` | Millward Crossing convoy ambush: asked for Kriv's damage roll against a Skeleton as plain `1d12+6`, omitting the Barrow-King's Blade's own stated **+1d8 radiant vs. undead** bonus (right there on his character sheet's Attacks table) — Skeletons are SRD undead. Caught by the DM asking directly rather than by re-checking the sheet before asking for the roll. Same root pattern already logged elsewhere this project (`bug-log.md`'s Kargrim's Reach entries): stating a mechanic from memory/assumption instead of the character file in front of me | No damage applied yet this turn, so nothing to retroactively fix — corrected before the roll was resolved. Standing discipline, not a new file: before asking for *any* PC's damage roll, check that PC's Attacks table for conditional bonuses (vs. undead, vs. a type, on a crit, etc.) against the actual target type, not from memory of what the weapon "usually" does. Applies for the rest of this fight (all 6 skeletons) and any future undead encounter |
| 2026-08-27 | `format-bug` | Millward Crossing convoy ambush, round 1→2 transition: the combat status display stayed frozen on "Round 1" even after every combatant had acted once and the turn order cycled back to Kriv. Root cause in `combat_status.py`'s `advance_turn`: the round counter only incremented `if idx == 0` — but Skeleton A, the combatant originally at raw array index 0, died in round 1, and dead enemies are skipped via `continue` in the turn-search loop *before* that check ever runs. Once the index-0 slot was dead, the chosen `idx` could never equal exactly 0 again for the rest of the fight, so the round counter froze permanently. This is the first fight to trip it because it's the first time the *highest-initiative* combatant (who typically lands at raw index 0) died before the turn order first wrapped | Manually corrected the live state (round set to 2, surprised conditions cleared on the 4 combatants whose one surprised turn had already passed) so play wasn't blocked. Structural fix: rewrote the wrap check to compare the newly chosen `idx` against the *previous* `active_index` (`idx <= old_idx` means the search wrapped past the end of the turn order) instead of hardcoding `idx == 0` — this works regardless of which raw slot is dead. Live-tested with a synthetic combat state (index-0 combatant dead, active elsewheere) before and after the fix: reproduced the freeze on the old code, confirmed the round correctly advances on the new code |
| 2026-08-27 | `workflow-skip` | **Dice ownership violated twice in the same session, the exact failure `roll.py --owner` was built to stop.** Both times: Ilvaneth's Burning Hands damage (an AoE spell hitting multiple targets) was rolled by Claude via `roll.py --owner dm-table` instead of asking the player — once at Skree's Warren (3d6), once at the Millward Crossing convoy ambush (4d6, upcast). `--owner` correctly *requires* a label on every plain roll, but it cannot stop the label itself being wrong: "dm-table" is meant for travel/random tables and DM-side environmental effects, not for a PC's own spell damage just because the spell happens to hit several enemies at once. Caught only because the DM called it out directly, having noticed the pattern repeating. This is the same root failure already logged and "fixed" multiple times (Thunderwave, session 2; Burning Hands, session 4) — the mechanism changed but the underlying judgment call (whose roll is this, really) is still made by Claude at the moment of the call, and AoE spells apparently make that call easier to get wrong, not harder, because resolving one roll against several enemies *feels* like a DM-side table effect | **Convoy ambush instance (fresh, not yet narratively locked in)**: reverted — asking the actual player to re-roll the 4d6 damage for real, then re-applying results to Priest A/B and the two skeletons. **Skree's Warren instance — decided 2026-08-29**: DM accepted the outcome as-is, explicitly scoped to this one fight only ("I'm only accepting it that way for this one fight") — not a general precedent for future violations. No retroactive re-roll; the loot/story consequences already built on it stand. Per `bug-protocol.md`'s escalation ladder: this is a pure judgment call (the script cannot know whether "dm-table" is an honest label), so a third rewritten prose rule alone won't hold — added an explicit, named trap warning to `combat-rules.md`'s dice-ownership section: **hitting multiple targets with one PC spell does not change whose roll the damage is** — still ask, every time, no exceptions for AoE |
| 2026-08-27 | `format-bug` | `cinematic_trigger_hook.py` false-fired on `**Current Location**: On the Karsgate road, ... 2 of 4-5 nights from Millward Crossing` — the party is still days from Karsgate itself, just naming the road that shares its name, but the hook's pre-context `APPROACH_WORDS` list (checking text *before* the matched settlement name) had no entry covering "on the X road," only phrases like "near," "toward," "en route to." "On the " alone is too generic to add as a blanket exemption — it also precedes genuine arrivals ("On the docks of Karsgate"). **First-pass fix incomplete**: fixed only `cinematic_trigger_hook.py` and moved on without sweeping for the same logic elsewhere — the very next turn, `location_check.py`'s `Stop` hook flagged the identical false positive, because it's a near-duplicate script with the exact same `APPROACH_WORDS` list and the same missing check, copy-pasted rather than shared | Added the same `ROUTE_WORDS` list (road, route, highway, path, yolu, yolunda) checked in the 15 characters *after* the matched name to **both** `cinematic_trigger_hook.py` and `location_check.py` this time. Live-tested both the exact false-positive case (now suppressed) and a genuine arrival case (still fires) in each file before considering it done. The two scripts share matching logic by duplication, not by importing a common module — a further sweep note for future maintenance: any fix to one needs to be checked against the other explicitly, since nothing currently keeps them in sync automatically |
| 2026-08-27 | `content-gap` | Stated the Harrowgate → Millward Crossing journey as "4-5 days" (a real range, matching the DM's own "we're leaving immediately, we're barely making it" framing for a day-42 meeting departing day 37), then silently rolled travel encounters for only 4 nights without flagging the choice — landing the party a full 1.5 days early, directly undercutting the tight-timing tension the table had just established. Caught by the DM asking why there was buffer time at all | Not silently patched — the DM's framing (rushed departure, cutting it close) is the correct read of a "4-5 day" estimate for a day-42 deadline from day 37, so the 5th night needs to actually happen, arriving day 42 itself. Rather than retcon nights already played, the fix is to add night 5 now before the "arrival" is treated as final, since nothing consequential had happened at Millward Crossing yet beyond the cinematic. Standing discipline: when a stated range ("4-5 days") is chosen from without comment, default to the value consistent with whatever narrative framing was already established (urgency implies the higher end), not silently to whichever number is convenient to roll |
| 2026-08-27 | `npc-inconsistency` | Orell Tain's meeting (session 6, Millward Crossing): when Kriv asked for help tracing who paid the Ironclad Compact to destroy House Shestendeliath, Tain was played offering to investigate "for now, expecting nothing in return... a way to show good faith" — pure altruism, with no basis in his character file. `npcs/orell-tain.md`'s own "What he actually wants" section frames him as a scholar losing an internal political argument who needs allies and leverage, not someone who does favors on spec. The DM flagged this directly: NPCs must act on their own self-interest, and the deeper problem is structural — NPC files record a static "what they want" bio but have no mechanism for an NPC's stance to visibly evolve as they learn things in play, so nothing forces a check of a new offer against established motive before it gets narrated | Rewrote the scene: Tain now asks for something concrete and immediate (examining a fragment in person) in exchange for pursuing the Shestendeliath lead — matching the same fragment-for-information currency already established with Sefwyn Marrow, not an invented act of charity. Structural fix, not just this one line: added a "Current knowledge & stance" section to `orell-tain.md` (a living record of what he's learned and where he stands, distinct from his static bio, updated as the meeting progresses) as the template, and added a standing rule (see `dm-notes.md`) that any NPC dialogue offer must be checked against that NPC's own stated self-interest before being narrated — no NPC acts against or outside their own motive without an established reason. **Not yet swept to other NPC files** — retrofitting a "current knowledge & stance" section onto every existing NPC file is a real follow-up task, flagged rather than done in the middle of this scene; the standing rule change applies immediately going forward, and existing files should get the section added at their next actual appearance in play, not blanket right now |
| 2026-08-27 | `content-gap` | Session 6, rolling Thornlands travel encounters for the Kargrim's Reach → Harrowgate leg: a d10 roll of 7 hit "Sorrel's Hollow deserters collecting 'tolls'" — but that entire deserter camp was permanently wiped out by the party in session 2 (`state.md`'s "Clear Sorrel's Hollow for Hesta" quest, Complete, all ~12 members confirmed dead). The table entry was never updated after that thread closed, so it could still roll dead content referencing an eliminated named group | Reflavored the roll in play: a new toll gang has moved into the same road-stop, a direct consequence of the vacuum the party's own actions created — not a continuity error, a hook. Fixed the table entry itself in `travel-encounters.md` so future rolls of the same result don't repeat the mistake. Swept the other 4 regional tables for the same shape of problem (a table entry naming a specific group/individual the party has since permanently eliminated): none found — every other named-NPC entry (Sefwyn Marrow, the Frostmere dragon) explicitly allows for substitution or is still-live, and every monster-type entry (harpies, hell hounds, wights) is a generic species reference, not a specific already-eliminated group, so no fix needed there |
| 2026-08-27 | `format-bug` | Session 6, initializing the basilisk fight (Kargrim's Reach, room 20): `combat_status.py --init` failed with "no enemies found." `kargrims-reach-basilisk.md` used `## Enemy` (singular — doesn't match the parser's `## Enemies` substring check) and a bare `HP` column instead of `HP Each` — the exact same two failure shapes already caught once individually for `widows-hollow-ochre-pool.md` (session 5, `HP` vs `HP Each` only). This is the **3rd instance of this specific sub-pattern** (encounter-file header not matching the parser's expected format) — a pattern per the frequency table, not one-off | Fixed the file (`## Enemies`, `Name/Count/HP Each/AC/Speed/Initiative`). Per `bug-protocol.md` step 3, swept **all 18** encounter files in `info/encounters/` for both the section-header and column-header shape — every other file already matches the correct format; this was the only offender. No structural parser fix made (the working format is already the overwhelming norm, 17/18 files); the standing mitigation remains the same as the Ochre Jelly fix — a new encounter file should be diffed against a working one's headers before its first use, not assumed correct |
