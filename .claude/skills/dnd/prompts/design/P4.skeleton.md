---
phase: P4
role: skeleton
kind: powers
effort: high
critics: 2
schema: skeleton
---
{{common}}

## Your task — the powers skeleton (Phase 4, single writer)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. {{scale_line}}. Attempt {{attempt}}; directions:
{{directions}}

Read: `design/premise.md`, `design/dm-only/premise-secret.md`, `design/cosmology.md`, `design/dm-only/cosmology.md`, every region and settlement file, `design/map.json`, `design/naming.json`, `design/entities.json`, and `design/dm-only/dice-log.json` for the secret antagonist rolls. You decide the whole board; the faction agents write dossiers inside your decisions.

### Rolls of this phase
{{phase_rolls}}

`faction.<n>.*` by ordinal → your `assignments`. Secret rolls: `bbeg_visibility`, `bbeg_shape`, `bbeg_origin`, `bbeg_faction_archetype`, `front_template`, `doom_shape`, `lieutenant.<n>.role`, `regional_count` (in the dm-only dice log).

### What to decide and write
1. **The roster of factions**: ids and English names (institution patterns), archetypes as rolled (the P3 stubs by archetype are these), each with its rolled fracture, endgame, secret kind, cult doctrine where cult, rung and link rolls; the signature institution is one of them. Assign each a naming language.
2. **The stance matrix**: every pair −3..+3 with **one reason line per edge** that both dossiers will carry verbatim (risk 24.1 #19); the party at 0 unless a socket says otherwise. Large stance pairs (a −3 and a +3) exist where the content mix's hooks demand.
3. **The antagonist hierarchy** (all in the dm-only skeleton): the BBEG as an `npc_` stub with an **opaque slug** (`npc_s01`-style) and secrecy `secret`, its faction (the rolled archetype), visibility pattern, shape, origin pinned to a dated event, the villain's answer; the lieutenants as `npc_` stubs with their rolled roles (major tier, faction leaders); regional villains as supporting stubs tied to sites P6 will detail. Reserve `goals.py` records for the BBEG and lieutenants in `seeds[]` (`goals add --kind npc` with metric and the three responses).
4. **The BBEG front on a hard calendar**: doom day = `antagonists.yaml rules.doom_day`; the rolled front template's portents as operation steps of the BBEG's faction with due days, kinds, defenders and the party-only telegraph; the rolled doom shape with its buy-time levers each an entity; the threat stage 1 in the overlay.
5. **Every faction's `factions.json` row** in `seeds[]` (`factions add`, `stance`, `op`): objective, power, intel, assets (existing ids: settlements, places, sites, NPCs), HQ, leader and heir stubs (roster NPCs of P5), controller `world`, machine doctrine (`aggression`, `target_preference`, `move_weights` per trigger from the trigger templates adjusted by archetype), a first operation with ≥2 dated steps of a `step_kind`, an `abandon_if` predicate, an optional metric.
6. **Fragments** under `design/_staging/P4/`: one per faction stub (with the stance reasons it must carry), one per antagonist stub, `skeleton.json` with `roster` (factions plus the calculus document id `calculus_<slug>`), `assignments`, `reads` per entity, and the `seeds[]` above.

The faction agents that follow read your fragment for their faction, the stance reasons, and the files you list.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
