---
phase: P1
role: writer
kind: premise
effort: high
critics: 2
schema: writer
---
{{common}}

## Your task — the premise (Phase 1, single writer)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Wishes: {{wishes}}. {{scale_line}}. Attempt {{attempt}}; directions from earlier rounds:
{{directions}}

You write the premise of this campaign: the one question it tries to answer, the big secret, the three signatures, the trope break(s), the naming languages, the two pitches. Everything downstream derives from what you decide here, so decide specifically and commit.

### Rolls you must honour
Public rolls of this phase (each names a row in `data/design/`; read the row and its `hooks`):
{{phase_rolls}}

Secret rolls (read `design/dm-only/dice-log.json` yourself; never repeat their ids or text outside dm-only): `secret_archetype`, `secret_twist`, `secret_trail`.

### What to write
1. **`{{prose_path}}`** from `{{template}}`: the question (from the tension row: the villain embodies one pole, the world's default is the other, the finale turns on the party's answer), the three signatures each named and mutated along its row's `mutations` so the seed is not recognisable, the trope break(s) stated as a native would state them, the naming languages (see 3), the **player pitch** (three sentences: the question, the signatures, the breaks — what the player will experience as different; no secret), and the spoiler-safe abstract line the card will show (archetype class only).
2. **`{{mirror_path}}`**: the big secret in one sentence combining the secret archetype and twist, pinned to a god (to be created in P2: name it now, P2 keeps it) and to an event; the villain's answer; the three clues by act as the trail row shapes them (`{n, act, layer, placed_in kind, how}`); the DM pitch. `mechanic`: if the roll says yes, sketch the signature mechanic (a track with thresholds, gains and costs) here and name it in the public file.
3. **`design/naming.json`** from the rolled families: for each, a mutated copy of the family row (one bank changed, one cluster forbidden, 12-20 English roots chosen from `naming.yaml`'s lexicon by the family's `best_for`), `tr_suffix_friendly: true`, a `label_tr`, 6-8 sample names per mode that you generate now and that pass the blacklist (`naming.yaml` `blacklist.*`) and `.name_registry.json`; `assignments` left empty for P3/P4 to fill; `banned` = the exact names `.name_registry.json` already holds.
4. **Registry rows** in `{{fragment_path}}` for: `premise_<slug>` (fields per `docs/schemas/entities.md`, the secret fields under `dm_only`), one `signature_` per signature (`kind`, `rule_tr`, `dm_only.true_rule_tr`), one `break_` per break (`row`), and the `tensions[]` ids. Stamp `question_tr`, `signatures`, `trope_breaks`, and under `dm_only.stamped_fields` the `secret_archetype`.

### Bars you must clear (the critic will ask)
- Three sentences that could only be true in this world — write them explicitly under the pitch.
- No forbidden default from `forbidden.yaml` unless it is a conscious break named in the DM pitch.
- Every signature will appear in at least three other entities: name in the notes file where you expect them.

Notes and doubts go to `{{notes_path}}`. Return, as your label `{{agent_label}}`:
```json
{{schema}}
```
