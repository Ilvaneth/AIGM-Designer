---
phase: P7
role: writer
kind: chapter
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — one chapter

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Entity **{{entity_id}}** ({{entity_name}}: {{entity_summary}}). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

The skeleton fixed act, level band, intended-path sites, key NPCs, the node ids and kinds, and the beats that land in this chapter. Write `{{prose_path}}` from `{{template}}`:

- Title, act, locations, level band, drive (why the party is here at all), intended-path sites with their tiers, key NPCs with what each wants this chapter.
- **The nodes** (3-5, hub-and-spoke, entered in any order): for each, what is here, what is at stake, the ways in (≥2), what happens if the party never arrives (a faction step by id or a news record shape), the sites and NPCs it uses. Each node kind's row in `arc.yaml#node` says what it must contain.
- The beats that can land here with their delivery paths in this chapter's places.
- The act's A/B scenario as it touches this chapter; the content-mix check (site / social / exploration ratio against the dial); the XP share.
- No `## Secret` section unless a clue or the BBEG's hand sits here (then to the dm-only mirror at the same path).

Fragment `{{fragment_path}}`: the chapter row (stamps `act`, `level_band`; `order`, `nodes[]`, `intended_sites[]`, `xp_share`, `content_mix`), the node rows updated, `refs`, `counts` (`nodes`, `sites`, `npcs`). The chapter file's front matter lists under `covers:` every `node_` and `item_` row whose `file` is this chapter (the validator's `file_unclaimed`; dry run 1 missed five).

The critic asks how the party can trigger each beat by three routes and what the world does if the party never enters this act.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
