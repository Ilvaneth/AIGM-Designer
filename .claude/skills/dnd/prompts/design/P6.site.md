---
phase: P6
role: writer
kind: site
effort: high
critics: 1
schema: writer
detailed: true
---
{{common}}

## Your task — one site, detailed at birth

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Entity **{{entity_id}}** ({{entity_name}}: {{entity_summary}}). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

The skeleton stamped tier, room count, act, thread and key NPCs, and wrote the skeleton file; you turn it into the four-phase detailed file from `{{template}}` without touching a stamp. Read `data/design/sites.yaml` (the type row, the hazard row of the tier, the boss checklist, the room categories), `loot-budget.yaml` (the tier row, the filler ban), `monster-ecology.yaml` rows whose habitats match the region and whose CR sits in the tier, and `srd-index-2014.json` for the blocks, items and hazards.

### Write `{{prose_path}}` (secret layer to `{{mirror_path}}`)
1. **Concept & ecology**: who is here and why, from the ecology rows; the occupants' own plan; the reskin notes with the CR estimator applied where HP, AC or damage moved more than 25%.
2. **Map**: ≥2 entrances, verticality, a loop that leads back to an exit and never to the payoff; the stamped **minimum depth** (≥ 60% of the rooms) from any entrance to the payoff room.
3. **Room-by-room table** in the exact template format: `| # | Room | Category | Content | Exits | XP |`, `[Entrance]` and `[Payoff]` markers in the Exits column, exactly the stamped room count, XP by raw CR sums without the group multiplier, hazards from the tier's row with DC and damage, loot inside the tier row with SRD ids (nothing from the filler ban), sanctuary rooms marked.
4. **Content variety check**: the category shares against the bands (minor sites: ±20).
5. **Running this well**, **Connections** (wiki-links to the thread, the NPCs, the region, the seeds), the three telegraphs, escape geometry, attitude, rest pressure from the danger dial's row.
6. **The boss** (standard role and up): every row of the boss checklist answered with a number or a room; the signature weapon and defensive item as real loot; the reference party is four PCs at the tier's intended level, never the actual party; run `burst_check.py` if the site has a boss and paste its output.
7. Secret (mirror): the true occupant if it differs, the clue if one sits here, what the DM protects.

### Fragment `{{fragment_path}}`
The site row (unchanged stamps; `xp_budget`, `min_depth`, `telegraphs`, `escape_tr`, `attitude`, `payoff`, `if_never_visited_tr`, `reoccupation`), `overlay: {"status": "detailed"}`, `refs`, `seeds[]` with `site_progress open` (entrances, payoff room, min depth, room count) and any `item_` or `creature_` rows the rooms created, `counts` (`rooms`, `telegraphs`, `words`).

The critic asks which thread ties this site to the game, what it has done in 60 days if nobody comes, how a party knows the danger before stepping in and where a party three levels early runs, and for three things that could exist only here.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
