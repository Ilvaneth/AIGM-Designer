---
phase: P3
role: writer
kind: settlement
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — a batch of villages (up to five)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Batch entity **{{entity_id}}** stands for the villages the skeleton listed under it in `design/_staging/P3/skeleton.json`. Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

A village is light (item 6.4): one sentence of place, one problem, one NPC. For each village in the batch write `design/settlements/<id>.md` from `{{template}}` with only the Public section filled (no districts, no anchors beyond the inn or the shrine, no small-point table), the problem as a `seed_` stub, the one NPC stub with a role, and the region's travel table as its road. A village that is a village of the signature people, a treaty people or a foreign quarter's origin says so in its one sentence.

Fragments: one per village under `design/_staging/P3/` (the settlement row with stamps `scale`, `polity`, `ruler_at_birth` = the one NPC; it replaces the stub the skeleton reserved, `status: pending` dropped), plus the batch **container** `{{fragment_path}}`: `id` the batch id, `registry: null`, `members[]` the village ids, `counts.villages` the number written. Write every village the skeleton listed under the batch: the count is exact.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
