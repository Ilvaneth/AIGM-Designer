---
phase: P7
role: writer
kind: seed
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — a batch of quest seeds (up to six)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Batch **{{entity_id}}** stands for the seed stubs the skeleton listed under it in `design/_staging/P7/skeleton.json`. Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Rolls of the batch's seeds
{{rolls}}

Each seed is a `seed_` entity: **hook** (how the party hears of it), **complication** (what makes it not simple), **resolution** (the concrete written outcome: none stays abstract), **reward** (from the loot table's tier or a service, an access link, a stance), tied to ≥1 NPC or faction, about half tied to a site. The seed shape's row in `arc.yaml#seed_shape` says what its kind must include. A seed that touches a settlement's local problem replaces that stub rather than duplicating it. Write them into `design/arc.md`'s seed bank section (append under the heading the skeleton left) in the narration language, English names only.

Fragments: one per seed (`hook_tr`, `complication_tr`, `resolution_tr`, `reward_tr`, `tied_to[]`, `site`), plus the batch **container** `{{fragment_path}}` (`registry: null`, `members[]` the seed ids, `counts.seeds`).

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
