---
phase: P5
role: skeleton
kind: people
effort: high
critics: 1
schema: skeleton
---
{{common}}

## Your task — the people skeleton (Phase 5, single writer)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. {{scale_line}}. Attempt {{attempt}}; directions:
{{directions}}

Read: `design/entities.json` (every stub P3 and P4 reserved: rulers, anchor owners, village NPCs, faction leaders, heirs, betrayal candidates, antagonists), `design/naming.json`, `design/cosmology.md` (the witness lists), every faction file's Structure & People section, `design/dm-only/entities.json` only for the antagonist stubs' opaque ids, and `data/design/npcs.yaml`.

### Rolls of this phase
{{phase_rolls}}

A `_count` line is exact (`= **N**`): produce N and assign every ordinal roll; an unused ordinal, a member left out of a batch, or an invented extra fails the skeleton critic that reads you next.

`npc.<n>.*` by ordinal: four axis positions (d5 → −2..+2), a secret kind, a speech tic, a species draw (d100) and a gender draw (d100). You map each ordinal to an NPC id (`assignments`); the species draw is read against the NPC's culture's demographic row in `npcs.yaml` (the cumulative shares), the gender draw against the row's gender rule.

### What to decide and write
1. **The roster**: fill every stub with a role from `npcs.yaml#role` under the quota and the stacking rules (at most two roles per person; ruler = state leader; lieutenant = faction leader; heir, betrayal candidate and anchor owner minor or stub); add ordinary people, witnesses, specialists and the free radicals up to the scale's count; three tiers by the scale's `npc_tiers`; sockets are drawn from this roster in P7, never added.
2. **Per NPC**: id, English name (phonetic mode of the culture's language; no first-name collision; the blacklist and `.name_registry.json` checked), species and gender from the draws, tier, faction, role, the four axis positions, the secret kind, the tic (no seed more than twice), the stat anchor from `npcs.yaml#stat_anchor` (class NPCs get a level), a fixed CR when combat-capable, location at birth, `heir` / `heir_of`, and the **relationship web**: every NPC ≥2 edges (`knows / owes / hates / fears / allied / controls / commands / heir_of / loves`) with one reason line per edge that both dossiers carry.
3. **Witness lists**: which NPCs witnessed which events (from the cosmology's living memory) and what Known Fact each holds, by the divergence row's tier.
4. **Fragments** under `design/_staging/P5/`: one stub fragment per NPC (the registry row with everything above and `file: null`), `skeleton.json` with `roster` (major and supporting NPC ids one each, minor NPCs grouped in batches of six as `npcbatch_<n>` ids that name their members), `assignments`, `reads` per entity, and `seeds[]` for `graph` (every node and edge) and `channels` (the standing private channels: a faction's internal correspondence, the court's chamber).

The validator flags a roster with more than 65% one species or one gender; check it before you return.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
