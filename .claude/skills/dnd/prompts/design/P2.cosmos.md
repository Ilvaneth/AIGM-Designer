---
phase: P2
role: writer
kind: cosmology
effort: high
critics: 1
schema: writer
---
{{common}}

## Your task — the cosmos (Phase 2, single writer)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. {{scale_line}}. Attempt {{attempt}}; directions:
{{directions}}

Read first: `design/premise.md`, `design/dm-only/premise-secret.md`, `design/naming.json`, and `design/entities.json` for the ids P1 created. Everything here serves the premise: the big secret usually hides in this layer and the trope breaks usually take concrete form here.

### Rolls you must honour
{{phase_rolls}}

Read the rows and their `hooks` in `pantheon.yaml`, `planes.yaml`, `magic.yaml`, `history.yaml`, `calendar.yaml`. A `forced` roll carries the reason (a break's override, the wild-magic gate).

### What to write, in `{{prose_path}}` from `{{template}}` (secret layer to `{{mirror_path}}`)
1. **Pantheon.** The rolled type and presence. Gods by the scale count, each a `god_` entity with name (phonetic mode of its culture's language plus an epithet from `naming.yaml patterns.god_epithet`), `rank`, 1-2 SRD domains (cover the domains the scale requires), alignment, symbol, as-worshipped, disposition, `church` (a faction to be created in P4 or a named institution; a separate church faction only in epic), relations forming one connected web (the rolled relationship patterns), a secret with a tier (the `god_secret` rows). Name in the mirror which god embodies the villain's answer and which the world's default. No real-world mythology name, ever.
2. **Planes.** For every baseline row of `planes.yaml`: unchanged / renamed / merged / removed / is-this-world / touched; the touched planes (scale count plus the modifiers) each a `plane_` entity with way in, cost, a site id reserved for P6, a time rate from the `time_rate` rows, a keeper. The primer rule line about plane-referencing spells.
3. **Magic.** Availability from the dial; the rolled source, constraint, visibility, taboos, regulator (name the faction P4 will create); the signature phenomenon defined as a rule with a cost; wild magic as rolled.
4. **History.** The ages (rolled templates, the last is the present named for what it fears), the dated events with the rolled types, each an `event_` entity with `taught_tr` and, where the divergence row says so, `dm_only.happened_tr`; the deep-past events; the villain's origin and the big secret pinned to the timeline; a witness list per event within living memory (roles now, NPC ids filled by P5).
5. **Calendar.** Months from `naming.yaml patterns.month` (all 30 days), day names, per-month sensory tone from the climate row's season templates, moon(s), festivals from the rolled types (at least one per greater god), the start date from the anchor row; end with the exact `calendar.py init` line.

### Fragment
`{{fragment_path}}` is a **container** (`id: doc_cosmology`, `registry: null`): every `god_`, `plane_`, `era_`, `event_` row goes under `rows[]` (fields per `docs/schemas/entities.md`; secret fields under `dm_only`), `prose` and `dm_only_prose` name the two files, plus `seeds[]` for `calendar` (`init`, the festivals) so `design_seed.py` initialises the calendar. The gods and events P1 reserved are stubs (`status: pending`): your rows replace them — same id, `status` dropped, `rank`, `domains`, `day` and the secret stamps set by you now. Do not write separate per-entity fragments beside the container.

The critic asks: which god embodies the villain's answer and which the world's, whether the taught and true layers differ where the divergence rows say, and whether a player could tell the month from its sensory line alone.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
