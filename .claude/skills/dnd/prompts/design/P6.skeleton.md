---
phase: P6
role: skeleton
kind: sites
effort: high
critics: 1
schema: skeleton
---
{{common}}

## Your task — the site directory (Phase 6, single writer)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. {{scale_line}}. Attempt {{attempt}}; directions:
{{directions}}

Read: `design/premise.md`, `design/dm-only/premise-secret.md` (the clue trail), `design/map.json` and every region file (the site ids and tiers P3 reserved, the over-tier Act 1 sites), every faction file (HQs, assets, operations), `design/entities.json` for the NPCs, `design/design.json` (`arc_skeleton`: acts and level bands), `data/design/sites.yaml`, `loot-budget.yaml`, `monster-ecology.yaml`, and `srd-index-2014.json` for candidate lists.

### Rolls of this phase
{{phase_rolls}}

A `_count` line is exact (`= **N**`): produce N and assign every ordinal roll; an unused ordinal, a member left out of a batch, or an invented extra fails the skeleton critic that reads you next.

`site.<n>.*` by ordinal: role (forced from the scale), type, three telegraph shapes, escape, attitude, payoff, never-visited, a room count inside the role's band, a hoard flavour. Map each ordinal to an id (`assignments`).

### What to decide and write — `design/sites.md` and one skeleton file per site
- Every site of the scale's count by role, with id and English name, region, type (the region's site-type bias and the tone's weights guide which rolled type sits where), **stamped** danger tier (the region's, or above it for the over-tier sites the danger dial demands), **stamped** room count (intended-path sites: the XP formula in `sites.yaml rules`; off-path: the rolled count), **stamped** act, **stamped** thread (a faction, NPC, seed or beat id: no site exists to fill a count), key NPCs (stamped), payoff, the three telegraphs made concrete from the rolled shapes, escape geometry, attitude, occupants from `monster-ecology.yaml` by habitat and tier (candidate lists from the index, then your choice and reskin), what happens if never visited (a dated step in a faction's operation), reoccupation candidate, intended-path flag, XP budget, `status: skeleton`.
- The big secret's three clues placed in three sites or NPCs in act order (`dm_only.clue` on the site rows) **and written back into the premise row**: re-emit `premise_<slug>` as a fragment with `dm_only.clues[].placed_in` set to those ids (nothing stamped changes; the validator's `clue_unplaced` fires at P8 otherwise — dry run 1).
- The touched planes' sites and every faction HQ that is a site.
- Which sites are **detailed at birth**: the first 2 / 3 / 3-4 on Act 1's intended path; their ids go in the roster for the site writers; every other site is a skeleton you write yourself here from `templates/design/site-skeleton.md`.
- `map.json`: fill the reserved site nodes and add edges for new sites; `site_progress` seeds for the detailed ones are left to their writers.

Fragments under `design/_staging/P6/`: one per site (the row with all stamps), `skeleton.json` with `roster` (the birth-detailed sites), `assignments`, `reads` per site, `graph` nodes and edges, `seeds[]` for `graph`.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
