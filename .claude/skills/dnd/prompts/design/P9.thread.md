---
phase: P9
role: writer
kind: thread
effort: high
critics: 1
schema: writer
---
{{common}}

## Your task — one player character's thread (`design integrate`)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Thread **{{entity_id}}** for its PC. Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files (the PC's sheet, the sockets, the arc, the factions and NPCs the sockets point at, `data/design/threads.yaml`):
{{files}}

### Your rolls
{{rolls}}

Secret rolls for this PC (`pc.<n>.truth`, `pc.<n>.antagonist`) are in `design/dm-only/dice-log.json`; read them there.

### Write `{{prose_path}}` from `{{template}}` (secret layer to `{{mirror_path}}`)
- **Personal thematic question**: the campaign's question from this character's angle, distinct from the other PC's.
- **The truth** (mirror): the rolled truth kind made concrete, pinned to a timeline event and a faction or NPC, filling one socket.
- **Three layers** (mirror; act, tier, placed in a real site or NPC): layer 1 in Act 1 (discoverable), layer 2 at the Act 1 climax or in Act 2, layer 3 in Act 2-3 (secret).
- **Personal antagonist**: the rolled binding, an NPC in the hierarchy (a lieutenant, the BBEG's agent, a regional villain, a rival in the PC's faction, the institution), never a sideshow.
- **Own sites**: ≥1 per act on or near the intended path (from the skeleton pool, or new within scale as `site_` skeletons).
- **Mission** (public): the rolled verb with a concrete object; a `goals.py` record of kind `pc` in `seeds[]` with metric and thresholds.
- **Crossing points** with the other thread, one per act (skipped for a solo party); the **engine**: one faction whose target list names the PC and two NPCs with a reason to seek the PC in Act 1.
- **Personal mechanical track** only if P1 rolled the signature mechanic (stages from `threads.yaml#track_stage`, all PCs' tracks in one file `design/tracks.md`).
- The sockets bound or marked unused.

Fragment `{{fragment_path}}`: the thread row (`pc`, `question_tr`, `antagonist`, `layers`, `sites[]`, `crossings[]`, `mission`; `dm_only.truth_tr`), the `pc_` row's `thread` field, updated `socket_` rows (`bound_to`), any new `site_` rows, `seeds[]` for `goals`, `counts` (`sites`, `npcs`, `layers`).

The critic asks whether the mission can be played independently of the main road, why the other PC would want to be at this thread's finale, and which faction and which two NPCs will come looking for this PC in Act 1.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
