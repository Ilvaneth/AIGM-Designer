---
phase: P3
role: writer
kind: region
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — one region and its travel table

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Entity **{{entity_id}}** ({{entity_name}}: {{entity_summary}}). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

Read the rows: the biome (its `texture_seeds`, `environmental_seeds`, `discovery_seeds`, `landmark_seeds`, `ecology_tags`, `terrain_tr`), the identity seed (its `sense` and hooks), the landmark kinds, the six social seeds and the six faction-presence seeds. The skeleton fixed the tier, polity, hub and landmark ids; you may not change them.

### Write `{{prose_path}}` from `{{template}}`
- The identity line: one sentence leaning on the seed's sense that could describe no other region.
- Biome, climate, polity, tier, factions present, settlements, the 2-4 landmarks with one line each, the sites the skeleton placed here with their tiers, and what everyone says about the dangerous places (the over-tier sites' public telegraph lines).
- **The travel table** in the exact Ashen Crown format the template shows: the d12 category table (4 / 1 / 1 / 1 / 3 / 2; shift one point as `regions.yaml rules.travel_table.road_shifts` says), six Early-tier d6 subtables and the Late-tier Combat and Faction/Political. Quiet/Texture, Environmental and Discovery from the biome's seeds made specific to this region; Social from the six social seeds bound to roster stubs or minor stubs by id; Faction/Political from the six presence seeds naming factions and, where the board exists, operation step ids; **Combat** only from `data/design/monster-ecology.yaml` rows whose `habitats` include this biome's ecology tags and whose CR sits in the region's tier (Late tier: the tier's top or one above), each with a reason to be here and the SRD block in backticks. At least three `[KANCA]` in Discovery and Faction/Political, one in Combat. Every subtable has one result that could happen only here.
- Discoverable: safe passages, the real reason behind a landmark, who actually holds the road. Secret (to `{{mirror_path}}`): a clue placement if the trail put one here, an over-tier site's true occupant, a hidden asset.

### Fragment `{{fragment_path}}`
The registry row (`danger_tier`, `biome`, `climate`, `polity`, `identity_tr`, `landmarks[]`, `travel_table` = the prose path; stamps `danger_tier`, `polity`), `refs` from your wiki-links, `graph` edges for `located_in` relations, `counts` (`travel_rows`, `hooks`, `unique_results`).

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
