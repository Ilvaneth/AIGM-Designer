---
phase: P4
role: writer
kind: faction
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — one faction's dossier

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Entity **{{entity_id}}** ({{entity_name}}: {{entity_summary}}). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

Read the rows in `factions.yaml` (archetype with its `typical_forces` and `services`, fracture, endgame, secret kind, cult doctrine, the rungs, the links) and the stance reasons the skeleton wrote for your edges; you carry them verbatim.

### Write `{{prose_path}}` from `{{template}}` — the four phases
1. **Identity & Belief**: archetype, founding (a dated event), alignment range, public position, true belief vs cover (discoverable), relation to the gods, signature presentation (what a native sees and smells when this faction is in the room).
2. **Power & Resources**: power and intel, reach, military as SRD blocks from `typical_forces`, magical and economic means, territory and assets on the map (ids), the HQ.
3. **Structure & People**: leader, heir, the key-NPC web (roster stubs), the fracture as a named person with a reason, the betrayal candidate, the access chain (the rolled links in order), recruitment and exit terms, services to the party with prices, the stance table with the shared reason lines.
4. **Intelligence & Operations**: the network and intel delay, the machine doctrine (aggression, target preference, move weights per trigger), a prose reaction per trigger (`counts_as` in the trigger rows), the first operation with its dated steps, abandon-if and metric, the endgame, vulnerabilities, default policy toward the party, and the provocation ladder (3-4 rungs; the first is never war; party-asset rungs are marked as parked).
5. Discoverable: true belief, the ladder, what it wants independent of the party and what it will have done in 60 days. Secret (`{{mirror_path}}`): the secret kind made concrete, the fracture's hidden side, the operation's true objective if it differs.

### Fragment `{{fragment_path}}`
The faction row (fields per `docs/schemas/entities.md`; stamps `archetype`, `objective`), `refs`, `graph` edges (`member_of`, `controls`, `allied_with` with stance values), `seeds[]` only if the skeleton left store calls to you, `counts` (`rungs`, `links`, `steps`, `assets`).

The critic asks what this faction wants independent of the party and what it would have done in 60 days if the party never appeared; if the answer is "it waits", you failed.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
