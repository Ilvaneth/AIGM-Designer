---
phase: P5
role: writer
kind: npc
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — one NPC's dossier (major or supporting)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Entity **{{entity_id}}** ({{entity_name}}: {{entity_summary}}). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

Read the rows in `npcs.yaml`: the secret kind (its `path` and `tier`), the tic, and the axis rows (`weakness_from` names the weakness pattern your positions yield). The skeleton fixed name, species, gender, tier, faction, role, stat anchor, relationships with their reason lines, and the witness facts; you write the person.

### Write `{{prose_path}}` from `{{template}}` (secret layer to `{{mirror_path}}`)
- Appearance with species and one concrete detail; role / CR / location; faction, tier, alignment; demeanor; the speech quirk from the tic row and **two or three voice samples** in the NPC's own voice (Turkish, with English names) that no other NPC could have said; schedule; what they can offer the party and want from it; attitude to the party; current goal.
- The four axes at the rolled positions, each explained in one line; ≥2 relationships with the shared reason lines; Known Facts from the witness list and the role.
- Discoverable: motivation with history (a dated event), the weakness derived from the axes, what changes if they die (who succeeds, which thread breaks), the goal tracker line for a major NPC.
- Secret (mirror only): the secret made concrete along its kind, the surfacing path, `truth_of` if this NPC carries a PC's truth, notes for the DM.

A **lieutenant or the BBEG** (the prompt's directions say so) is written at the highest care: their objective is the villain's answer made policy, their origin a choice within the history, every portent of the front leaves a visible sign; two critics read the file.

### Fragment `{{fragment_path}}`
The NPC row (fields per `docs/schemas/entities.md`; stamps `faction`; `dm_only` with `secret_tr`, `surfacing_tr`, `weakness_tr`, `truth_of`, and `stamped_fields: [secret_tr]`), `refs`, `graph` edges only if the skeleton left them to you, `seeds[]` for `goals` (major tier: `goals add --kind npc` with metric and the three responses), `counts` (`relations`, `voice_samples`, `known_facts`).

The critic asks what this NPC wants that has nothing to do with the party, and for the one contradiction that makes them a person.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
