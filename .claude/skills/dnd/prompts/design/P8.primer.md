---
phase: P8
role: writer
kind: primer
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — one culture's section of the player primer

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Section **{{entity_id}}** (a polity or people the skeleton listed in `design/_staging/P8/skeleton.json`). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

The primer (`design/player-primer.md`, from `{{template}}`) is the one bible file fully open to the player: **the world as a native of this culture knows it**, nothing more. Write your culture's section: the regions, polities and who rules them as this culture sees them; the gods as worshipped here; the calendar, festivals, money and prices, languages, peoples; what everyone knows about magic and its regulator; famous places; the **taught history** (public layer only); current rumours (the day-0 news slice the skeleton gave you); **what is considered dangerous** (the over-tier sites' public telegraph lines: "nobody goes to the flats"); and, at the end of the primer, the sockets' spoiler-free questions in this culture's voice.

Rules: public tier only (a discoverable or secret sentence here is a leak; the validator runs on the primer); a native of this culture knows less about the other polities; every proper noun English, every sentence Turkish; ids never appear (write names; `render_player.py` checks); folklore may carry a song or a saying the history table planted.

Fragment `{{fragment_path}}`: `registry` is the section stub (`kind: primer_section`), `refs` the ids the section names, `counts` (`words`, `rumours`, `questions`). Also write `common-knowledge.json` facts for your culture into `design/_staging/P8/<id>.facts.json` (keywords, `text_tr`, `origin`, `refs`) for the script to merge.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
