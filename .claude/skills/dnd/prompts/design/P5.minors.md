---
phase: P5
role: writer
kind: npc
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — a batch of minor NPCs (up to six)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Batch **{{entity_id}}** stands for the minor NPCs the skeleton listed under it in `design/_staging/P5/skeleton.json`. Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Rolls of the batch's members
{{rolls}}

A minor NPC gets the index row and **three lines**: one physical detail, what they want, a speech tic (from the rolled tic row; no seed more than twice across the roster). No dossier, no secret section unless the skeleton gave them a secret kind, in which case one line in the mirror. Write `design/npcs/<id>.md` per member from `{{template}}` with only the Public section's first lines and the `### Minor` block the template shows; the fragment per member carries the registry row (stamps `faction`) and `refs`.

The batch fragment `{{fragment_path}}` has the batch stub as `registry` (`kind: batch`) and `counts.npcs` = members written. The player may latch onto any of these; `detail` promotes them later and raises `tier`.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
