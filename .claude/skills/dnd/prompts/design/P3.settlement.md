---
phase: P3
role: writer
kind: settlement
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — one town or city

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Entity **{{entity_id}}** ({{entity_name}}: {{entity_summary}}). Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

### Your rolls
{{rolls}}

Read the rows in `settlements.yaml`: the kind, the fear, the problem, the goods (their `pairs_with_need`), the districts (their `character`, `fear_seed`, `law_modifier`), and the anchor kinds. The skeleton fixed kind, region, polity, ruler stub, district and anchor ids; you fill them.

### Write `{{prose_path}}` from `{{template}}`
- Two or three sentences of arrival by horse: what is seen, smelled, heard.
- Scale, population inside the kind's band, wealth and law with who keeps it, polity, ruler, region; districts with a character line each; economy (sells / needs / price modifier from the wealth ladder); what the settlement fears (the rolled row made local); the local problem as a seed (hook + complication + resolution + reward, the `seed_` stub's fields in the fragment).
- The **anchor table**: the six anchors (plus the era's variants the dial names) with kind, owner stub, services from `anchor_kind` rows, one note each; the signature place references a `signature_` entity.
- The empty small-point table with its budget (never filled at birth).
- People here: the roster stubs assigned, with schedules.
- Rumours on day 0: two or three lines that P8 will turn into news records (leave the ids to P8).
- Discoverable: the two open truths and a price table for the sold goods (SRD equipment prices × the modifier). Secret (`{{mirror_path}}`): the third truth, who really holds power, clue placements, the fracture that runs through this town.

### Fragment `{{fragment_path}}`
The settlement row (fields per `docs/schemas/entities.md`; stamps `scale`, `polity`, `ruler_at_birth`), the `district_` and `place_` rows it covers (`covers:` in the front matter), the `seed_` row of the local problem, `graph` nodes and `located_in` edges, `seeds[]` for `graph`, `counts` (`anchors`, `districts`, `rumours`).

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
