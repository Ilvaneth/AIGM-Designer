---
phase: P9
role: writer
kind: session1
effort: high
critics: 1
schema: writer
---
{{common}}

## Your task — the session-1 pack

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Document **{{entity_id}}**. Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files (the start settlement, chapter 1, the threads' public faces, the arc's opening roll, the first detailed sites, `news.json`):
{{files}}

Write `{{prose_path}}`, the pack the DM opens at session 1 (risk 24.1 #20):
1. **The start settlement's small points** for day 0: fill enough of the budget for the first session (the inn's rooms and regulars, the market's stalls, the two or three places the opening touches), each a `place_` entity, permanent once named.
2. **Chapter 1's nodes with day-0 simulation applied**: who is where on the start date, each site's current state, the news records that reach the start settlement.
3. **Two candidate opening bangs** from the rolled opening scene type, neither in an inn, each tied to a socket or a beat, with the first ten minutes sketched and the first decision the party faces.
4. The DM's first-session checklist: the primer sections each PC's origin unlocks, the telegraphs already visible from the start settlement, the first travel table to use, the calendar's start date and the festival nearest to it.

Fragment `{{fragment_path}}`: `registry` is the document row (`kind: session1`, `secrecy: discoverable`), the `place_` rows created, `refs`, `counts` (`places`, `openings`, `news`).

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
