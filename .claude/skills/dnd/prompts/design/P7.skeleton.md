---
phase: P7
role: skeleton
kind: arc
effort: high
critics: 1
schema: skeleton
---
{{common}}

## Your task — the arc skeleton (Phase 7, single writer)

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. {{scale_line}}. Attempt {{attempt}}; directions:
{{directions}}

Read: `design/premise.md`, `design/dm-only/premise-secret.md`, `design/design.json` (`arc_skeleton`: chapter ids, acts, level bands), `design/sites.md`, every faction file's operation (the BBEG front's steps by id), `design/consequence-calculus.md`, `design/entities.json`, `data/design/arc.yaml`, `threads.yaml#socket_type`, `antagonists.yaml#escalation_stage`.

### Rolls of this phase
{{phase_rolls}}

A `_count` line is exact (`= **N**`): produce N and assign every ordinal roll; an unused ordinal, a member left out of a batch, or an invented extra fails the skeleton critic that reads you next.

Beat change kinds by beat template, node kinds per chapter, hook kinds per act, seed shapes, the opening scene type, the plot engine, the socket types.

### What to decide and write — `design/arc.md` from `templates/design/arc.md` (secret layer to `design/dm-only/arc.md`)
1. **The spine**: the beats of the scale's templates, each a `beat_` entity with the rolled `change_kind`, `state_before_tr` and `state_after_tr` as facts about the world, `what_changes` consequence-shaped (nothing matching `arc.yaml#forbidden_phrasing`), `world_pressure` = a BBEG front step id (`<operation>.<step>`), a telegraph scene, 2-3 delivery paths (sites, NPCs, news), and the three fallbacks (cost, secondary, deferred) written now.
2. **The threat escalation** wired to the front's steps (stage advances on steps, never on feel); the doom day from the front.
3. **Chapters**: for each `chapter_` of the arc skeleton, title, act, level band, drive, intended-path sites, key NPCs, the rolled node kinds as 3-5 `node_` entities (what is here, at stake, ways in, if never arrives wired to a step or a news record), the act's A/B scenario reference, the content-mix check, the XP share; the chapter files themselves are written by the chapter agents.
4. **Quest seeds** of the rolled shapes as `seed_` stubs (hook, complication, resolution, reward, tied_to, site or null); the seed writers fill them in sixes.
5. **Planted hooks**: 3-5 per act of the rolled kinds with payoff hints and status.
6. **Sockets**: party size × the scale's sockets per PC, each a `socket_` entity of the rolled type pointing at a node and a roster NPC (never a new one), with the primer's spoiler-free question.
7. **Endings**: win, loss, pyrrhic, each consequence-shaped with a world_after; the `arc_` entity with `acts`, `beats[]`, `chapters[]`, `doom_day`, `endings`. The opening scene type and plot engine recorded on the arc row.
8. The big secret's clues confirmed in act order against the sites.

Fragments under `design/_staging/P7/`: the arc, beats, chapters (stubs with `file: null`), nodes, seed stubs, sockets, hook records; `skeleton.json` with `roster` (chapter ids and `seedbatch_<n>` ids), `assignments`, `reads`.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
