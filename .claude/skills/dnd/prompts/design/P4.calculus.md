---
phase: P4
role: writer
kind: calculus
effort: medium
critics: 1
schema: writer
---
{{common}}

## Your task — the consequence calculus

Campaign **{{campaign}}** at `{{campaign_dir}}`. Dials: {{dials}}. Document **{{entity_id}}**. Attempt {{attempt}}; directions:
{{directions}}

Read exactly these files:
{{files}}

Read every faction dossier's ladder and the arc skeleton in `design/design.json` (`arc_skeleton`: acts and chapters). Write `{{prose_path}}` from `{{template}}`:

- **Provocation ladders** per major actor (each faction leader and each faction): the 3-4 rungs the dossier rolled, as a table of what the party did → what the actor does → on the calendar (delay days from the rung rows); the first rung is never war; a rung that touches a party asset is marked `park` and never resolved off-screen.
- **A / B scenarios by act**: for each act, an A path (the party leans toward one power) and a B path (toward its rival), each drawn from **that act's own actors** — never Act 1's factions carried into Act 2 — with the concrete shifts (which step moves earlier or later, which stance moves), and the list of the act's own actors by id.
- Discoverable: the ladders. Secret (`{{mirror_path}}`): the BBEG's ladder and what the front does when the party pulls a buy-time lever.

Fragment `{{fragment_path}}` is a **container** (`registry: null`; the calculus is a document, not a registry entity): `prose` and `dm_only_prose` name the two files, `refs` every id you linked, `counts` (`ladders`, `rungs`, `scenarios`). The public file's front matter is `entity: none`, `secrecy: discoverable`, `mirror: {{mirror_path}}`; the mirror's is `secrecy: secret`, `mirror_of: {{prose_path}}`; the `## Secret` heading exists only in the mirror.

Notes to `{{notes_path}}`. Return, as `{{agent_label}}`:
```json
{{schema}}
```
